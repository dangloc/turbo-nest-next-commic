---
phase: 36-user-profile-security-management
plan: 01
type: execute
wave: 1
depends_on: [26-01, 35-01]
files_modified:
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/local-auth.service.ts
  - apps/api/src/auth/dto/change-password.dto.ts
  - apps/api/src/auth/__tests__/auth.controller.spec.ts
  - apps/api/src/auth/__tests__/local-auth.service.spec.ts
  - apps/web/src/features/profile/types.ts
  - apps/web/src/features/profile/api.ts
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/src/features/dashboard/__tests__/dashboard-profile-security.dom.test.jsx
  - apps/web/app/globals.css
autonomous: true
requirements: [ACCOUNT-01]
must_haves:
  truths:
    - "Signed-in users can view current account identity fields and edit profile name/email from dashboard profile section"
    - "Profile updates persist and return immediate success/error feedback in UI"
    - "Users can change password only by providing current password and a valid new password"
    - "Weak password and invalid current password paths are blocked with explicit messages"
  artifacts:
    - path: "apps/api/src/auth/dto/change-password.dto.ts"
      provides: "validated password-change request contract"
      exports: ["ChangePasswordDto"]
    - path: "apps/api/src/auth/auth.controller.ts"
      provides: "profile identity update and password-change endpoints"
      exports: ["getProfile", "updateProfile", "changePassword"]
    - path: "apps/web/src/features/profile/api.ts"
      provides: "frontend profile + password change API wrappers"
      exports: ["fetchProfile", "updateProfile", "changePassword"]
    - path: "apps/web/src/features/dashboard/__tests__/dashboard-profile-security.dom.test.jsx"
      provides: "UI behavior tests for profile edit and password change flows"
      min_lines: 80
  key_links:
    - from: "apps/web/src/features/profile/api.ts"
      to: "/auth/profile"
      via: "PATCH profile identity updates"
      pattern: "auth/profile"
    - from: "apps/web/src/features/profile/api.ts"
      to: "/auth/password"
      via: "POST current/new password flow"
      pattern: "auth/password"
    - from: "apps/api/src/auth/auth.controller.ts"
      to: "apps/api/src/auth/local-auth.service.ts"
      via: "password verification and update orchestration"
      pattern: "changePassword"
---

<objective>
Deliver account profile and security management for phase 36 by extending the existing dashboard profile module with editable identity fields and secure password change.

Purpose: satisfy ACCOUNT-01 with backend-enforced security rules and dashboard-native UX.
Output: authenticated profile identity update + password-change backend contracts, frontend profile/security forms, and automated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-profile-management-dashboard/26-01-SUMMARY.md
@.planning/phases/35-frontend-local-authentication/35-01-PLAN.md
@apps/api/src/auth/auth.controller.ts
@apps/api/src/auth/local-auth.service.ts
@apps/api/src/auth/dto/register.dto.ts
@apps/api/src/auth/dto/login.dto.ts
@apps/api/src/auth/__tests__/auth.controller.spec.ts
@apps/api/src/auth/__tests__/local-auth.service.spec.ts
@apps/web/src/features/profile/types.ts
@apps/web/src/features/profile/api.ts
@apps/web/src/features/dashboard/dashboard.tsx
@apps/web/src/lib/auth/session-store.ts

<interfaces>
From apps/api/src/auth/auth.controller.ts:
```typescript
@Get('profile')
async getProfile(@Req() req: Request)

@Patch('profile')
async updateProfile(@Req() req: Request, @Body() body: ProfileUpdateInput)
```

From apps/api/src/auth/local-auth.service.ts:
```typescript
async login(input: LoginDto): Promise<LocalLoginResult>
async register(input: RegisterDto): Promise<LocalAuthResult>
```

From apps/web/src/features/profile/types.ts:
```typescript
export interface ProfileData {
  id: number;
  email: string;
  role: "USER" | "AUTHOR" | "ADMIN";
  nickname: string | null;
  avatar: string | null;
  updatedAt: string;
}
```

From apps/web/src/features/profile/api.ts:
```typescript
export async function fetchProfile(token?: string, signal?: AbortSignal): Promise<ApiResult<ProfileResponse>>;
export async function updateProfile(input: UpdateProfileInput, token?: string): Promise<ApiResult<ProfileResponse>>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add secure backend contracts for profile identity updates and password change</name>
  <files>
    apps/api/src/auth/dto/change-password.dto.ts
    apps/api/src/auth/local-auth.service.ts
    apps/api/src/auth/auth.controller.ts
    apps/api/src/auth/__tests__/local-auth.service.spec.ts
    apps/api/src/auth/__tests__/auth.controller.spec.ts
  </files>
  <behavior>
    - Test 1: profile update accepts editable identity fields (email plus display name mapped to nickname) with normalization, uniqueness checks, and validation errors for invalid formats.
    - Test 2: password change endpoint requires currentPassword and valid newPassword (8+ chars, at least one letter and one number).
    - Test 3: incorrect current password returns unauthorized error; valid current password updates hash and preserves login compatibility.
  </behavior>
  <action>
Implement ACCOUNT-01 backend contracts by extending existing auth module behavior from phase 26.

Details:
- Create `ChangePasswordDto` with class-validator rules for `currentPassword` and `newPassword`.
- Extend profile update path to support editing email and display name (use existing `nickname` field as display-name storage to avoid schema migration in this phase).
- Add uniqueness/format checks for email updates and preserve existing nickname/avatar behavior.
- Add authenticated `POST /auth/password` endpoint that delegates to `LocalAuthService` for secure verification and password hash update.
- Add service method that verifies current password using existing local/legacy compatibility logic before writing new bcrypt hash.
- Extend API/service tests for success and failure paths; avoid breaking existing local auth contracts.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/auth/__tests__/local-auth.service.spec.ts src/auth/__tests__/auth.controller.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Backend exposes secure profile identity and password-change contracts required by ACCOUNT-01 with passing regression-aware tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend dashboard profile section with editable identity form and password-change UX</name>
  <files>
    apps/web/src/features/profile/types.ts
    apps/web/src/features/profile/api.ts
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/src/features/dashboard/__tests__/dashboard-profile-security.dom.test.jsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: profile section loads current identity values and submits updated email/display name with success feedback.
    - Test 2: password-change form blocks weak passwords and mismatched confirmation before API request.
    - Test 3: password-change submit with invalid current password shows server error; successful change clears sensitive inputs and confirms update.
  </behavior>
  <action>
Deliver ACCOUNT-01 frontend behavior inside existing dashboard profile section.

Details:
- Extend profile contracts and API wrapper to support identity edits and password-change call (`POST /auth/password`).
- Update dashboard profile UI to include:
  - identity form (email + display name mapped to nickname)
  - dedicated password security form (`currentPassword`, `newPassword`, `confirmPassword`)
  - inline validation, loading states, success/error messaging.
- Keep AppContext/session-store sync after identity update so visible account labels refresh immediately.
- Add focused DOM tests for profile update and password-change behaviors using existing vitest/testing-library setup.
- Keep wallet/purchases/notifications rendering unchanged.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/features/dashboard/__tests__/dashboard-profile-security.dom.test.jsx && npx eslint src/features/profile/api.ts src/features/profile/types.ts src/features/dashboard/dashboard.tsx src/features/dashboard/__tests__/dashboard-profile-security.dom.test.jsx app/globals.css && npm run check-types</automated>
  </verify>
  <done>
Dashboard profile section supports profile identity editing and secure password changes with clear validation and feedback, fully satisfying ACCOUNT-01 UX.
  </done>
</task>

</tasks>

<verification>
- Authenticated user can view current profile identity values, edit display name/email, and see persisted results in dashboard.
- Password change requires current password plus strong new password, with failures and success clearly communicated.
- Existing dashboard modules and local auth login flow remain functional.
</verification>

<success_criteria>
ACCOUNT-01 is complete with backend-enforced password security and dashboard-native profile/security management UX.
</success_criteria>

<output>
After completion, create `.planning/phases/36-user-profile-security-management/36-01-SUMMARY.md`
</output>
