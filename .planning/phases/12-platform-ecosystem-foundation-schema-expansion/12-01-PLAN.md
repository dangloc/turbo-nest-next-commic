---
phase: 12-platform-ecosystem-foundation-schema-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/20260408xxxxxx_platform_ecosystem_schema/migration.sql
  - apps/api/src/etl/types.ts
  - apps/api/src/etl/__tests__/content-schema.spec.ts
  - apps/api/package.json
autonomous: true
requirements: [ECO-01, ECO-02, ECO-03, ECO-04, ECO-05, ECO-06, ECO-07, ECO-08, ECO-09, ECO-10, ECO-11, ECO-12]

must_haves:
  truths:
    - "Schema contains additive contracts for VIP tiers, author profiles, withdrawals, banners, gamification, reader history/bookmarks/reviews/notifications, and nested comments/reactions"
    - "Wallet split and user VIP/referral upgrades are migration-safe and preserve existing data without destructive reset"
    - "Novel and Chapter include additive BigInt view counters with non-breaking defaults"
    - "Comment nesting and reaction uniqueness constraints prevent duplicate user reactions per comment"
    - "Prisma schema validates and migration SQL is deterministic for replay in controlled environments"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "New ecosystem models/enums and upgraded existing models"
      contains: "VipLevel|AuthorProfile|WithdrawalRequest|Banner|Mission|UserMissionLog|PointTransaction|ReadingHistory|Bookmark|Review|Notification|Comment|CommentReaction"
    - path: "apps/api/prisma/migrations/20260408xxxxxx_platform_ecosystem_schema/migration.sql"
      provides: "Safe additive migration and wallet backfill strategy"
      contains: "ALTER TABLE|UPDATE wallet backfill|CREATE TABLE"
    - path: "apps/api/src/etl/types.ts"
      provides: "Runtime config compatibility for any new schema verification paths if needed"
      contains: "EtlConfig runtime compatibility"
  key_links:
    - from: "User.currentVipLevelId"
      to: "VipLevel.id"
      via: "optional relation for legacy-safe user rows"
      pattern: "currentVipLevelId"
    - from: "Wallet.balance"
      to: "Wallet.depositedBalance + Wallet.earnedBalance + Wallet.totalDeposited"
      via: "explicit additive migration backfill"
      pattern: "UPDATE wallet"
    - from: "CommentReaction"
      to: "Comment"
      via: "unique composite constraint (userId, commentId)"
      pattern: "@@unique([userId, commentId])"
---

<objective>
Define and apply a safe Prisma schema expansion for milestone v1.4 so platform ecosystem features are contract-ready without disrupting current production data.

Purpose: Establish durable schema foundations for VIP system, author center, gamification, banners, and advanced social/reader UX.
Output: Updated Prisma schema + additive migration SQL + validation checks.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-taxonomy-etl-backfill-verification/11-01-SUMMARY.md
@apps/api/prisma/schema.prisma

<interfaces>
From current schema contract direction:
```prisma
model User {
  id Int @id
  wallet Wallet?
}

model Wallet {
  userId Int @unique
  balance Decimal
}

model Novel {
  id Int @id
}

model Chapter {
  id Int @id
}
```

Target additions in this phase are additive-first and relation-safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.4 Enums and New Foundation Models</name>
  <files>
    apps/api/prisma/schema.prisma
  </files>
  <action>
    Add schema enums and new models required by ECO-01/ECO-05/ECO-06/ECO-07/ECO-09/ECO-10/ECO-12:

    1. VIP + Author Center models:
       - `VipLevel` with preserved integer `id`, `name`, `vndValue`, `kimTe`, optional `colorCode`, optional `iconUrl`.
       - `AuthorProfile` (1:1 with `User`) with pen name, bank metadata, and approval enum.
       - `WithdrawalRequest` with amount and status enum.

    2. Banner + Gamification models:
       - `Banner` with image/link, position enum, active window dates.
       - `Mission`, `UserMissionLog`, `PointTransaction` (non-fiat points ledger).

    3. Reader/Social models:
       - `ReadingHistory`, `Bookmark`, `Review` (rating constrained 1-5), `Notification`.
       - `Comment` with nullable `novelId`/`chapterId`, self-relation (`parentId`, `replies`).
       - `CommentReaction` with enum type and `@@unique([userId, commentId])`.

    Ensure indexes/uniques are added for read paths and dedupe safety, and keep model naming consistent with existing Prisma conventions.
  </action>
  <verify>
    <automated>npm run prisma:format --workspace=api && npm run prisma:validate --workspace=api</automated>
  </verify>
  <done>
    - All new v1.4 models/enums compile in Prisma schema.
    - Required uniqueness and relation constraints are present.
    - Review rating range and reaction uniqueness are enforced at schema level (constraint or enum-driven guard strategy documented).
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Existing Models with Backward-Compatible Additive Fields</name>
  <files>
    apps/api/prisma/schema.prisma,
    apps/api/prisma/migrations/20260408xxxxxx_platform_ecosystem_schema/migration.sql
  </files>
  <action>
    Implement ECO-02/ECO-03/ECO-04/ECO-08/ECO-11 by upgrading existing models safely:

    1. `User` upgrades:
       - Add optional `currentVipLevelId` relation to `VipLevel`.
       - Add unique `referralCode` and nullable self-relation `referredById`.
       - Keep all new fields nullable or defaulted where needed for existing rows.

    2. `Wallet` upgrades (safe split strategy):
       - Add `depositedBalance`, `earnedBalance`, `totalDeposited` with non-breaking defaults.
       - Migration SQL must backfill deterministically from legacy `balance`:
         - initial `depositedBalance = balance`
         - initial `earnedBalance = 0`
         - initial `totalDeposited = balance`
       - Preserve existing `balance` in the same migration (do not drop immediately). Mark deprecation in comments for future cleanup phase.

    3. `Novel` and `Chapter` upgrades:
       - Add `viewCount BigInt @default(0)` to both models.

    4. Generate migration SQL and harden for replay-safe local execution patterns.
  </action>
  <verify>
    <automated>npm run prisma:generate --workspace=api && npm run check-types --workspace=api</automated>
  </verify>
  <done>
    - Existing data remains representable after migration.
    - Wallet split and cumulative total fields are backfilled deterministically.
    - View counters and user referral/VIP links are additive and non-breaking.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Schema Safety Validation and Migration Notes for Execution</name>
  <files>
    apps/api/src/etl/__tests__/content-schema.spec.ts,
    apps/api/package.json
  </files>
  <action>
    Add minimal automated safety checks and execution helpers for this schema-only phase:

    1. Extend or add schema tests to assert presence of critical v1.4 contracts in `schema.prisma`:
       - `VipLevel`, `AuthorProfile`, `CommentReaction` unique composite,
       - `Wallet` split fields,
       - `Novel.viewCount` + `Chapter.viewCount`.

    2. Add/update script aliases if needed to standardize validation command flow for this phase.

    3. Ensure verification commands remain under 60s and do not require live DB connectivity for structural checks.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/content-schema.spec.ts && npm run prisma:validate --workspace=api</automated>
  </verify>
  <done>
    - Structural schema contracts are machine-checked.
    - Developers can run a single repeatable command set for schema verification before migration apply.
  </done>
</task>

</tasks>

<verification>
- Prisma schema format and validate pass.
- Type generation passes after schema changes.
- Schema-focused tests cover critical v1.4 contracts.
- Migration SQL backfill for wallet split is explicit and deterministic.
</verification>

<success_criteria>
- Phase 12 introduces all required schema contracts for ECO-01 through ECO-12.
- No destructive changes to legacy wallet balance path in this phase.
- Existing user/content/taxonomy records remain compatible with upgraded schema.
</success_criteria>

<output>
After completion, create `.planning/phases/12-platform-ecosystem-foundation-schema-expansion/12-01-SUMMARY.md`
</output>
