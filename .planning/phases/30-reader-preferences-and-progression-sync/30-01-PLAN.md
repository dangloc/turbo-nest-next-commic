---
phase: 30-reader-preferences-and-progression-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/reader/types.ts
  - apps/api/src/reader/reader-personal.controller.ts
  - apps/api/src/reader/reader.service.ts
  - apps/api/src/reader/__tests__/reader-progression-sync.spec.ts
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "Authenticated chapter-open events are accepted through a dedicated reader sync endpoint"
    - "Repeated open events for the same user and chapter do not double-increment chapter/novel view counters"
    - "Backend returns deterministic sync outcome data so frontend can resume safely"
  artifacts:
    - path: "apps/api/src/reader/reader-personal.controller.ts"
      provides: "Authenticated chapter-open sync route"
      exports: ["POST /reader/me/chapter-opens"]
    - path: "apps/api/src/reader/reader.service.ts"
      provides: "Idempotent progression sync logic"
      exports: ["syncChapterOpen()"]
    - path: "apps/api/src/reader/types.ts"
      provides: "Progression sync request/response contracts"
      exports: ["ReaderChapterOpenInput", "ReaderChapterOpenResult"]
    - path: "apps/api/src/reader/__tests__/reader-progression-sync.spec.ts"
      provides: "Idempotent view count update behavior tests"
  key_links:
    - from: "apps/api/src/reader/reader-personal.controller.ts"
      to: "apps/api/src/reader/reader.service.ts"
      via: "controller delegation"
      pattern: "this\\.readerService\\.syncChapterOpen"
    - from: "apps/api/src/reader/reader.service.ts"
      to: "prisma.readingHistory|prisma.chapter|prisma.novel"
      via: "first-open check and conditional view increment"
      pattern: "prisma\\.(readingHistory|chapter|novel)"
---

<objective>
Introduce backend progression-sync primitives so chapter-open analytics can be recorded idempotently for authenticated readers.

Purpose: Satisfy SYNC-01 by moving authenticated chapter-open tracking into a dedicated endpoint with deterministic first-open behavior.
Output: New personal sync endpoint, service logic, and test coverage for idempotent chapter-open updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reader-content-apis/16-01-SUMMARY.md
@.planning/phases/29-core-chapter-reader-interface/29-01-SUMMARY.md
@apps/api/src/reader/types.ts
@apps/api/src/reader/reader-personal.controller.ts
@apps/api/src/reader/reader.service.ts

<interfaces>
From apps/api/src/reader/reader-personal.controller.ts:
```typescript
@Put('reading-history')
async upsertReadingHistory(...)
```

From apps/api/src/reader/reader.service.ts:
```typescript
async upsertReadingHistory(userId: number, input: ReadingHistoryInput)
async readChapter(chapterId: number, novelId?: number)
```

From apps/api/src/reader/types.ts:
```typescript
export interface ReadingHistoryInput {
  novelId: number;
  chapterId?: number;
  progressPercent: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add progression sync contracts and failing idempotence tests</name>
  <files>
    apps/api/src/reader/types.ts
    apps/api/src/reader/__tests__/reader-progression-sync.spec.ts
  </files>
  <behavior>
    - Test 1: First chapter-open sync call increments chapter and novel view counts.
    - Test 2: Repeated sync call for same user and chapter does not increment again.
    - Test 3: Unknown chapter returns not-found behavior.
  </behavior>
  <action>
Define explicit chapter-open sync contracts and create RED tests that assert idempotent view count behavior tied to authenticated reader identity and chapter identity.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-progression-sync.spec.ts</automated>
  </verify>
  <done>
    Contracts compile and RED tests capture first-open vs repeat-open idempotence guarantees.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated chapter-open sync endpoint</name>
  <files>
    apps/api/src/reader/reader-personal.controller.ts
    apps/api/src/reader/reader.service.ts
  </files>
  <behavior>
    - Test 1: POST chapter-open sync endpoint delegates to service and returns sync result shape.
    - Test 2: Service records first-open with view increments and reading-history upsert.
    - Test 3: Service returns repeat-open state without duplicate increments.
  </behavior>
  <action>
Add `POST /reader/me/chapter-opens` protected under existing personal reader guards and implement `syncChapterOpen` in reader service. Sync logic must resolve chapter identity, upsert reading-history state, and increment view counters only on first observed open for that user/chapter. Preserve existing discovery and chapter-read routes for compatibility.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-progression-sync.spec.ts src/reader/__tests__/reader-personal.spec.ts && npm run check-types</automated>
  </verify>
  <done>
    Authenticated chapter-open sync is idempotent, tested, and type-safe.
  </done>
</task>

</tasks>

<verification>
- First-open and repeated-open behavior is covered by automated tests.
- Endpoint remains under existing authenticated reader namespace.
- API typecheck remains green.
</verification>

<success_criteria>
- SYNC-01 backend contract exists and is idempotent for authenticated chapter opens.
- Frontend can consume a stable sync result for progression handling in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/30-reader-preferences-and-progression-sync/30-01-SUMMARY.md`
</output>
