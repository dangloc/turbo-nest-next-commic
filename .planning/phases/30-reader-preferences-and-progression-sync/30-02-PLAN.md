---
phase: 30-reader-preferences-and-progression-sync
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/api.ts
  - apps/web/src/features/reader/reader.tsx
autonomous: true
requirements: [SYNC-01, SYNC-02]

must_haves:
  truths:
    - "Opening a chapter while authenticated triggers backend progression sync without duplicate analytics spikes"
    - "Reader resume position is restored from server-side reading history when available"
    - "Progress updates continue to persist during session and across reloads"
  artifacts:
    - path: "apps/web/src/features/reader/types.ts"
      provides: "Frontend progression sync response contracts"
      exports: ["ReaderChapterOpenResult"]
    - path: "apps/web/src/features/reader/api.ts"
      provides: "Authenticated chapter-open sync API bridge"
      exports: ["syncReaderChapterOpen()"]
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Chapter open + resume integration with sync endpoint"
  key_links:
    - from: "apps/web/src/features/reader/api.ts"
      to: "/reader/me/chapter-opens"
      via: "authenticated apiRequest"
      pattern: "reader/me/chapter-opens"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/reader/api.ts"
      via: "syncReaderChapterOpen call during chapter initialization"
      pattern: "syncReaderChapterOpen"
---

<objective>
Wire authenticated progression synchronization into the reader frontend so chapter opens and resume history are safely persisted and restored.

Purpose: Complete SYNC-01 and SYNC-02 user behavior by integrating new backend sync primitives into chapter initialization and progress flows.
Output: Reader frontend sync bridge + chapter-open integration + resume restoration consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-reader-experience-ui/22-01-SUMMARY.md
@.planning/phases/29-core-chapter-reader-interface/29-02-SUMMARY.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-01-PLAN.md
@apps/web/src/features/reader/types.ts
@apps/web/src/features/reader/api.ts
@apps/web/src/features/reader/reader.tsx

<interfaces>
From apps/web/src/features/reader/api.ts:
```typescript
export async function fetchChapterById(chapterId: number): Promise<ApiResult<ReaderChapter>>;
export async function fetchReadingHistory(token?: string, novelId?: number): Promise<ApiResult<ReadingHistoryEntry[]>>;
export async function upsertReadingHistory(input: ReadingHistoryUpsertInput, token?: string)
```

From apps/web/src/features/reader/reader.tsx:
```typescript
useEffect(() => { ... chapter initialization ... }, [chapterId])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend chapter-open sync contracts and API wrapper</name>
  <files>
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/api.ts
  </files>
  <behavior>
    - Test 1: Sync API wrapper sends authenticated chapter-open payload and receives typed sync response.
    - Test 2: Sync API wrapper surfaces backend errors for UI handling.
  </behavior>
  <action>
Add reader sync types and implement `syncReaderChapterOpen` API wrapper targeting the authenticated chapter-open endpoint from plan 30-01. Keep existing chapter fetch and history APIs intact for compatibility.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Reader frontend has typed API surface for backend chapter-open synchronization.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate chapter-open sync and robust resume restoration in reader view</name>
  <files>
    apps/web/src/features/reader/reader.tsx
  </files>
  <behavior>
    - Test 1: Authenticated chapter initialization triggers one sync call with chapter and novel ids.
    - Test 2: Resume progress restores from reading history entry when available.
    - Test 3: Unauthenticated flow skips sync call and keeps stable fallback messaging.
  </behavior>
  <action>
Update chapter initialization flow to call `syncReaderChapterOpen` for authenticated users after chapter and context fetch succeed. Align history restore logic so resume progress and unlock state derive from server reading history first, then maintain progress updates through existing save actions. Ensure no duplicate sync calls during a single chapter mount lifecycle.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types && npm run lint -- src/features/reader/reader.tsx src/features/reader/api.ts src/features/reader/types.ts</automated>
  </verify>
  <done>
    Reader chapter flow performs authenticated progression sync and resume restoration without regressions.
  </done>
</task>

</tasks>

<verification>
- Authenticated chapter-open path calls new sync API exactly once per mount cycle.
- Resume behavior remains stable when history exists or is absent.
- Web typecheck and targeted lint remain green.
</verification>

<success_criteria>
- SYNC-01 and SYNC-02 are satisfied through frontend-backend integration for chapter open and resume persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/30-reader-preferences-and-progression-sync/30-02-SUMMARY.md`
</output>
