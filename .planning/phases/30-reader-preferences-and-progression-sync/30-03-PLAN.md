---
phase: 30-reader-preferences-and-progression-sync
plan: 03
type: execute
wave: 3
depends_on: [30-02]
files_modified:
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/reader.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [READER-03, READER-04]

must_haves:
  truths:
    - "Reader can adjust font size in chapter view and changes apply immediately"
    - "Reader can switch between light and dark reading themes in chapter view"
    - "Reader preferences persist across page refresh and chapter navigation"
  artifacts:
    - path: "apps/web/src/features/reader/types.ts"
      provides: "Reader preference option contracts"
      exports: ["ReaderFontSizeOption", "ReaderThemeMode"]
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Preference controls and applied reader presentation state"
    - path: "apps/web/app/globals.css"
      provides: "Reader theme and typography style variants"
  key_links:
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "window.localStorage"
      via: "preference persistence"
      pattern: "localStorage"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/app/globals.css"
      via: "theme/font-size class names on reader content container"
      pattern: "reader-theme|reader-font"
---

<objective>
Add in-reader font-size and theme controls with immediate visual updates and persistence across sessions.

Purpose: Complete READER-03 and READER-04 while preserving the immersive chapter interface delivered in Phase 29.
Output: Reader preference controls, persisted settings, and style variants for font size and light/dark modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-chapter-reader-interface/29-02-SUMMARY.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-02-PLAN.md
@apps/web/src/features/reader/types.ts
@apps/web/src/features/reader/reader.tsx
@apps/web/app/globals.css

<interfaces>
From apps/web/src/features/reader/reader.tsx:
```typescript
export function ChapterReaderView({ chapterId }: { chapterId: number })
```

From apps/web/app/globals.css:
```css
.reader-content { ... }
.reader-card { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define reader preference model and persistence helpers</name>
  <files>
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/reader.tsx
  </files>
  <behavior>
    - Test 1: Font-size option changes update active reader typography class immediately.
    - Test 2: Theme-mode changes update active reader theme class immediately.
    - Test 3: Saved preferences restore on reload from local storage.
  </behavior>
  <action>
Add reader preference types and implement local preference state in chapter reader with deterministic load/save helpers. Persist selected font size and theme mode in local storage under namespaced reader keys.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Reader preference state model exists and persisted values rehydrate on chapter load.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render preference controls and style variants for light/dark + typography</name>
  <files>
    apps/web/src/features/reader/reader.tsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: Reader control bar exposes font size options and theme toggle controls.
    - Test 2: Light and dark modes are visually distinct and readable for long-form content.
    - Test 3: Font size variants apply to chapter content body without affecting navigation controls.
  </behavior>
  <action>
Add an in-reader preferences control section near chapter content with font size selector and theme mode toggle. Apply preference-driven class names to chapter content container and extend `globals.css` with corresponding style variants. Preserve mobile and desktop readability.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types && npm run lint -- src/features/reader/reader.tsx src/features/reader/types.ts app/globals.css</automated>
  </verify>
  <done>
    Reader preference controls function, persist, and visually update chapter content for both theme and typography options.
  </done>
</task>

</tasks>

<verification>
- Preferences apply instantly and survive reload.
- Reader content remains readable across both theme modes and font-size variants.
- Targeted web typecheck and lint are green.
</verification>

<success_criteria>
- READER-03 is satisfied with immediate font-size adjustment in reader.
- READER-04 is satisfied with immediate light/dark mode toggle in reader.
</success_criteria>

<output>
After completion, create `.planning/phases/30-reader-preferences-and-progression-sync/30-03-SUMMARY.md`
</output>
