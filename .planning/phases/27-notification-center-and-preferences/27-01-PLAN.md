---
phase: 27-notification-center-and-preferences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/notifications/notifications.controller.ts
  - apps/api/src/notifications/notifications.service.ts
  - apps/api/src/notifications/__tests__/notifications.controller.spec.ts
  - apps/web/src/features/notifications/types.ts
  - apps/web/src/features/notifications/api.ts
  - apps/web/src/features/notifications/notifications.tsx
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [NOTI-01, NOTI-02, NOTI-03]

must_haves:
  truths:
    - "User can see notification inbox grouped by read/unread state"
    - "User can mark individual notifications as read"
    - "User can mark all notifications as read at once"
    - "User can toggle notification preferences and settings persist"
    - "Dashboard notifications section shows unread count badge"
    - "Notification state changes are reflected without page reload"
  artifacts:
    - path: "apps/api/src/notifications/notifications.controller.ts"
      provides: "Authenticated notification API endpoints"
      exports: ["GET /notifications", "PATCH /notifications/:id/read", "PATCH /notifications/read-all", "GET /notifications/preferences", "PATCH /notifications/preferences"]
    - path: "apps/api/src/notifications/notifications.service.ts"
      provides: "Notification query and mutation operations"
      exports: ["getNotifications()", "markRead()", "markReadAll()", "getPreferences()", "updatePreferences()"]
    - path: "apps/web/src/features/notifications/types.ts"
      provides: "Frontend notification contracts and types"
      exports: ["Notification", "NotificationPreferences", "NotificationGroup"]
    - path: "apps/web/src/features/notifications/api.ts"
      provides: "Frontend API wrappers for notification operations"
      exports: ["fetchNotifications()", "markRead()", "markAllRead()", "updatePreferences()"]
    - path: "apps/web/src/features/notifications/notifications.tsx"
      provides: "Notification inbox component with grouping and actions"
      min_lines: 80
    - path: "apps/web/src/features/dashboard/dashboard.tsx"
      provides: "Dashboard integration of notifications section"
      pattern: "renderNotificationsSection"
  key_links:
    - from: "apps/web/src/features/notifications/notifications.tsx"
      to: "apps/api/src/notifications/notifications.controller.ts"
      via: "fetch calls to /api/notifications"
      pattern: "fetch.*api/notifications"
    - from: "apps/api/src/notifications/notifications.controller.ts"
      to: "prisma.notification"
      via: "service queries"
      pattern: "prisma\\.notification\\.(find|update)"
    - from: "apps/web/src/features/dashboard/dashboard.tsx"
      to: "apps/web/src/features/notifications/notifications.tsx"
      via: "section routing on ?section=notifications"
      pattern: "section.*notifications.*renderNotifications"
---

<objective>
Build complete notification center for user account with inbox, read state management, and preference controls.

Purpose: Enable users to stay informed of account activity, manage notification states, and control notification preferences from the dashboard.
Output: Notification API endpoints, service layer, frontend module, and integrated dashboard section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-wallet-top-up-and-chapter-purchases/25-01-SUMMARY.md

## Codebase Context

**Backend notification model:**
- `prisma/schema.prisma` contains `Notification` model with fields: id, userId, type (enum: SYSTEM, NOVEL_UPDATE, COMMENT_REPLY, COMMENT_REACTION, MISSION, REWARD), title, message, metadata (JSON), isRead, createdAt, readAt
- Index on (userId, isRead, createdAt) for efficient filtering
- `NotificationPreference` model exists for per-user preference toggles

**Frontend dashboard structure:**
- Dashboard section navigation via ?section={id} query param
- Existing sections: wallet, purchases, profile (phase 24-26 completed)
- Dashboard shell in `apps/web/src/features/dashboard/dashboard.tsx` with `renderNotificationsSection()` stub

**Established patterns:**
- Feature-local structure: `src/features/{feature}/(types|api|component).ts`
- Bearer token auth via session storage
- Dashboard API helpers in `apps/web/src/features/dashboard/api.ts`
- Styles appended to `apps/web/app/globals.css`

## Interfaces from Prior Phases

From dashboard (Phase 24):
```typescript
export type DashboardSectionId = "wallet" | "purchases" | "profile" | "notifications";
export interface DashboardSection {
  id: DashboardSectionId;
  title: string;
  description: string;
  href: string;
  phaseLabel: string;
}
export interface DashboardSummaryCard {
  id: DashboardSectionId;
  title: string;
  value: string;  // "Unread: N"
  subtitle: string;
  href: string;
  ctaLabel: string;
  phaseLabel: string;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend notification API endpoints, service, and tests</name>
  <files>
    apps/api/src/notifications/notifications.controller.ts
    apps/api/src/notifications/notifications.service.ts
    apps/api/src/notifications/__tests__/notifications.controller.spec.ts
  </files>
  <action>
Create notification API and service layer:

**File: notifications.controller.ts**
- Create authenticated controller with RolesGuard for user-scoped access
- Implement endpoints:
  - `GET /notifications` query params (limit, skip, filter by read status) returning paginated list with count
  - `PATCH /notifications/:id/read` to mark single notification as read, return updated notification
  - `PATCH /notifications/read-all` batch marks all unread as read, return affected count
  - `GET /notifications/preferences` returns user's preference toggles (by NotificationType)
  - `PATCH /notifications/preferences` updates preference flags, return updated preferences
- Use session token from request.user for userId context
- Add proper error handling and 404/400 responses

**File: notifications.service.ts**
- Create injectable service with Prisma queries:
  - `getNotifications(userId, limit?, skip?, isRead?)` with proper ordering and filtering
  - `markRead(userId, notificationId)` checks ownership before updating
  - `markReadAll(userId)` batch update with atomic transaction
  - `getPreferences(userId)` or create defaults if missing
  - `updatePreferences(userId, preferences)` upsert operation
- Use database index (userId, isRead, createdAt) for efficient queries

**File: notifications.controller.spec.ts**
- Add 5+ tests covering:
  - GET /notifications returns unread/read split with pagination
  - PATCH /notifications/:id/read succeeds and marks as read
  - PATCH /notifications/:id/read with wrong userId returns 403
  - PATCH /notifications/read-all marks all unread and returns count
  - GET/PATCH preferences endpoints work end-to-end
- All tests pass; npm test --workspace=api passes

Verify notification endpoints are available and working via npm test.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/notifications/__tests__/notifications.controller.spec.ts && npm run lint:check && npm run typecheck</automated>
  </verify>
  <done>
    - Notification API endpoints implemented at /notifications, /notifications/:id/read, /notifications/read-all
    - Preferences endpoints at GET/PATCH /notifications/preferences  
    - Service layer handles all queries and mutations correctly
    - All tests pass, API typechecks without errors
    - Code follows existing NestJS patterns (roles guards, service injection, error handling)
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend notification feature module and dashboard section integration</name>
  <files>
    apps/web/src/features/notifications/types.ts
    apps/web/src/features/notifications/api.ts
    apps/web/src/features/notifications/notifications.tsx
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/app/globals.css
  </files>
  <action>
Create notification feature module and integrate into dashboard:

**File: types.ts**
- Define TypeScript contracts:
  - `Notification` interface: id, userId, type, title, message, metadata, isRead, createdAt, readAt
  - `NotificationType` enum matching backend (SYSTEM, NOVEL_UPDATE, COMMENT_REPLY, COMMENT_REACTION, MISSION, REWARD)
  - `NotificationGroup` interface: groupType ("unread" | "read"), notifications: Notification[], count: number
  - `NotificationPreferences` interface: toggles by type (enabled: boolean per NotificationType)
  - `MarkReadResult` interface: success: boolean, affectedCount?: number

**File: api.ts**
- Create API wrappers with Bearer token auth:
  - `fetchNotifications(limit?, skip?)` returns { unread: Notification[], read: Notification[], total: number }
  - `markRead(notificationId)`  calls PATCH /notifications/:id/read
  - `markAllRead()` calls PATCH /notifications/read-all  
  - `fetchPreferences()` returns NotificationPreferences
  - `updatePreferences(prefs)` calls PATCH /notifications/preferences with updated toggles
  - All use getStorageToken() for Bearer auth (follow finance/profile pattern)

**File: notifications.tsx**
- Create NotificationsSection component (default export):
  - Fetch notifications and group by unread/read on mount and after each action via useEffect
  - Render two sections: "Unread (N)" and "Read (N)" with collapsible groups
  - For each notification:
    - Show notification icon by type (COMMENT_REPLY → comment icon, REWARD → star, etc.)
    - Show title, message, and relative time (createdAt)
    - Show read/unread badge or icon
    - Add single-read checkbox/button for unread items, remove for read items
  - Add "Mark all as read" button at top (visible when unread count > 0)
  - On single mark read: optimistically update UI, call API, revalidate if error
  - On mark all: disable button, call API, refresh full list
  - Add onClick handlers for notification items to set active detail or trigger action (future)
  - Include preferences toggle panel below notifications (show/hide via button):
    - Show toggles for each NotificationType (SYSTEM, NOVEL_UPDATE, COMMENT_REPLY, COMMENT_REACTION, MISSION, REWARD)
    - On toggle change: update UI optimistically, call updatePreferences, revalidate if error
  - Add loading and error states with message rendering
  - Minimum 80 lines

**File: dashboard.tsx**
- Update renderNotificationsSection() implementation (currently stub):
  - Import NotificationsSection component
  - Render NotificationsSection component within section container
  - Do NOT call separate data fetching (NotificationsSection handles it internally)
  - Preserve wallet/purchases/profile section renders unchanged

**File: globals.css**
- Add notification-specific styles:
  - `.notification-item` — card layout with padding, border, and hover state
  - `.notification-unread` — bold text and background highlight
  - `.notification-read` — lighter text color
  - `.notification-icon-{type}` — type-specific icon styles (comment, star, system, etc.)
  - `.notification-badge` — unread count badge styling  
  - `.notification-preferences-panel` — settings panel layout
  - `.notification-preferences-toggle` — switch/checkbox styling
  - Responsive styles for mobile (full-width cards on sm, grid on md+)

Verify frontend compiles and integrates into dashboard via npm run lint + npm run check-types. Manually verify dashboard section=notifications renders without errors.
  </action>
  <verify>
    <automated>cd apps/web && npm run lint --max-warnings 0 && npm run check-types</automated>
  </verify>
  <done>
    - Notification feature module created in apps/web/src/features/notifications
    - Frontend types, API wrappers, and NotificationsSection component implemented
    - Dashboard notifications section renders when user navigates to dashboard?section=notifications
    - Notification grouping (unread/read) displays with counts
    - Mark single and mark all read actions are wired to API
    - Preference toggles render and persist
    - Web app lints and typechecks without errors
    - Code follows established feature-module patterns (types/api/component split)
  </done>
</task>

</tasks>

<verification>
**QA steps after both tasks complete:**

1. API verification:
   - npm test --workspace=api passes with notification tests
   - Typecheck and lint pass in API workspace
   - No regressions in existing financial/profile/auth tests

2. Frontend verification:
   - Web app lints and typechecks without errors
   - npm run build succeeds for web app
   - No import/export mismatches

3. Integration check (manual):
   - Start dev server: npm run dev
   - Sign in with any account
   - Navigate to /dashboard
   - Navigate to ?section=notifications (or click notifications card)
   - Verify section renders without console errors
   - Backend must have seeded notifications for display (or verify 0 unread/read states show correctly)
</verification>

<success_criteria>
✅ NOTI-01 complete: Users can view notification inbox with unread/read grouping
- Inbox lists notifications grouped by read state
- Unread count displayed prominently

✅ NOTI-02 complete: Users can mark single and all notifications as read
- Single notification has read action (checkbox or button)
- Mark all button available in UI
- Actions update state without page reload

✅ NOTI-03 complete: Users can manage notification preferences
- Preference toggles visible in notifications section
- Toggles persist after update
- Settings reflected on next load

✅ Backend requirements:
- RolesGuard protects user-scoped endpoints
- Service layer uses database index efficiently
- Tests cover all endpoints

✅ Frontend requirements:
- Feature module follows established pattern (types/api/component)
- Dashboard integration maintains existing section behavior
- Styling consistent with dashboard theme
- No type errors or lint warnings

✅ Code quality:
- All API tests pass
- All frontend lint/typecheck passes
- Commits are atomic per task
</success_criteria>

<output>
After completion, create `.planning/phases/27-notification-center-and-preferences/27-01-SUMMARY.md` with:
- Phase number, plan number, status (complete)
- Requirements addressed: [NOTI-01, NOTI-02, NOTI-03]
- What was built (backend + frontend)
- Files created/modified  
- Verification results
- Commits created
- Duration in minutes
</output>
