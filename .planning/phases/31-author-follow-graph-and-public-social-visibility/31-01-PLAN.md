---
phase: 31-author-follow-graph-and-public-social-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/reader/types.ts
  - apps/api/src/reader/reader-discovery.controller.ts
  - apps/api/src/reader/reader-personal.controller.ts
  - apps/api/src/reader/reader.service.ts
  - apps/api/src/reader/__tests__/reader-author-follow.spec.ts
autonomous: true
requirements: [CREATOR-01, CREATOR-02]
must_haves:
  truths:
    - "Authenticated readers can follow and unfollow a public author idempotently"
    - "Author profile payload includes follower count and viewer follow state"
    - "Public author profile responses still work for unauthenticated users"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Author follow relation model"
      contains: "model AuthorFollow"
    - path: "apps/api/src/reader/reader-personal.controller.ts"
      provides: "Authenticated follow/unfollow endpoints"
      exports: ["POST /reader/me/follows/authors/:authorId", "DELETE /reader/me/follows/authors/:authorId"]
    - path: "apps/api/src/reader/reader.service.ts"
      provides: "Follow graph persistence and author-profile follower projection"
      exports: ["followAuthor", "unfollowAuthor", "getAuthorProfile"]
    - path: "apps/api/src/reader/__tests__/reader-author-follow.spec.ts"
      provides: "Follow/unfollow and profile follower-state tests"
  key_links:
    - from: "apps/api/src/reader/reader-personal.controller.ts"
      to: "apps/api/src/reader/reader.service.ts"
      via: "followAuthor/unfollowAuthor delegation"
      pattern: "readerService\\.(followAuthor|unfollowAuthor)"
    - from: "apps/api/src/reader/reader.service.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "authorFollow prisma relation queries"
      pattern: "prisma\\.authorFollow"
---

<objective>
Ship backend follow graph contracts and APIs so authenticated users can toggle follows while public author profile responses expose follower visibility fields.

Purpose: Satisfy CREATOR-01 and backend half of CREATOR-02 with idempotent write behavior and public read projection.
Output: Prisma follow model, API contracts, controller routes, service logic, and tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-social-interaction-apis/17-01-SUMMARY.md
@.planning/phases/28-public-author-profiles/28-01-SUMMARY.md
@apps/api/prisma/schema.prisma
@apps/api/src/reader/types.ts
@apps/api/src/reader/reader-discovery.controller.ts
@apps/api/src/reader/reader-personal.controller.ts
@apps/api/src/reader/reader.service.ts

<interfaces>
From apps/api/src/reader/reader-discovery.controller.ts:
```typescript
@Get('authors/:id')
async getAuthorProfile(@Param('id', ParseIntPipe) id: number, ...)
```

From apps/api/src/reader/reader-personal.controller.ts:
```typescript
@UseGuards(RolesGuard)
@Roles('USER', 'AUTHOR', 'ADMIN')
```

From apps/api/src/reader/types.ts:
```typescript
export interface AuthorStats {
  totalPublishedNovels: number;
  totalViews: number;
  latestUpdateAt: Date | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add follow graph contracts, schema model, and RED behavior tests</name>
  <files>
    apps/api/prisma/schema.prisma
    apps/api/src/reader/types.ts
    apps/api/src/reader/__tests__/reader-author-follow.spec.ts
  </files>
  <behavior>
    - Test 1: Follow endpoint creates relation on first call and is idempotent on repeat.
    - Test 2: Unfollow endpoint removes relation and remains idempotent when relation is already absent.
    - Test 3: Author profile stats response includes followerCount and viewerFollowsAuthor fields.
  </behavior>
  <action>
Define follow graph domain contracts and Prisma relation model keyed by follower user and author user/profile identifiers. Add focused RED tests for idempotent follow mutations and profile follower-state projection before implementing controller/service logic.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-author-follow.spec.ts</automated>
  </verify>
  <done>
    Follow graph contracts and tests describe required behavior for idempotent writes and public profile read projection.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement follow/unfollow APIs and author profile follower projection</name>
  <files>
    apps/api/src/reader/reader-personal.controller.ts
    apps/api/src/reader/reader-discovery.controller.ts
    apps/api/src/reader/reader.service.ts
  </files>
  <behavior>
    - Test 1: Authenticated follow call succeeds and returns deterministic followed state.
    - Test 2: Authenticated unfollow call succeeds and returns deterministic unfollowed state.
    - Test 3: Public author profile includes follower count, while authenticated requests also include viewer follow state.
  </behavior>
  <action>
Add guarded routes for follow/unfollow in reader personal controller and implement service methods using Prisma follow relation upsert/delete patterns for idempotence. Extend author profile response projection in reader service to return follower count and viewer follow state, while preserving existing author-not-found and catalog pagination behavior.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-author-follow.spec.ts src/reader/__tests__/reader-author-profile.spec.ts && npm run check-types</automated>
  </verify>
  <done>
    Backend follow graph endpoints and profile follower visibility are implemented and validated.
  </done>
</task>

</tasks>

<verification>
- Follow and unfollow flows are idempotent for authenticated readers.
- Author profile payload exposes follower count and viewer follow state without breaking public access.
- API tests and typecheck pass.
</verification>

<success_criteria>
- CREATOR-01 is met via authenticated follow/unfollow behavior.
- CREATOR-02 backend contract is met via follower count and viewer follow-state projection.
</success_criteria>

<output>
After completion, create `.planning/phases/31-author-follow-graph-and-public-social-visibility/31-01-SUMMARY.md`
</output>
