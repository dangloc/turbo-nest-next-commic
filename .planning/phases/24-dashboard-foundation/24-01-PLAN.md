---
phase: 24-dashboard-foundation
plan: 01
type: execute
wave: 1
depends_on: [20-01, 20-02, 22-01]
files_modified:
  - apps/web/src/features/dashboard/types.ts
  - apps/web/src/features/dashboard/api.ts
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/app/dashboard/page.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "Authenticated users can open a dedicated dashboard page that summarizes account modules"
    - "Dashboard section navigation links to wallet, purchases, profile, and notifications entry paths while preserving session state"
    - "Unauthenticated users are redirected to sign-in instead of seeing dashboard content"
  artifacts:
    - path: "apps/web/src/features/dashboard/types.ts"
      provides: "Dashboard section and summary card contracts"
      min_lines: 35
    - path: "apps/web/src/features/dashboard/api.ts"
      provides: "Session-aware dashboard data loader and route helper contracts"
      min_lines: 60
    - path: "apps/web/src/features/dashboard/dashboard.tsx"
      provides: "Dashboard shell UI with account summary cards and section nav"
      min_lines: 120
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard route entry with auth guard integration"
      min_lines: 25
  key_links:
    - from: "apps/web/app/dashboard/page.tsx"
      to: "apps/web/src/features/dashboard/dashboard.tsx"
      via: "route-level composition"
      pattern: "DashboardView"
    - from: "apps/web/src/features/dashboard/api.ts"
      to: "apps/web/src/lib/auth/api.ts"
      via: "session fetch bootstrap"
      pattern: "fetchSession"
    - from: "apps/web/src/features/dashboard/dashboard.tsx"
      to: "apps/web/src/providers/app-provider.tsx"
      via: "AppContext session guard"
      pattern: "useContext\(AppContext\)"
---

<objective>
Build phase-1 dashboard foundation for signed-in readers: dedicated dashboard route, summary cards, and section navigation/guard wiring.

Purpose: establish the account hub that all v1.9 wallet/purchase/profile/notification phases will extend.
Output: reusable dashboard feature contracts + dashboard route and auth-aware UI shell.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-frontend-foundation-auth-integration/20-01-SUMMARY.md
@.planning/phases/20-frontend-foundation-auth-integration/20-02-SUMMARY.md
@.planning/phases/22-reader-experience-ui/22-01-SUMMARY.md
@apps/web/src/providers/app-provider.tsx
@apps/web/src/lib/api/types.ts
@apps/web/src/lib/auth/session-store.ts
@apps/web/src/lib/auth/api.ts
@apps/web/app/page.tsx

<interfaces>
From apps/web/src/providers/app-provider.tsx:
```typescript
interface AppContextValue {
  user: SessionUser | null;
  loaded: boolean;
  setUser: (user: SessionUser | null) => void;
}
```

From apps/web/src/lib/auth/api.ts:
```typescript
export async function fetchSession(token?: string): Promise<ApiResult<SessionPayload | { user: null }>>;
export async function logoutSession(): Promise<ApiResult<{ success: boolean }>>;
```

From apps/web/src/lib/api/types.ts:
```typescript
export interface SessionUser {
  id: number;
  email: string;
  nickname?: string | null;
  role: "USER" | "AUTHOR" | "ADMIN";
}
```

From apps/api/src/finance/payment.controller.ts:
```typescript
POST /finance/payments/initiate
POST /finance/payments/verify
```

From apps/api/src/finance/purchase.controller.ts:
```typescript
POST /finance/purchases/chapters/:chapterId
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dashboard contracts and auth-aware data helpers</name>
  <files>
    apps/web/src/features/dashboard/types.ts
    apps/web/src/features/dashboard/api.ts
  </files>
  <behavior>
    - Test 1: dashboard section map exposes wallet, purchases, profile, and notifications entries with stable href keys.
    - Test 2: dashboard bootstrap helper resolves signed-in user summary from existing session state and fetchSession fallback.
    - Test 3: unauthenticated bootstrap path returns redirect intent for dashboard route guard.
  </behavior>
  <action>
Define interface-first dashboard foundations for DASH-01/DASH-02.

Implementation details:
- Add typed dashboard section identifiers, summary-card DTOs, and navigation metadata contracts.
- Add auth-aware helper functions for dashboard bootstrap that reuse existing session-store/auth API behavior from phase 20.
- Keep wallet/purchase/profile/notification card data minimal and explicitly marked as phase-24 summary placeholders (no phase-25+ business logic yet).
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Dashboard contracts and helper APIs exist, are typed, and support authenticated bootstrap + guard outcomes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard route, summary shell, and section navigation wiring</name>
  <files>
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/app/dashboard/page.tsx
    apps/web/app/page.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement dashboard foundation UI and route wiring for DASH-01/DASH-02.

Implementation details:
- Create dashboard view component rendering account summary cards and section navigation links for wallet, purchases, profile, and notifications.
- Add dashboard route (`/dashboard`) that enforces auth guard behavior: signed-in users see dashboard; signed-out users are redirected to login.
- Update homepage signed-in call-to-action to route users into dashboard while preserving existing discovery functionality.
- Add focused dashboard styles that match existing storefront visual language and remain responsive on mobile.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Dashboard route is accessible for authenticated users, navigation entry points are visible and stable, and auth redirection works for signed-out users.
  </done>
</task>

</tasks>

<verification>
- Authenticated session can open `/dashboard` and see all four section entry cards.
- Signed-out session cannot access dashboard content and is routed to login.
- Dashboard navigation links are deterministic and preserved across refresh.
</verification>

<success_criteria>
DASH-01 and DASH-02 are satisfied with a production-ready dashboard shell and section navigation baseline for subsequent v1.9 phases.
</success_criteria>

<output>
After completion, create `.planning/phases/24-dashboard-foundation/24-01-SUMMARY.md`
</output>
