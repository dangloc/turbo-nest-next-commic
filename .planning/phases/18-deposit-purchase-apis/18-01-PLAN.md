---
phase: 18-deposit-purchase-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/finance/finance.module.ts
  - apps/api/src/finance/finance.service.ts
  - apps/api/src/finance/payment.controller.ts
  - apps/api/src/finance/purchase.controller.ts
  - apps/api/src/finance/types.ts
  - apps/api/src/finance/__tests__/payment.spec.ts
  - apps/api/src/finance/__tests__/purchase.spec.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements: [PAY-01, PAY-02, WAL-01, WAL-02, PUR-01, PUR-02]
must_haves:
  truths:
    - "Users can initiate and verify top-up payments with idempotent settlement behavior"
    - "Verified top-ups update deposited wallet balances and total deposited history"
    - "Users can purchase chapters only once with correct balance deduction and access grants"
  artifacts:
    - path: "apps/api/src/finance/payment.controller.ts"
      provides: "Top-up intent and verification API surface"
      min_lines: 80
    - path: "apps/api/src/finance/purchase.controller.ts"
      provides: "Wallet-backed chapter purchase endpoint"
      min_lines: 70
    - path: "apps/api/src/finance/finance.service.ts"
      provides: "Transactional payment settlement and purchase orchestration"
      min_lines: 240
    - path: "apps/api/src/finance/__tests__/payment.spec.ts"
      provides: "Payment initiation/verification contract tests"
      min_lines: 120
    - path: "apps/api/src/finance/__tests__/purchase.spec.ts"
      provides: "Purchase safety and access-grant tests"
      min_lines: 140
  key_links:
    - from: "apps/api/src/finance/payment.controller.ts"
      to: "apps/api/src/finance/finance.service.ts"
      via: "initiateTopUp() and verifyTopUp() flow"
      pattern: "initiateTopUp|verifyTopUp"
    - from: "apps/api/src/finance/finance.service.ts"
      to: "prisma.wallet/prisma.transaction/prisma.purchasedChapter"
      via: "transactional settlement and purchase writes"
      pattern: "prisma\\.(wallet|transaction|purchasedChapter)"
    - from: "apps/api/src/finance/finance.module.ts"
      to: "apps/api/src/app.module.ts"
      via: "module import registration"
      pattern: "FinanceModule"
---

<objective>
Implement secure financial APIs for deposit and purchase flow so users can top up wallet funds and buy chapter access with auditable balance updates.

Purpose: establish the core payment and purchase engine before adding revenue split and withdrawal operations.
Output: Finance module with tested payment initiation/verification, wallet settlement, and purchase endpoints.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@apps/api/prisma/schema.prisma
@apps/api/src/app.module.ts
@apps/api/src/auth/guards/roles.guard.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Wallet {
  userId           Int     @unique
  depositedBalance Decimal @default(0)
  earnedBalance    Decimal @default(0)
  totalDeposited   Decimal @default(0)
}

model Transaction {
  userId          Int
  amountIn        Decimal
  amountOut       Decimal
  accumulated     Decimal
  type            TransactionType
  transactionDate DateTime
  content         String?
}

model PurchasedChapter {
  userId      Int
  novelId     Int
  chapterId   Int
  pricePaid   Decimal
  purchasedAt DateTime

  @@unique([userId, chapterId])
}
```

From apps/api/src/auth/guards/roles.guard.ts:
```typescript
@UseGuards(RolesGuard)
@Roles('USER', 'AUTHOR', 'ADMIN')
```

From apps/api/src/app.module.ts:
```typescript
@Module({
  imports: [
    PassportModule.register({ defaultStrategy: 'google' }),
    CmsModule,
    ReaderModule,
    SocialModule,
  ],
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build payment intent and verification APIs with idempotent contracts</name>
  <files>
    apps/api/src/finance/types.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/payment.controller.ts
    apps/api/src/finance/__tests__/payment.spec.ts
  </files>
  <behavior>
    - Test 1: authenticated user can create payment intent with amount/provider/reference and gateway-ready metadata.
    - Test 2: verification endpoint accepts provider transaction result and rejects duplicate successful verification for the same reference.
    - Test 3: invalid verification payloads are rejected with clear validation errors.
  </behavior>
  <action>
Create payment contracts and endpoints for top-up initiation and settlement verification.

Implementation details:
- Add endpoints under finance scope:
  - `POST /finance/payments/initiate`
  - `POST /finance/payments/verify`
- Guard both routes for authenticated users.
- Persist and enforce idempotency by provider reference and provider transaction id so callback retries cannot double-credit wallets.
- Keep provider integration abstracted for VNPay/MoMo readiness (prepare request/response contracts, no live gateway coupling).
- Return deterministic API responses for pending, success, and duplicate-verification states.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/payment.spec.ts</automated>
  </verify>
  <done>
Payment initiation and verification APIs are in place with idempotent callback behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settle verified top-ups into wallet balances and VIP evaluation</name>
  <files>
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/__tests__/payment.spec.ts
  </files>
  <behavior>
    - Test 1: successful verification increments depositedBalance and totalDeposited exactly once.
    - Test 2: settlement appends immutable transaction record with updated accumulated balance.
    - Test 3: VIP upgrade evaluation runs after top-up and updates user currentVipLevelId when threshold is crossed.
  </behavior>
  <action>
Implement transactional top-up settlement logic.

Implementation details:
- Wrap settlement in a single Prisma transaction that:
  1. verifies idempotency lock/reference state,
  2. updates wallet balances,
  3. writes transaction ledger row,
  4. evaluates and applies VIP tier upgrade.
- Use Decimal-safe arithmetic for money updates and avoid floating-point operations.
- Ensure retries of the same successful verification are no-op from a balance perspective.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/payment.spec.ts</automated>
  </verify>
  <done>
Verified top-ups update wallet and VIP state safely with replay protection and audit logging.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement chapter purchase API with wallet deduction and access grant</name>
  <files>
    apps/api/src/finance/finance.module.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/purchase.controller.ts
    apps/api/src/finance/__tests__/purchase.spec.ts
    apps/api/src/app.module.ts
  </files>
  <behavior>
    - Test 1: purchase succeeds when depositedBalance is sufficient, deducts funds, logs transaction, and creates purchased_chapters access.
    - Test 2: duplicate purchase for same user/chapter is rejected or returned as already-owned without double deduction.
    - Test 3: insufficient funds returns failure without partial writes.
    - Test 4: concurrent purchase attempts remain single-charge safe through transaction/unique constraints.
  </behavior>
  <action>
Add the purchase endpoint and wire finance module into the app.

Implementation details:
- Endpoint: `POST /finance/purchases/chapters/:chapterId` (authenticated user roles).
- Resolve chapter and price inputs, then run wallet deduction + purchase grant + transaction log in one Prisma transaction.
- Use `PurchasedChapter @@unique([userId, chapterId])` to enforce duplicate purchase safety.
- Keep revenue split out of this phase; write purchase records and ledgers so Phase 19 can apply author/platform distribution cleanly.
- Register `FinanceModule` in `AppModule` and run full API regression + typecheck after implementation.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/purchase.spec.ts && npm test --workspace=api -- --runInBand && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Purchase flow is transactional, duplicate-safe, and grants durable access with auditable wallet changes.
  </done>
</task>

</tasks>

<verification>
- Payment verification cannot double-credit wallets under callback retries.
- Wallet top-up settlement writes both balance updates and immutable ledger records.
- Chapter purchase atomically deducts funds, logs transaction, and grants access once.
</verification>

<success_criteria>
Phase 18 is complete when PAY-01 through PUR-02 are implemented, FinanceModule is mounted, targeted tests pass, and full API regression plus typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/phases/18-deposit-purchase-apis/18-01-SUMMARY.md`
</output>
