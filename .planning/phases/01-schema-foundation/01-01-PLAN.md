---
phase: 01-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/prisma/schema.prisma
  - package-lock.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "The PostgreSQL schema normalizes WordPress user, provider, wallet, transaction, VIP, and purchased chapter data into dedicated tables."
    - "Migrated users keep the legacy WordPress ID and password hash semantics."
    - "PurchasedChapter blocks duplicate purchases for a user/chapter pair and remains queryable at scale."
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Normalized Prisma data model"
      min_lines: 120
    - path: "apps/api/package.json"
      provides: "Prisma tooling scripts and dependency declarations"
      min_lines: 1
    - path: "package-lock.json"
      provides: "Locked dependency graph after Prisma install"
      min_lines: 1
  key_links:
    - from: "apps/api/package.json"
      to: "apps/api/prisma/schema.prisma"
      via: "prisma generate/validate scripts"
      pattern: "prisma.*(generate|validate)"
    - from: "apps/api/prisma/schema.prisma"
      to: "legacy WordPress identifiers"
      via: "canonical id mapping and unique constraints"
      pattern: "model User|model PurchasedChapter"
---

<objective>
Build the schema foundation for the WordPress-to-NestJS/PostgreSQL migration.

Purpose: Normalize the legacy WordPress data model so the downstream ETL phase can move data safely without ambiguity or duplication.
Output: Prisma tooling wired in the API workspace and a normalized PostgreSQL schema ready for migration work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@apps/api/package.json
@apps/api/src/main.ts
@apps/api/src/app.module.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Prisma tooling to the API workspace</name>
  <files>apps/api/package.json, package-lock.json</files>
  <action>
Add Prisma workspace dependencies and scripts in the API package manifest so this workspace owns schema generation and validation. Keep the existing Nest scripts intact, and add only the minimum commands needed for schema authoring (`prisma:generate`, `prisma:validate`, and any formatting helper you need) because later phases will rely on a stable API-side Prisma workflow.
  </action>
  <verify>Run `npm install` at the repo root if dependency versions changed, then confirm the API package manifest contains the Prisma scripts and the lockfile updated cleanly without unrelated package churn.</verify>
  <done>Prisma tooling is available from the API workspace and the dependency graph is locked for schema work.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the normalized Prisma schema</name>
  <files>apps/api/prisma/schema.prisma</files>
  <action>
Create the full PostgreSQL Prisma schema for the migration boundary. Define `User`, `UserProvider`, `Wallet`, `Transaction`, `VipSubscription`, and `PurchasedChapter` with the locked legacy-ID strategy from CONTEXT.md: preserve WordPress `wp_users.ID` as the canonical user identifier, keep the WordPress password hash string, normalize the serialized usermeta into dedicated tables, and add the purchased-chapter uniqueness and lookup index that supports large-scale queries. Make the schema explicit enough that downstream ETL planning can target it without guessing field intent.
  </action>
  <verify>Run `npx prisma validate` and `npx prisma format --check` from `apps/api` to confirm the schema parses, models relate correctly, and formatting is stable.</verify>
  <done>The normalized schema exists, validates cleanly, and clearly expresses the migration data model needed for Phase 2.</done>
</task>

</tasks>

<verification>
The phase is complete when Prisma dependencies/scripts exist in the API workspace, the schema file validates, and the resulting model preserves the legacy identifier and purchased-chapter constraints from the phase context.
</verification>

<success_criteria>
- `apps/api/package.json` contains Prisma tooling that supports schema generation and validation.
- `apps/api/prisma/schema.prisma` defines the normalized migration model with the legacy identifier strategy locked in CONTEXT.md.
- Purchased chapters are protected by a user/chapter uniqueness rule and an additional lookup index suitable for high-volume histories.
- The schema validates successfully with Prisma tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-foundation/01-01-SUMMARY.md`.
</output>
