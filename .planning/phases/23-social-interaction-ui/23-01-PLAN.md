---
phase: 23-social-interaction-ui
plan: 01
type: execute
wave: 1
depends_on: [20-01, 20-02, 22-01]
files_modified:
  - apps/web/src/features/social/types.ts
  - apps/web/src/features/social/api.ts
  - apps/web/src/features/social/social.tsx
  - apps/web/src/features/reader/reader.tsx
  - apps/web/app/reader/chapters/[chapterId]/page.tsx
  - apps/web/app/novels/[id]/page.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [SOC-01, SOC-02, SOC-03]
must_haves:
  truths:
    - "Readers can see nested comment trees in stable parent-to-reply order for chapter and novel contexts"
    - "Authenticated users can post top-level comments and replies with inline validation/error feedback"
    - "Authenticated users can toggle reactions on comments and the UI reflects create, switch, and remove behavior"
  artifacts:
    - path: "apps/web/src/features/social/types.ts"
      provides: "Typed social comment/reaction/scope contracts matching backend APIs"
      min_lines: 60
    - path: "apps/web/src/features/social/api.ts"
      provides: "Shared social API helpers for list/create/toggle"
      min_lines: 80
    - path: "apps/web/src/features/social/social.tsx"
      provides: "Reusable nested comments + reply + reaction UI components"
      min_lines: 140
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Reader page integration point for chapter/novel social panels"
      min_lines: 220
    - path: "apps/web/app/reader/chapters/[chapterId]/page.tsx"
      provides: "Chapter-scoped social interaction route integration"
      min_lines: 40
  key_links:
    - from: "apps/web/src/features/social/api.ts"
      to: "apps/api/src/social/social-comments.controller.ts"
      via: "scope-based fetch/create comment requests"
      pattern: "social/comments"
    - from: "apps/web/src/features/social/api.ts"
      to: "apps/api/src/social/social-reactions.controller.ts"
      via: "reaction toggles by comment id"
      pattern: "social/comments/.*/reactions"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/social/social.tsx"
      via: "chapter reader social panel composition"
      pattern: "Comments|Reactions|reply"
---

<objective>
Integrate nested comments and reaction interactions into the chapter/novel reader experience with authenticated write paths and robust UI feedback.

Purpose: complete the social engagement surface required for v1.8 storefront usability and set up reusable social components for future screens.
Output: typed social contracts, social API helpers, reusable thread/reaction components, chapter/novel integration wiring, and supporting styles.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-frontend-foundation-auth-integration/20-01-SUMMARY.md
@.planning/phases/20-frontend-foundation-auth-integration/20-02-SUMMARY.md
@.planning/phases/22-reader-experience-ui/22-01-SUMMARY.md
@apps/api/src/social/types.ts
@apps/api/src/social/social-comments.controller.ts
@apps/api/src/social/social-reactions.controller.ts
@apps/api/src/social/social.service.ts
@apps/api/src/social/__tests__/social-comments.spec.ts
@apps/api/src/social/__tests__/social-reactions.spec.ts
@apps/web/src/lib/auth/session-store.ts
@apps/web/src/features/reader/reader.tsx

<interfaces>
From apps/api/src/social/social-comments.controller.ts:
```typescript
@Get('comments')
async listComments(@Query('novelId') novelId?: string, @Query('chapterId') chapterId?: string)

@Post('comments')
async createComment(@Body() body: CreateSocialCommentInput)
```

From apps/api/src/social/types.ts:
```typescript
export interface SocialCommentNode {
  id: number;
  userId: number;
  novelId: number | null;
  chapterId: number | null;
  parentId: number | null;
  content: string;
  replies: SocialCommentNode[];
}
```

From apps/api/src/social/social-reactions.controller.ts:
```typescript
@Post('comments/:commentId/reactions')
async toggleReaction(@Param('commentId', ParseIntPipe) commentId: number, @Body('type', new ParseEnumPipe(CommentReactionType)) type: CommentReactionType)
```

From apps/api/src/social/__tests__/social-reactions.spec.ts:
```typescript
CommentReactionType: LIKE | HEART | HAHA | WOW | SAD | ANGRY
```

From apps/web/src/lib/auth/session-store.ts:
```typescript
export function getSessionToken() {
  return loadSessionFromStorage().token;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define social contracts and API helpers for nested comments and reactions</name>
  <files>
    apps/web/src/features/social/types.ts
    apps/web/src/features/social/api.ts
  </files>
  <behavior>
    - Test 1: comment list helper maps scope to exactly one of chapterId or novelId and preserves nested reply tree shape.
    - Test 2: create-comment helper supports top-level and reply payloads with inline error payload mapping.
    - Test 3: reaction helper supports create, switch, and remove (null response) paths for allowed reaction types.
  </behavior>
  <action>
Build interface-first social contracts before rendering UI.

Implementation details:
- Add frontend social DTOs matching backend `SocialCommentNode` and reaction toggle contracts.
- Add API helpers for `GET /social/comments`, `POST /social/comments`, and `POST /social/comments/:commentId/reactions` using the shared HTTP client.
- Reuse session token from phase 20 auth store for authenticated write requests and keep read requests public.
- Normalize request scope and reaction inputs so invalid values fail fast in UI code before network calls.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Social contracts and API helpers are reusable and cover list/create/reaction paths for chapter and novel scopes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate nested comments, replies, and reactions into reader pages</name>
  <files>
    apps/web/src/features/social/social.tsx
    apps/web/src/features/reader/reader.tsx
    apps/web/app/reader/chapters/[chapterId]/page.tsx
    apps/web/app/novels/[id]/page.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement the social interaction UI and wire it into chapter/novel reading surfaces.

Implementation details:
- Build reusable social components for thread rendering, reply forms, and reaction controls with deterministic nested ordering display.
- Add inline validation and error handling for comment/reply submissions.
- Integrate social components into chapter reader view and novel detail context so both chapter and novel scope are supported.
- Update styles for comment depth, author metadata, reply affordances, and reaction states while preserving existing reader/discovery visual language.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Users can read nested discussions, post replies/comments when authenticated, and toggle reactions with immediate, consistent UI feedback.
  </done>
</task>

</tasks>

<verification>
- Chapter reader page renders social thread in deterministic parent/reply order.
- Comment/reply submission and reaction toggles show inline success/error feedback without page reload.
- Novel and chapter scope requests are sent with correct query parameters and auth handling.
</verification>

<success_criteria>
SOC-01, SOC-02, and SOC-03 are complete with functional nested comments, reply submission UX, and reaction toggles integrated into reader-facing pages.
</success_criteria>

<output>
After completion, create `.planning/phases/23-social-interaction-ui/23-01-SUMMARY.md`
</output>
