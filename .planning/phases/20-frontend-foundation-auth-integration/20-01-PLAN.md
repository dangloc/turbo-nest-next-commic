---
phase: 20-frontend-foundation-auth-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/env.ts
  - apps/web/src/lib/api/types.ts
  - apps/web/src/lib/api/http.ts
  - apps/web/src/lib/auth/session-store.ts
  - apps/web/src/providers/app-provider.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [WEB-01, WEB-02]
must_haves:
  truths:
    - "Frontend app can start with environment-driven API base URL configuration"
    - "All UI features call backend through one typed API client layer"
    - "App has a shared provider/session bootstrap path reusable by auth and reader screens"
  artifacts:
    - path: "apps/web/src/lib/env.ts"
      provides: "Runtime-safe env parsing for API base URL"
      min_lines: 20
    - path: "apps/web/src/lib/api/http.ts"
      provides: "Shared fetch wrapper with typed response + normalized errors"
      min_lines: 80
    - path: "apps/web/src/lib/api/types.ts"
      provides: "API contract types consumed by feature modules"
      min_lines: 60
    - path: "apps/web/src/providers/app-provider.tsx"
      provides: "App-wide provider/session bootstrapping shell"
      min_lines: 40
  key_links:
    - from: "apps/web/src/lib/env.ts"
      to: "apps/web/src/lib/api/http.ts"
      via: "API base URL resolution"
      pattern: "NEXT_PUBLIC_API_BASE_URL"
    - from: "apps/web/src/providers/app-provider.tsx"
      to: "apps/web/app/layout.tsx"
      via: "provider wiring"
      pattern: "AppProvider"
    - from: "apps/web/src/lib/api/http.ts"
      to: "apps/web/src/lib/api/types.ts"
      via: "typed request/response contracts"
      pattern: "ApiError|ApiResult"
---

<objective>
Create the frontend technical foundation and reusable API client layer required by all v1.8 UI phases.

Purpose: avoid per-feature ad-hoc networking/auth state and establish stable contracts before building discovery/reader/social pages.
Output: typed API client utilities, env config, session store skeleton, and app provider wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@apps/web/package.json
@apps/web/app/layout.tsx

<interfaces>
From apps/web/package.json:
```json
{
  "scripts": {
    "lint": "eslint --max-warnings 0",
    "check-types": "next typegen && tsc --noEmit"
  }
}
```

From apps/web/app/layout.tsx:
```tsx
export default function RootLayout({ children }: { children: React.ReactNode })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define frontend environment and API contracts (interface-first)</name>
  <files>
    apps/web/src/lib/env.ts
    apps/web/src/lib/api/types.ts
    apps/web/src/lib/auth/session-store.ts
  </files>
  <action>
Create interface and contract files first.

Implementation details:
- Add `env.ts` that validates/exports `NEXT_PUBLIC_API_BASE_URL` and default dev fallback behavior.
- Add API contract types (`ApiResult`, `ApiError`, auth/session DTO placeholders, paginated response base type) in `api/types.ts`.
- Add a minimal session store contract in `session-store.ts` for authenticated-user bootstrap (get/set/clear and load state shape).
- Keep these files free of UI concerns so downstream phases import stable contracts directly.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Typed env/API/session contracts exist and compile without relying on feature-specific code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement shared HTTP client and provider wiring for app-wide consumption</name>
  <files>
    apps/web/src/lib/api/http.ts
    apps/web/src/providers/app-provider.tsx
    apps/web/app/layout.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement the core HTTP utility and wire provider shell into root layout.

Implementation details:
- Build `http.ts` wrapper with:
  - base URL from `env.ts`,
  - JSON request/response handling,
  - standardized error mapping into `ApiError`,
  - optional auth credential inclusion strategy compatible with backend session cookies.
- Create `AppProvider` as the single bootstrap boundary for global app/session state.
- Update `layout.tsx` to wrap children in `AppProvider`.
- Add minimal global style tokens that support future storefront components without locking final visual design.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Web app boots with shared provider and all networking flows route through one typed HTTP client.
  </done>
</task>

</tasks>

<verification>
- No page implements direct ad-hoc fetch logic outside shared client for initial foundation work.
- Web lint and typecheck pass after provider/client integration.
</verification>

<success_criteria>
WEB-01 and WEB-02 are complete with reusable frontend contracts and HTTP infrastructure ready for auth/discovery/reader/social phases.
</success_criteria>

<output>
After completion, create `.planning/phases/20-frontend-foundation-auth-integration/20-01-SUMMARY.md`
</output>
