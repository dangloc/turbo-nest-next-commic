---
phase: 20-frontend-foundation-auth-integration
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/auth.module.ts
  - apps/api/src/auth/__tests__/auth.controller.spec.ts
  - apps/api/src/app.module.ts
  - apps/web/src/lib/auth/api.ts
  - apps/web/src/lib/auth/session-store.ts
  - apps/web/app/auth/login/page.tsx
  - apps/web/app/page.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Users can start Google sign-in from the web app and return through a working callback flow"
    - "Web app can bootstrap authenticated session state on load and expose sign-out path"
    - "Auth integration uses backend session endpoints instead of mock local-only state"
  artifacts:
    - path: "apps/api/src/auth/auth.controller.ts"
      provides: "Google auth start/callback/session/logout endpoints"
      min_lines: 80
    - path: "apps/web/src/lib/auth/api.ts"
      provides: "Frontend auth API calls for login/session/logout"
      min_lines: 50
    - path: "apps/web/app/auth/login/page.tsx"
      provides: "User-facing sign-in entry page"
      min_lines: 50
    - path: "apps/web/app/page.tsx"
      provides: "Session-aware homepage shell"
      min_lines: 60
  key_links:
    - from: "apps/web/app/auth/login/page.tsx"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "Google auth start URL"
      pattern: "/auth/google"
    - from: "apps/web/src/lib/auth/api.ts"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "session bootstrap + logout"
      pattern: "/auth/me|/auth/logout"
    - from: "apps/api/src/auth/auth.module.ts"
      to: "apps/api/src/app.module.ts"
      via: "module registration"
      pattern: "AuthModule"
---

<objective>
Integrate the frontend with real Google OAuth session behavior by exposing explicit auth endpoints and wiring session-aware web login/bootstrap flows.

Purpose: satisfy frontend auth requirements before discovery/reader/social UI phases depend on authenticated user context.
Output: backend auth controller/module, frontend auth API utilities, login page, and session-aware home shell.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@apps/api/src/auth/strategies/google.strategy.ts
@apps/api/src/app.module.ts
@apps/web/src/lib/api/http.ts

<interfaces>
From apps/api/src/auth/strategies/google.strategy.ts:
```typescript
export class GoogleStrategy extends PassportStrategy(Strategy, 'google') {}
```

From apps/api/src/app.module.ts:
```typescript
PassportModule.register({ defaultStrategy: 'google' })
```

From apps/web/src/lib/api/http.ts:
```typescript
// shared HTTP client created in 20-01
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose backend auth routes for Google flow and session bootstrap</name>
  <files>
    apps/api/src/auth/auth.controller.ts
    apps/api/src/auth/auth.module.ts
    apps/api/src/auth/__tests__/auth.controller.spec.ts
    apps/api/src/app.module.ts
  </files>
  <behavior>
    - Test 1: `GET /auth/google` delegates to passport google guard start flow.
    - Test 2: callback route finalizes auth and redirects to configured web origin.
    - Test 3: `GET /auth/me` returns authenticated user payload and `POST /auth/logout` clears session.
  </behavior>
  <action>
Add explicit auth API surface for frontend integration.

Implementation details:
- Create `AuthController` routes:
  - `GET /auth/google`
  - `GET /auth/google/callback`
  - `GET /auth/me`
  - `POST /auth/logout`
- Use guard and request-user patterns already established in backend auth/roles code.
- Add `AuthModule` and register it in `AppModule`.
- Add controller-level tests for metadata/behavior contracts.
- Keep callback redirect configurable via env (frontend origin), with safe local default.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/auth/__tests__/auth.controller.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Backend exposes stable auth endpoints consumable by web app for login/session/logout lifecycle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire frontend login and session bootstrap against backend auth endpoints</name>
  <files>
    apps/web/src/lib/auth/api.ts
    apps/web/src/lib/auth/session-store.ts
    apps/web/app/auth/login/page.tsx
    apps/web/app/page.tsx
  </files>
  <action>
Implement web auth integration using the shared HTTP client and backend auth endpoints.

Implementation details:
- Add `auth/api.ts` wrappers for login URL generation, session fetch (`/auth/me`), and logout.
- Extend session store from 20-01 to load initial session on app boot and persist sign-in state.
- Create a focused `/auth/login` page with explicit "Continue with Google" CTA.
- Update root homepage shell to show session-aware state (signed-in vs signed-out actions).
- Keep UX intentionally minimal for foundation phase; prioritize reliable auth flow over design polish.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web && npm test --workspace=api -- --runInBand src/auth/__tests__/auth.controller.spec.ts</automated>
  </verify>
  <done>
Users can initiate Google login from web UI, app can load current session state, and sign-out path is available.
  </done>
</task>

</tasks>

<verification>
- Web login entry successfully routes to backend Google auth start endpoint.
- Session bootstrap path uses real backend `/auth/me` contract and handles unauthenticated state gracefully.
</verification>

<success_criteria>
AUTH-01 and AUTH-02 are complete with a working frontend-backend OAuth/session handshake and passing API/web verification commands.
</success_criteria>

<output>
After completion, create `.planning/phases/20-frontend-foundation-auth-integration/20-02-SUMMARY.md`
</output>
