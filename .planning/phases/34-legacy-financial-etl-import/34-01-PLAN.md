---
phase: 34-legacy-financial-etl-import
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/api/src/etl/types.ts
  - apps/api/src/etl/source-mysql-loaders.ts
  - apps/api/src/etl/parse-wordpress.ts
  - apps/api/src/etl/prisma-repositories.ts
  - apps/api/src/etl/migrate-user-financials.ts
  - apps/api/src/etl/etl-runner.ts
  - apps/api/src/etl/index.ts
  - apps/api/src/etl/__tests__/migrate-user-financials.spec.ts
autonomous: true
requirements:
  - ETL-01
  - ETL-02
  - ETL-03
must_haves:
  truths:
    - "Legacy balance and VIP data come from one grouped wp_usermeta pivot per user"
    - "Deprecated wp_users.price and wp_users.vip_level_id are ignored"
    - "Serialized purchased chapter payloads are decoded and normalized into target purchase rows"
    - "Wallet/VIP updates and purchased chapter inserts are loaded through Prisma without N+1 writes"
  artifacts:
    - path: "apps/api/src/etl/migrate-user-financials.ts"
      provides: "Combined legacy financial ETL service"
      min_lines: 120
    - path: "apps/api/src/etl/source-mysql-loaders.ts"
      provides: "Single-query wp_usermeta pivot loader"
      min_lines: 120
    - path: "apps/api/src/etl/parse-wordpress.ts"
      provides: "PHP unserialize normalization helpers"
      min_lines: 120
    - path: "apps/api/src/etl/prisma-repositories.ts"
      provides: "Prisma load methods for wallet/VIP and purchase history"
      min_lines: 180
    - path: "apps/api/src/etl/__tests__/migrate-user-financials.spec.ts"
      provides: "Behavior coverage for pivot, parse, and load flow"
      min_lines: 120
  key_links:
    - from: "apps/api/src/etl/source-mysql-loaders.ts"
      to: "apps/api/src/etl/parse-wordpress.ts"
      via: "pivoted meta payload handed to PHP serializer"
      pattern: "MAX\(CASE WHEN meta_key = '(_user_balance|_user_vip_level_id|_purchased_chapters)'"
    - from: "apps/api/src/etl/parse-wordpress.ts"
      to: "apps/api/src/etl/prisma-repositories.ts"
      via: "normalized purchase rows mapped to Prisma createMany payload"
      pattern: "createMany\(|purchasedChapter\.createMany"
    - from: "apps/api/src/etl/etl-runner.ts"
      to: "apps/api/src/etl/migrate-user-financials.ts"
      via: "ETL runner stage wiring"
      pattern: "migrate-user-financials|financials"
---

<objective>
Implement the legacy financial ETL migration that imports user balance, VIP level, and purchased chapter history from WordPress wp_usermeta into PostgreSQL.

Purpose: This closes the last data-import gap in the ETL pipeline by moving financial truth to the correct legacy source and loading it into the new Prisma schema without N+1 queries.
Output: A dedicated migration service plus loader/repository wiring that can process users in bulk and preserve chapter purchase history.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/api/package.json
@apps/api/prisma/schema.prisma
@apps/api/src/etl/types.ts
@apps/api/src/etl/source-mysql-loaders.ts
@apps/api/src/etl/parse-wordpress.ts
@apps/api/src/etl/prisma-repositories.ts
@apps/api/src/etl/etl-runner.ts
@apps/api/src/etl/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the ETL behavior with failing tests and source contracts</name>
  <files>apps/api/src/etl/__tests__/migrate-user-financials.spec.ts, apps/api/src/etl/types.ts</files>
  <behavior>
    - Test that the loader contract exposes a single grouped usmeta pivot per user with `_user_balance`, `_user_vip_level_id`, and `_purchased_chapters`.
    - Test that deprecated `wp_users.price` and `wp_users.vip_level_id` are not used for the migrated financial snapshot.
    - Test that serialized chapter purchases normalize both arrays and numeric-key objects into Prisma-ready rows.
    - Test that the purchase history payload maps to the actual PurchasedChapter schema fields and retains parsed purchase dates.
  </behavior>
  <action>Create the regression spec first, then adjust source-row types for the new legacy financial snapshot shape so the implementation has a precise contract to satisfy.</action>
  <verify><automated>cd apps/api && npm test -- --runInBand src/etl/__tests__/migrate-user-financials.spec.ts</automated></verify>
  <done>The tests describe the exact ETL behavior and fail until the new loader/service is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the grouped usmeta pivot and PHP purchase transformation</name>
  <files>apps/api/src/etl/source-mysql-loaders.ts, apps/api/src/etl/parse-wordpress.ts, apps/api/src/etl/migrate-user-financials.ts</files>
  <behavior>
    - Query `wp_usermeta` once per batch with a `GROUP BY user_id` pivot using `MAX(CASE WHEN meta_key = '...' THEN meta_value END)`.
    - Ignore the legacy `wp_users.price` and `wp_users.vip_level_id` columns entirely.
    - Parse `_user_balance` and `_user_vip_level_id` as integers.
    - Decode `_purchased_chapters` with `php-serialize` and normalize both strict arrays and object-like numeric-key payloads.
    - Map the purchase payload into the target Prisma row shape without inventing unsupported fields.
  </behavior>
  <action>Add the new loader/service path that turns the pivoted source rows into a typed legacy financial batch, reusing existing parser helpers where possible and keeping the extraction logic free of N+1 meta lookups.</action>
  <verify><automated>cd apps/api && npm test -- --runInBand src/etl/__tests__/migrate-user-financials.spec.ts src/etl/__tests__/migrate-purchased-chapters.spec.ts</automated></verify>
  <done>The ETL can extract and transform legacy balances, VIP levels, and serialized purchase history from one grouped source query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Prisma loading and ETL execution for financial migration</name>
  <files>apps/api/src/etl/prisma-repositories.ts, apps/api/src/etl/etl-runner.ts, apps/api/src/etl/index.ts</files>
  <behavior>
    - Update the target database through Prisma using repository methods for wallet/balance and VIP-level persistence.
    - Bulk insert mapped purchase rows with `createMany` and duplicate protection.
    - Integrate the new migration stage into the ETL runner so it executes as part of the normal import pipeline.
    - Keep the CLI entrypoint behavior unchanged aside from the new ETL stage being invoked.
  </behavior>
  <action>Add the Prisma repository surface needed for the new legacy financial snapshot and connect it to the ETL runner and CLI so the script executes end-to-end without extra manual steps.</action>
  <verify><automated>cd apps/api && npm run check-types && npm run prisma:validate && npm test -- --runInBand src/etl/__tests__/migrate-user-financials.spec.ts</automated></verify>
  <done>The migration stage updates user financial state and purchase history in PostgreSQL through Prisma and is reachable from the ETL CLI.</done>
</task>

</tasks>

<verification>
- Single-query extraction must be visible in the source loader and covered by tests.
- PHP unserialization must be exercised with both array and numeric-key object payloads.
- Prisma loading must prove the target repository can update financial state and bulk insert purchases without per-row inserts.
- The ETL runner must wire the new migration stage into the normal CLI flow.
</verification>

<success_criteria>
- Legacy financial data is sourced only from wp_usermeta and not from deprecated wp_users financial columns.
- The migration transforms serialized purchased chapter history into the current Prisma purchase schema.
- Wallet/VIP state and purchase history are loaded through Prisma repositories and bulk writes.
- The phase can be executed with targeted tests and type checks before implementation is considered complete.
</success_criteria>

<output>
After completion, create `.planning/phases/34-legacy-financial-etl-import/34-01-SUMMARY.md`
</output>
