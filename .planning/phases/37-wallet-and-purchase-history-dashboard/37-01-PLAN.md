---
phase: 37-wallet-and-purchase-history-dashboard
plan: 01
type: execute
wave: 1
depends_on: [25-02, 34-01, 36-01]
files_modified:
  - apps/api/src/finance/types.ts
  - apps/api/src/finance/finance.service.ts
  - apps/api/src/finance/payment.controller.ts
  - apps/api/src/finance/purchase.controller.ts
  - apps/api/src/finance/__tests__/payment.spec.ts
  - apps/api/src/finance/__tests__/purchase.spec.ts
  - apps/api/src/finance/__tests__/purchase-history.spec.ts
  - apps/web/src/features/finance/types.ts
  - apps/web/src/features/finance/api.ts
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/src/features/dashboard/__tests__/dashboard-wallet-history.dom.test.jsx
  - apps/web/app/globals.css
autonomous: true
requirements: [ACCOUNT-02]
must_haves:
  truths:
    - "Signed-in users can see wallet balances plus current VIP tier in dashboard wallet section"
    - "Signed-in users can browse purchased chapter history with chapter title, author, purchase date, and unlock state"
    - "Purchase history supports 100+ rows through explicit pagination controls"
    - "History unlock state aligns with chapter reader access behavior for the same user"
  artifacts:
    - path: "apps/api/src/finance/finance.service.ts"
      provides: "wallet summary with VIP tier projection and paginated purchase-history query"
      exports: ["getWalletSummary", "listPurchaseHistory"]
    - path: "apps/api/src/finance/purchase.controller.ts"
      provides: "authenticated purchase-history HTTP endpoint"
      exports: ["getNovelPricing", "purchaseChapter", "purchaseNovelCombo", "listPurchaseHistory"]
    - path: "apps/web/src/features/finance/api.ts"
      provides: "wallet summary + purchase-history API client wrappers"
      exports: ["fetchWalletSummary", "fetchPurchaseHistory"]
    - path: "apps/web/src/features/dashboard/dashboard.tsx"
      provides: "wallet VIP display and purchase-history table with pagination"
      min_lines: 250
    - path: "apps/web/src/features/dashboard/__tests__/dashboard-wallet-history.dom.test.jsx"
      provides: "DOM tests for VIP rendering, purchase metadata rendering, and pagination"
      min_lines: 120
  key_links:
    - from: "apps/web/src/features/finance/api.ts"
      to: "/finance/wallet/summary"
      via: "fetchWalletSummary"
      pattern: "wallet/summary"
    - from: "apps/web/src/features/finance/api.ts"
      to: "/finance/purchases/history"
      via: "fetchPurchaseHistory"
      pattern: "purchases/history"
    - from: "apps/api/src/finance/finance.service.ts"
      to: "prisma.purchasedChapter + prisma.chapter + prisma.novel + prisma.user"
      via: "joined purchase-history projection"
      pattern: "purchasedChapter.findMany"

---

<objective>
Deliver ACCOUNT-02 by adding wallet VIP visibility and a performant purchased-chapter history experience in the dashboard.

Purpose: make legacy ETL-imported financial/account data visible and actionable for end users in a single account hub.
Output: API contracts for VIP + paginated purchase history, dashboard UI rendering, and automated tests covering metadata and pagination behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-dashboard-foundation/24-01-SUMMARY.md
@.planning/phases/25-wallet-top-up-and-chapter-purchases/25-01-SUMMARY.md
@.planning/phases/25-wallet-top-up-and-chapter-purchases/25-02-SUMMARY.md
@.planning/phases/34-legacy-financial-etl-import/34-01-SUMMARY.md
@.planning/phases/36-user-profile-security-management/36-01-SUMMARY.md
@apps/api/src/finance/types.ts
@apps/api/src/finance/finance.service.ts
@apps/api/src/finance/payment.controller.ts
@apps/api/src/finance/purchase.controller.ts
@apps/web/src/features/finance/types.ts
@apps/web/src/features/finance/api.ts
@apps/web/src/features/dashboard/dashboard.tsx

<interfaces>
From apps/api/src/finance/types.ts:
```typescript
export interface WalletSummaryResponse {
  balances: {
    depositedBalance: number;
    earnedBalance: number;
    totalDeposited: number;
  };
  purchaseSummary: {
    recentActions: number;
    recentSpent: number;
  };
  transactions: WalletSummaryItem[];
}
```

From apps/api/src/finance/payment.controller.ts:
```typescript
@Get('wallet/summary')
async getWalletSummary(@Req() req: { user?: { id: number } })
```

From apps/api/src/finance/purchase.controller.ts:
```typescript
@Get('novels/:novelId/pricing')
@Post('chapters/:chapterId')
@Post('novels/:novelId/combo')
```

From apps/web/src/features/finance/api.ts:
```typescript
export async function fetchWalletSummary(
  token?: string,
  signal?: AbortSignal,
): Promise<ApiResult<WalletSummaryResponse>>
```

From apps/web/src/features/dashboard/dashboard.tsx:
```typescript
function renderWalletSection()
function renderPurchasesSection()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API support for VIP tier projection and paginated purchase-history metadata</name>
  <files>
    apps/api/src/finance/types.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/payment.controller.ts
    apps/api/src/finance/purchase.controller.ts
    apps/api/src/finance/__tests__/payment.spec.ts
    apps/api/src/finance/__tests__/purchase.spec.ts
    apps/api/src/finance/__tests__/purchase-history.spec.ts
  </files>
  <behavior>
    - Test 1: wallet summary includes current VIP tier payload (level id/name/range metadata) while preserving balances and purchaseSummary shape.
    - Test 2: purchase-history endpoint returns purchased chapters with chapter title, author display, purchase date, and unlock status per row.
    - Test 3: purchase-history endpoint supports pagination (page + pageSize bounded) and stable newest-first ordering for 100+ rows.
  </behavior>
  <action>
Implement ACCOUNT-02 backend contracts by extending existing finance module without introducing new dependencies.

Details:
- Extend `WalletSummaryResponse` to include `vipTier` derived from `User.currentVipLevelId` + `VipLevel` relation.
- Add paginated purchase-history contract and service method (`listPurchaseHistory`) that joins purchase rows to chapter/novel/author metadata and returns deterministic `unlockStatus` values.
- Add authenticated route `GET /finance/purchases/history` in `purchase.controller.ts` that accepts `page` and `pageSize` query params.
- Preserve existing top-up and purchase APIs (no regression to phase 25 flows).
- Add/extend API tests to cover VIP projection, metadata shape, and pagination boundaries.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/payment.spec.ts src/finance/__tests__/purchase.spec.ts src/finance/__tests__/purchase-history.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Wallet summary and purchase-history APIs expose ACCOUNT-02 data contracts with passing backend tests and type safety.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render wallet VIP and purchased-chapter history UI with scalable pagination</name>
  <files>
    apps/web/src/features/finance/types.ts
    apps/web/src/features/finance/api.ts
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/src/features/dashboard/__tests__/dashboard-wallet-history.dom.test.jsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: dashboard wallet section renders VIP tier details alongside balances.
    - Test 2: purchases section renders chapter title, author, purchase date, and unlock status for returned rows.
    - Test 3: pagination controls fetch additional pages and maintain stable list behavior when dataset exceeds 100 entries.
  </behavior>
  <action>
Implement ACCOUNT-02 dashboard UX using existing section routing and finance feature-local API wrappers.

Details:
- Extend finance frontend types and API wrapper with `fetchPurchaseHistory(page, pageSize)` and updated wallet summary VIP fields.
- Update purchases section in `dashboard.tsx` to render a dedicated purchase-history table/list with required metadata columns and unlock status badges.
- Add pagination controls (`Previous`/`Next` + page indicator) to keep rendering bounded and responsive for large histories.
- Wire unlock status actions to existing reader navigation routes (link to chapter reader when readable).
- Preserve existing wallet top-up and combo purchase interactions from phase 25.
- Add focused DOM tests for VIP display, purchase metadata rendering, and pagination behavior.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/features/dashboard/__tests__/dashboard-wallet-history.dom.test.jsx && npx eslint src/features/finance/types.ts src/features/finance/api.ts src/features/dashboard/dashboard.tsx src/features/dashboard/__tests__/dashboard-wallet-history.dom.test.jsx app/globals.css && npm run check-types</automated>
  </verify>
  <done>
Dashboard account pages show VIP + purchase-history metadata with pagination and no regressions in wallet/purchases interactions.
  </done>
</task>

</tasks>

<verification>
- Account wallet section displays balances and VIP tier from the signed-in user profile.
- Purchases section displays purchased chapter metadata and unlock state with scalable pagination.
- Existing top-up, combo purchase, and profile sections remain functional.
</verification>

<success_criteria>
ACCOUNT-02 is complete: users can view wallet balances, VIP level, and paginated purchased chapter history with reader-access-aware status.
</success_criteria>

<output>
After completion, create `.planning/phases/37-wallet-and-purchase-history-dashboard/37-01-SUMMARY.md`
</output>
