---
phase: 17-social-interaction-apis
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/api/src/social/social.module.ts
  - apps/api/src/social/social.service.ts
  - apps/api/src/social/social-comments.controller.ts
  - apps/api/src/social/social-reactions.controller.ts
  - apps/api/src/social/types.ts
  - apps/api/src/social/__tests__/social-comments.spec.ts
  - apps/api/src/social/__tests__/social-reactions.spec.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements: [SOC-01, SOC-02, SOC-03, SOC-04, SOC-05]
must_haves:
  truths:
    - "Clients can fetch nested comment trees for a novel or chapter with replies ordered consistently"
    - "Authenticated users can create top-level comments and replies with scope validation"
    - "Authenticated users can toggle a single reaction per comment without duplicate reaction rows"
  artifacts:
    - path: "apps/api/src/social/social.service.ts"
      provides: "Comment tree retrieval, create/reply validation, and reaction toggle business logic"
      min_lines: 220
    - path: "apps/api/src/social/social-comments.controller.ts"
      provides: "GET and POST comment endpoints for novel/chapter discussion"
      min_lines: 90
    - path: "apps/api/src/social/social-reactions.controller.ts"
      provides: "Reaction toggle endpoint for authenticated users"
      min_lines: 60
    - path: "apps/api/src/social/social.module.ts"
      provides: "Module wiring for social discussion APIs"
      min_lines: 20
  key_links:
    - from: "apps/api/src/social/social-comments.controller.ts"
      to: "apps/api/src/social/social.service.ts"
      via: "tree query and createComment() orchestration"
      pattern: "createComment|listComments"
    - from: "apps/api/src/social/social-reactions.controller.ts"
      to: "apps/api/src/social/social.service.ts"
      via: "toggleReaction() uses unique userId+commentId constraint"
      pattern: "toggleReaction"
    - from: "apps/api/src/social/social.module.ts"
      to: "apps/api/src/app.module.ts"
      via: "module import registration"
      pattern: "SocialModule"
---

<objective>
Implement Phase 17 social interaction APIs so frontend clients can render nested comment threads and let authenticated users post replies and toggle reactions.

Purpose: add discussion/engagement primitives after the reader delivery APIs are in place.
Output: Social module with tested comment retrieval, comment creation, and reaction toggle behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@apps/api/prisma/schema.prisma
@apps/api/src/app.module.ts
@apps/api/src/reader/reader-personal.controller.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
enum CommentReactionType {
  LIKE
  HEART
  HAHA
  WOW
  SAD
  ANGRY
}

model Comment {
  id        Int
  userId    Int
  novelId   Int?
  chapterId Int?
  parentId  Int?
  content   String
  createdAt DateTime
  updatedAt DateTime
  replies   Comment[]
  reactions CommentReaction[]
}

model CommentReaction {
  id        Int
  userId    Int
  commentId Int
  type      CommentReactionType
  createdAt DateTime

  @@unique([userId, commentId])
}
```

From apps/api/src/reader/reader-personal.controller.ts:
```typescript
@UseGuards(RolesGuard)
@Roles('USER', 'AUTHOR', 'ADMIN')
```

From apps/api/src/app.module.ts:
```typescript
@Module({
  imports: [
    PassportModule.register({ defaultStrategy: 'google' }),
    CmsModule,
    ReaderModule,
  ],
  controllers: [AppController, NovelsController, UsersController],
  providers: [AppService, PrismaService, GoogleStrategy, RolesGuard],
  exports: [RolesGuard],
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define social comment thread contracts and retrieval behavior</name>
  <files>
    apps/api/src/social/types.ts
    apps/api/src/social/social.service.ts
    apps/api/src/social/social-comments.controller.ts
    apps/api/src/social/__tests__/social-comments.spec.ts
  </files>
  <behavior>
    - Test 1: GET /social/comments returns a nested tree for novelId or chapterId scope.
    - Test 2: reply rows are attached under their parent comment and ordered deterministically.
    - Test 3: comment retrieval rejects ambiguous scope inputs and never mixes novel/chapter threads.
  </behavior>
  <action>
Create the social comment contract layer first, then implement retrieval.

Implementation details:
- Use the existing Comment model and its self-relation to build thread trees.
- Keep comment reads public unless a later scope rule requires auth.
- Shape response data for frontend comment cards, including author identifiers and reply arrays.
- Normalize scope inputs so exactly one of `novelId` or `chapterId` drives each tree query.
- Keep tree-building logic in the service so the controller stays thin.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/social/__tests__/social-comments.spec.ts</automated>
  </verify>
  <done>
Threaded comment retrieval works for novel/chapter scopes with stable nested reply ordering.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated comment creation and reply validation</name>
  <files>
    apps/api/src/social/social.service.ts
    apps/api/src/social/social-comments.controller.ts
    apps/api/src/social/__tests__/social-comments.spec.ts
  </files>
  <behavior>
    - Test 1: authenticated user can create a top-level comment for a novel or chapter.
    - Test 2: authenticated user can reply to an existing comment with parentId.
    - Test 3: comment creation validates scope ownership rules and rejects invalid novel/chapter combinations.
    - Test 4: unauthenticated comment creation is rejected by the existing guard pattern.
  </behavior>
  <action>
Add comment creation and reply workflows on top of the retrieval foundation.

Implementation details:
- Accept `content`, `novelId`, `chapterId`, and optional `parentId`.
- Enforce that every write has exactly one top-level scope target and that replies inherit the same scope as their parent thread.
- Use the authenticated user id from the request for ownership, not client-supplied identity fields.
- Reuse the existing `RolesGuard` + roles decorator pattern for signed-in access.
- Return frontend-friendly comment payloads after create/reply so clients can update threads without refetching the whole tree.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/social/__tests__/social-comments.spec.ts</automated>
  </verify>
  <done>
Comment creation and reply behavior is authenticated, scope-safe, and covered by tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add reaction toggle endpoint and wire social module into the app</name>
  <files>
    apps/api/src/social/social.module.ts
    apps/api/src/social/social.service.ts
    apps/api/src/social/social-reactions.controller.ts
    apps/api/src/social/__tests__/social-reactions.spec.ts
    apps/api/src/app.module.ts
  </files>
  <behavior>
    - Test 1: authenticated user can toggle LIKE, HEART, HAHA, WOW, SAD, and ANGRY reactions.
    - Test 2: toggling the same reaction twice removes the row instead of duplicating it.
    - Test 3: changing reaction type updates the existing user/comment record rather than creating a second row.
    - Test 4: concurrent duplicate reaction attempts respect the unique userId+commentId constraint.
  </behavior>
  <action>
Implement the reaction toggle path and finish module wiring.

Implementation details:
- Add a dedicated reaction controller that accepts a comment id and `CommentReactionType`.
- Use a transaction or upsert-based path keyed by `userId + commentId` so duplicates cannot appear.
- Keep reaction logic in the shared social service so comment and reaction behavior stay consistent.
- Register the new module in `AppModule` using the same pattern as `ReaderModule` and `CmsModule`.
- After code passes, run full API regression and typecheck to catch interaction bugs between social and existing modules.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/social/__tests__/social-reactions.spec.ts && npm test --workspace=api -- --runInBand && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Reaction toggles are authenticated, uniqueness-safe, and fully wired into the Nest application.
  </done>
</task>

</tasks>

<verification>
- Clients can render nested comment threads for both novel and chapter discussion scopes.
- Authenticated users can post top-level comments and replies without breaking scope validation.
- Reactions toggle deterministically with no duplicate `CommentReaction` rows.
</verification>

<success_criteria>
Phase 17 is complete when all SOC-01 through SOC-05 requirements are implemented, the social module is mounted in the API app, and the targeted + full regression/typecheck commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/17-social-interaction-apis/17-01-SUMMARY.md`
</output>
