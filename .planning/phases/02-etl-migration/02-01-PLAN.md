---
phase: 02-etl-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/etl/config.ts
  - apps/api/src/etl/types.ts
  - apps/api/src/etl/mysql-client.ts
  - apps/api/src/etl/prisma-client.ts
  - apps/api/src/etl/parse-wordpress.ts
  - apps/api/src/etl/quarantine-repository.ts
  - apps/api/src/etl/__tests__/parse-wordpress.spec.ts
autonomous: true
requirements:
  - MIG-01
  - MIG-03
  - MIG-06
user_setup:
  - service: wordpress-mysql
    why: "Source system for ETL extraction"
    env_vars:
      - name: WP_MYSQL_HOST
        source: "WordPress hosting or DB admin panel"
      - name: WP_MYSQL_PORT
        source: "WordPress MySQL instance settings"
      - name: WP_MYSQL_USER
        source: "WordPress MySQL credentials"
      - name: WP_MYSQL_PASSWORD
        source: "WordPress MySQL credentials"
      - name: WP_MYSQL_DATABASE
        source: "WordPress database name"
  - service: postgresql-target
    why: "Destination system for normalized records"
    env_vars:
      - name: DATABASE_URL
        source: "PostgreSQL connection string for target database"
must_haves:
  truths:
    - "The ETL process can connect to both source MySQL and target PostgreSQL with explicit configuration."
    - "Serialized WordPress metadata is parsed deterministically, and parse failures trigger user-level skip behavior per D-07."
    - "Reruns can safely reuse stable idempotent keys without creating duplicate logical records per D-09."
  artifacts:
    - path: "apps/api/src/etl/config.ts"
      provides: "Typed ETL runtime configuration and env validation"
      min_lines: 40
    - path: "apps/api/src/etl/parse-wordpress.ts"
      provides: "Serialization decoding helpers and strict parse contracts"
      min_lines: 80
    - path: "apps/api/src/etl/quarantine-repository.ts"
      provides: "Quarantine writer for skipped users and mapping failures"
      min_lines: 40
  key_links:
    - from: "apps/api/src/etl/config.ts"
      to: "apps/api/src/etl/mysql-client.ts"
      via: "source DB connection bootstrap"
      pattern: "WP_MYSQL_"
    - from: "apps/api/src/etl/parse-wordpress.ts"
      to: "apps/api/src/etl/quarantine-repository.ts"
      via: "parse failure -> skip user + log"
      pattern: "skip|quarantine|error"
    - from: "apps/api/src/etl/types.ts"
      to: "apps/api/src/etl/*"
      via: "shared idempotent key contracts"
      pattern: "sourceUserId|chapterId|provider"
---

<objective>
Create the ETL foundation contracts and adapters that all migration steps build on.

Purpose: De-risk the migration by locking configuration, parsing, and quarantine behavior before domain writes begin.
Output: Typed ETL infrastructure (config, connectors, parser, quarantine store, tests) aligned with D-06, D-07, D-09.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-etl-migration/02-CONTEXT.md
@.planning/phases/01-schema-foundation/01-01-SUMMARY.md
@apps/api/package.json
@apps/api/prisma/schema.prisma
@apps/api/src/main.ts
@apps/api/src/app.module.ts

<interfaces>
From apps/api/src/main.ts:
```typescript
async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  await app.listen(process.env.PORT ?? 8000);
}
```

From apps/api/src/app.module.ts:
```typescript
@Module({
  imports: [],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

From apps/api/prisma/schema.prisma:
```prisma
model User { id Int @id ... }
model UserProvider { @@unique([provider, providerId]) }
model Wallet { userId Int @unique }
model VipSubscription { userId Int @unique }
model PurchasedChapter { @@unique([userId, chapterId]) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ETL contracts, env config, and database adapters</name>
  <files>apps/api/package.json, apps/api/src/etl/config.ts, apps/api/src/etl/types.ts, apps/api/src/etl/mysql-client.ts, apps/api/src/etl/prisma-client.ts</files>
  <behavior>
    - Missing required source or target env variables causes startup validation error.
    - MySQL and PostgreSQL adapters expose connect/disconnect methods with typed config.
    - Shared ETL types include canonical WP user IDs and idempotence keys per D-09.
  </behavior>
  <action>
Add ETL dependencies and scripts to the API workspace (for example `mysql2`, `php-serialize`, and a migration run command) and create typed config + client adapters for both databases. Enforce explicit env validation so the job never starts in a partially configured state. The shared ETL types must encode canonical key contracts (per D-09) and reference WordPress user IDs as source-of-truth identifiers from Phase 1 and D-06.
  </action>
  <verify>
    <automated>cd apps/api && npm run lint && npm run check-types || npx tsc --noEmit</automated>
  </verify>
  <done>ETL runtime can be instantiated with validated env config and both DB adapters compile cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict metadata parsing and quarantine contracts</name>
  <files>apps/api/src/etl/parse-wordpress.ts, apps/api/src/etl/quarantine-repository.ts, apps/api/src/etl/__tests__/parse-wordpress.spec.ts</files>
  <behavior>
    - Valid `vip_package` and `_purchased_chapters` values deserialize into typed normalized objects.
    - Invalid serialized payload returns parse failure metadata and triggers user-level skip decision per D-07.
    - Quarantine record captures source user ID, failure reason, and raw evidence needed for Phase 2 remediation per D-06.
  </behavior>
  <action>
Build parser helpers for WordPress serialized payloads with deterministic success/failure outputs. Do not silently coerce invalid payloads; instead surface explicit parse errors and write a quarantine entry. Implement tests first for valid payloads, malformed payloads, and empty/missing metadata behavior. Keep parsing code pure and side-effect free; quarantine I/O belongs in repository helper.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand parse-wordpress.spec.ts</automated>
  </verify>
  <done>Serialized metadata parsing is test-covered and parse failures produce quarantine-ready error objects without lossy fallback.</done>
</task>

</tasks>

<verification>
Phase foundation is complete when ETL configuration, adapters, and parser/quarantine contracts all compile and parser tests prove D-07 parse-or-skip behavior.
</verification>

<success_criteria>
- ETL can validate env config for both MySQL source and PostgreSQL target before running.
- Parsing logic decodes valid serialized metadata and explicitly marks invalid payloads for quarantine.
- Shared contracts encode idempotent keys required by rerun-safe writes.
- Parser tests pass and prove strict non-lossy handling.
</success_criteria>

<output>
After completion, create `.planning/phases/02-etl-migration/02-01-SUMMARY.md`.
</output>
