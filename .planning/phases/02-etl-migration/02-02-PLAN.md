---
phase: 02-etl-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/etl/migrate-users.ts
  - apps/api/src/etl/migrate-providers.ts
  - apps/api/src/etl/migrate-wallets.ts
  - apps/api/src/etl/migrate-vip.ts
  - apps/api/src/etl/migrate-transactions.ts
  - apps/api/src/etl/idempotence.ts
  - apps/api/src/etl/__tests__/migrate-users.spec.ts
  - apps/api/src/etl/__tests__/migrate-transactions.spec.ts
autonomous: true
requirements:
  - MIG-02
  - MIG-05
  - MIG-06
must_haves:
  truths:
    - "Users, social mappings, wallets, and VIP records migrate with canonical WordPress identifiers preserved."
    - "Transaction history migrates with essential legacy details (amounts, timestamps, content) preserved."
    - "Reruns update-or-skip deterministically instead of duplicating logical records."
  artifacts:
    - path: "apps/api/src/etl/migrate-users.ts"
      provides: "User/provider/wallet migration orchestration"
      min_lines: 100
    - path: "apps/api/src/etl/migrate-vip.ts"
      provides: "VIP migration from parsed metadata"
      min_lines: 60
    - path: "apps/api/src/etl/migrate-transactions.ts"
      provides: "Transaction migration with normalized enum mapping"
      min_lines: 80
    - path: "apps/api/src/etl/idempotence.ts"
      provides: "Stable upsert-key utilities and dedupe strategy"
      min_lines: 40
  key_links:
    - from: "apps/api/src/etl/migrate-users.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "User/Wallet/UserProvider upserts"
      pattern: "upsert|createMany|update"
    - from: "apps/api/src/etl/migrate-vip.ts"
      to: "apps/api/src/etl/parse-wordpress.ts"
      via: "parsed vip_package payload"
      pattern: "vip"
    - from: "apps/api/src/etl/migrate-transactions.ts"
      to: "TransactionType"
      via: "legacy transaction kind mapping"
      pattern: "DEPOSIT|PURCHASE_"
---

<objective>
Implement the primary domain migration steps for users, identity mappings, wallet balances, VIP data, and transactions.

Purpose: Deliver the highest-risk financial and identity migration paths using strict parsing outcomes and idempotent writes.
Output: Executable migration modules for users/providers/wallets/VIP/transactions with rerun-safe semantics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-etl-migration/02-CONTEXT.md
@.planning/phases/02-etl-migration/02-01-PLAN.md
@apps/api/prisma/schema.prisma
@apps/api/src/etl/types.ts
@apps/api/src/etl/parse-wordpress.ts
@apps/api/src/etl/quarantine-repository.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model User { id Int @id email String @unique ... }
model UserProvider { @@unique([provider, providerId]) }
model Wallet { userId Int @unique balance Decimal }
model VipSubscription { userId Int @unique }
model Transaction { userId Int amountIn Decimal amountOut Decimal transactionDate DateTime }
```

Contracts from 02-01 expected:
```typescript
export type EtlConfig = { source: {...}; target: {...}; };
export type ParsedVipPackage = {...};
export type ParseFailure = { sourceUserId: number; reason: string; raw: unknown };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build idempotent user/provider/wallet migration pipeline</name>
  <files>apps/api/src/etl/migrate-users.ts, apps/api/src/etl/migrate-providers.ts, apps/api/src/etl/migrate-wallets.ts, apps/api/src/etl/idempotence.ts, apps/api/src/etl/__tests__/migrate-users.spec.ts</files>
  <behavior>
    - User migration preserves WordPress ID and password hash semantics.
    - Social login mapping (Google provider records) upserts against provider + providerId uniqueness.
    - Wallet migration sets exact balance without creating duplicate wallet rows on reruns.
    - Quarantined users from parser failures are skipped from downstream writes per D-06 and D-07.
  </behavior>
  <action>
Implement migration modules that read source rows and upsert destination rows for User, UserProvider, and Wallet. Use D-09 idempotence keys and explicit skip paths for users quarantined by parse or mapping failures. Add tests first for duplicate reruns, provider uniqueness collisions, and wallet overwrite behavior so reruns remain deterministic. Keep per-user processing deterministic for resume semantics.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand migrate-users.spec.ts</automated>
  </verify>
  <done>User/provider/wallet modules migrate canonical records and reruns do not duplicate logical entities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VIP and transaction migration with preserved legacy semantics</name>
  <files>apps/api/src/etl/migrate-vip.ts, apps/api/src/etl/migrate-transactions.ts, apps/api/src/etl/__tests__/migrate-transactions.spec.ts</files>
  <behavior>
    - VIP migration uses parsed `vip_package` payload only; invalid payload users are skipped and quarantined.
    - Transaction migration preserves amountIn, amountOut, accumulated, timestamp, and content where available.
    - Legacy transaction types map to Prisma `TransactionType` enum consistently and reruns remain idempotent.
  </behavior>
  <action>
Implement VIP and transaction migration modules that depend on 02-01 parser outputs and apply strict skip policy from D-07. Preserve the best-available source timestamps and textual content. Introduce deterministic transaction idempotence keying (for example hash of userId/date/type/content/amount tuple) so reruns update-or-skip without duplicating ledger entries, per D-09. Write tests first for type mapping, timestamp preservation, and duplicate rerun behavior.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand migrate-transactions.spec.ts</automated>
  </verify>
  <done>VIP and transaction modules preserve legacy data fidelity and remain rerun-safe across repeated ETL runs.</done>
</task>

</tasks>

<verification>
The plan is complete when user/provider/wallet/vip/transaction migrations run against typed contracts and tests prove deterministic idempotence for reruns.
</verification>

<success_criteria>
- Users, providers, wallets, VIP records, and transactions load into normalized Prisma tables with canonical keys.
- Quarantined users are skipped from all affected destination writes.
- Transaction mappings preserve financial meaning and timestamps.
- Rerunning the same source dataset does not duplicate destination logical records.
</success_criteria>

<output>
After completion, create `.planning/phases/02-etl-migration/02-02-SUMMARY.md`.
</output>
