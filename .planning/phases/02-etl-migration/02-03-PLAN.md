---
phase: 02-etl-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - apps/api/src/etl/migrate-purchased-chapters.ts
  - apps/api/src/etl/etl-runner.ts
  - apps/api/src/etl/summary-report.ts
  - apps/api/src/etl/index.ts
  - apps/api/src/etl/__tests__/migrate-purchased-chapters.spec.ts
  - apps/api/src/etl/__tests__/etl-runner.spec.ts
  - apps/api/package.json
autonomous: true
requirements:
  - MIG-03
  - MIG-04
  - MIG-06
must_haves:
  truths:
    - "Purchased chapters migrate using per-user streaming and bounded chunks, not global unbounded buffers."
    - "ETL execution produces actionable summary output including processed, skipped, and quarantined users."
    - "Reruns skip duplicate purchased chapters and preserve consistency across full pipeline execution."
  artifacts:
    - path: "apps/api/src/etl/migrate-purchased-chapters.ts"
      provides: "Per-user streamed purchased chapter migration with chunked createMany"
      min_lines: 100
    - path: "apps/api/src/etl/etl-runner.ts"
      provides: "End-to-end execution orchestration for all migration modules"
      min_lines: 120
    - path: "apps/api/src/etl/summary-report.ts"
      provides: "Final run metrics and quarantine summary"
      min_lines: 50
  key_links:
    - from: "apps/api/src/etl/etl-runner.ts"
      to: "apps/api/src/etl/migrate-purchased-chapters.ts"
      via: "per-user chapter phase invocation"
      pattern: "for.*user|chunk|createMany"
    - from: "apps/api/src/etl/migrate-purchased-chapters.ts"
      to: "PurchasedChapter @@unique([userId, chapterId])"
      via: "skip duplicates on rerun"
      pattern: "skipDuplicates|upsert|unique"
    - from: "apps/api/src/etl/summary-report.ts"
      to: "quarantine-repository"
      via: "aggregate skipped user reasons"
      pattern: "quarantine|skipped"
---

<objective>
Deliver high-volume purchased chapter migration and wire the full ETL command entrypoint.

Purpose: Complete the scale-critical part of migration (million-row chapter history) while preserving resumability and visibility.
Output: Per-user chunked chapter migrator and executable ETL runner command with final summary reporting.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-etl-migration/02-CONTEXT.md
@.planning/phases/02-etl-migration/02-01-PLAN.md
@.planning/phases/02-etl-migration/02-02-PLAN.md
@apps/api/prisma/schema.prisma
@apps/api/package.json
@apps/api/src/etl/migrate-users.ts
@apps/api/src/etl/migrate-transactions.ts
@apps/api/src/etl/quarantine-repository.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model PurchasedChapter {
  id Int @id @default(autoincrement())
  userId Int
  novelId Int
  chapterId Int
  @@unique([userId, chapterId])
  @@index([userId, novelId])
}
```

Contracts from earlier plans expected:
```typescript
type QuarantineEntry = { sourceUserId: number; reason: string; raw: unknown };
type MigrationStats = { processedUsers: number; skippedUsers: number; chaptersInserted: number };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement per-user chunked purchased chapter migration</name>
  <files>apps/api/src/etl/migrate-purchased-chapters.ts, apps/api/src/etl/__tests__/migrate-purchased-chapters.spec.ts</files>
  <behavior>
    - Chapters are processed user-by-user and inserted in fixed chunks of 1000 per D-08.
    - Duplicate chapter purchases are skipped deterministically on reruns per D-09.
    - Users with parse failures are skipped entirely and counted in quarantine totals per D-07.
  </behavior>
  <action>
Create the purchased chapter migrator using per-user streaming. For each eligible user, map `_purchased_chapters` payload into normalized records and write via `createMany()` in chunk size 1000 (per D-08). Enforce duplicate safety with schema uniqueness and explicit duplicate-skip semantics so reruns are safe (per D-09). Add tests for chunk boundaries, duplicate rerun behavior, and skip propagation from quarantine list.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand migrate-purchased-chapters.spec.ts</automated>
  </verify>
  <done>Purchased chapter migration scales with bounded memory and stays rerun-safe for repeated executions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire full ETL runner command and run summary output</name>
  <files>apps/api/src/etl/etl-runner.ts, apps/api/src/etl/summary-report.ts, apps/api/src/etl/index.ts, apps/api/package.json, apps/api/src/etl/__tests__/etl-runner.spec.ts</files>
  <behavior>
    - Runner executes migrations in deterministic order: users/providers/wallets -> VIP/transactions -> purchased chapters.
    - Runner writes summary metrics including processed/skipped users, quarantined users, and table-level write counts.
    - Fatal connection errors fail fast; row-level mapping issues quarantine and continue per D-06.
  </behavior>
  <action>
Implement the top-level ETL command entrypoint and wire it into `apps/api/package.json` scripts. Orchestrate all migration modules with explicit sequencing and shared stats context. Ensure quarantine output is persisted and summarized at the end. Add tests first for run-order, failure handling boundaries (fatal vs quarantined), and summary metrics shape. Keep this as CLI-oriented execution (no HTTP endpoint required in this phase).
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand etl-runner.spec.ts && npm run lint</automated>
  </verify>
  <done>One command runs the full ETL pipeline with deterministic ordering, quarantine-aware continuation, and actionable completion summary.</done>
</task>

</tasks>

<verification>
The ETL phase is execution-ready when purchased chapters process in per-user chunks, the full runner command orchestrates all modules, and reruns remain duplicate-safe with clear summaries.
</verification>

<success_criteria>
- Purchased chapter migration uses per-user chunking and does not rely on global unbounded buffers.
- End-to-end ETL runner command exists and executes all domain migration stages in the intended order.
- Run summary includes counts for processed/skipped/quarantined entities and supports remediation workflow.
- Duplicate reruns do not create duplicate purchased chapter records.
</success_criteria>

<output>
After completion, create `.planning/phases/02-etl-migration/02-03-SUMMARY.md`.
</output>
