---
phase: 28-public-author-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/reader/types.ts
  - apps/api/src/reader/reader-discovery.controller.ts
  - apps/api/src/reader/reader.service.ts
  - apps/api/src/reader/__tests__/reader-author-profile.spec.ts
autonomous: true
requirements: [AUTHOR-01, AUTHOR-03]

must_haves:
  truths:
    - "Reader can request a public author profile payload without authentication"
    - "Unknown author id returns not-found behavior"
    - "Payload includes identity fallback and aggregate stats needed by profile UI"
  artifacts:
    - path: "apps/api/src/reader/reader-discovery.controller.ts"
      provides: "Public author profile endpoint"
      exports: ["GET /reader/authors/:id"]
    - path: "apps/api/src/reader/reader.service.ts"
      provides: "Author profile query orchestration and aggregation"
      exports: ["getAuthorProfile()"]
    - path: "apps/api/src/reader/types.ts"
      provides: "Author profile response contracts"
      exports: ["AuthorProfileResponse", "AuthorCatalogItem", "AuthorStats"]
    - path: "apps/api/src/reader/__tests__/reader-author-profile.spec.ts"
      provides: "Behavior coverage for author profile payload and 404 safety"
  key_links:
    - from: "apps/api/src/reader/reader-discovery.controller.ts"
      to: "apps/api/src/reader/reader.service.ts"
      via: "controller delegation"
      pattern: "this\.readerService\.getAuthorProfile"
    - from: "apps/api/src/reader/reader.service.ts"
      to: "prisma.user|prisma.authorProfile|prisma.novel"
      via: "author identity and aggregate queries"
      pattern: "prisma\.(user|authorProfile|novel)"
---

<objective>
Define and implement backend contracts for public author profiles, including identity fallback, authored novel catalog pagination metadata, and live aggregate stats.

Purpose: Deliver a stable public API contract that frontend can consume without auth while honoring D-01..D-12 (especially D-02, D-03, D-08, D-09, D-10, D-11).
Output: New typed public author endpoint + tests with deterministic not-found and aggregation behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-public-author-profiles/28-CONTEXT.md
@.planning/phases/16-reader-content-apis/16-01-SUMMARY.md

<interfaces>
From apps/api/src/reader/reader-discovery.controller.ts:
```typescript
@Get('novels')
async listNovels(...)
```

From apps/api/src/reader/types.ts:
```typescript
export interface ReaderDiscoveryQuery {
  page?: number;
  limit?: number;
  sortBy?: ReaderSortBy;
  sortDir?: ReaderSortDir;
  category?: string;
  tag?: string;
  status?: string;
}
```

From apps/api/src/reader/reader.service.ts:
```typescript
async listNovels(query: ReaderDiscoveryQuery)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add author profile API contracts and failing coverage first</name>
  <files>
    apps/api/src/reader/types.ts
    apps/api/src/reader/__tests__/reader-author-profile.spec.ts
  </files>
  <behavior>
    - Test 1: Existing author id resolves profile payload with identityName using penName fallback to nickname per D-02.
    - Test 2: Missing author id throws NotFoundException per D-11.
    - Test 3: Stats payload includes totalPublishedNovels, totalViews, latestUpdateAt per D-08.
  </behavior>
  <action>
Create explicit author profile response contracts in `reader/types.ts` and add `reader-author-profile.spec.ts` with RED-state tests for service behavior before implementation (per D-02, D-08, D-11). Keep contract fields aligned with CONTEXT.md decisions and include pagination metadata needed by UI task in plan 28-02.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-author-profile.spec.ts</automated>
  </verify>
  <done>
    Contracts compile and new author-profile tests exist with at least one expected failing assertion before implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement public author endpoint with live aggregate query</name>
  <files>
    apps/api/prisma/schema.prisma
    apps/api/src/reader/reader-discovery.controller.ts
    apps/api/src/reader/reader.service.ts
  </files>
  <behavior>
    - Test 1: GET /reader/authors/:id returns public payload without auth checks per D-10.
    - Test 2: Identity displayName resolves from penName then nickname per D-02.
    - Test 3: Stats and catalog ordering are live-computed (latest updated first) per D-06, D-08, D-09.
  </behavior>
  <action>
Implement `GET /reader/authors/:id` in discovery controller and `getAuthorProfile()` service logic with no auth guard (per D-10). Query user + author profile identity and return display name fallback (penName -> nickname, per D-02). Include avatar and bio fields in payload per D-03; if bio is missing, return null so frontend can render fallback layout per D-04. Return 404 when user id does not exist per D-11. Compute aggregate stats live (count novels, sum viewCount, latest updatedAt) per D-08 and D-09. Ensure authored novels are sorted by `updatedAt desc` by default per D-06 and include pagination metadata consumed by plan 28-02.
  </action>
  <verify>
    <automated>cd apps/api && npm run prisma:generate && npm test -- --runInBand src/reader/__tests__/reader-author-profile.spec.ts src/reader/__tests__/reader-discovery.spec.ts && npm run check-types</automated>
  </verify>
  <done>
    Public author endpoint returns contract-compliant data, 404 behavior is deterministic, and reader tests/typecheck pass.
  </done>
</task>

</tasks>

<verification>
- Backend only checks must pass: Prisma client generation, targeted reader tests, and api typecheck.
- Endpoint shape must include identity, stats, and catalog data required by AUTHOR-01 and AUTHOR-03.
</verification>

<success_criteria>
- AUTHOR-01 backend contract is public and stable for `/author/[id]` UI integration.
- AUTHOR-03 aggregates are returned from live backend queries with no caching layer.
- Decision traceability is preserved for D-02, D-03, D-04, D-06, D-08, D-09, D-10, D-11.
</success_criteria>

<output>
After completion, create `.planning/phases/28-public-author-profiles/28-01-SUMMARY.md`.
</output>
