---
phase: 28-public-author-profiles
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - apps/web/src/features/author-profile/types.ts
  - apps/web/src/features/author-profile/api.ts
  - apps/web/src/features/author-profile/author-profile.tsx
  - apps/web/app/author/[id]/page.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [AUTHOR-01, AUTHOR-02, AUTHOR-03]

must_haves:
  truths:
    - "Reader can open /author/[id] publicly and see author identity details"
    - "Reader can browse this author's novels using discovery-consistent cards"
    - "Reader sees aggregate stats and a friendly empty state when author has zero novels"
  artifacts:
    - path: "apps/web/app/author/[id]/page.tsx"
      provides: "Public author route entrypoint"
    - path: "apps/web/src/features/author-profile/author-profile.tsx"
      provides: "Author profile view with stats, catalog, empty/error states"
      min_lines: 90
    - path: "apps/web/src/features/author-profile/api.ts"
      provides: "Typed public author API wrapper"
      exports: ["fetchAuthorProfile", "getAuthorProfileHref"]
    - path: "apps/web/src/features/author-profile/types.ts"
      provides: "Author profile frontend contracts and defaults"
      exports: ["AuthorProfileResponse", "AuthorCatalogItem", "AuthorStats"]
  key_links:
    - from: "apps/web/src/features/author-profile/api.ts"
      to: "apps/api/src/reader/reader-discovery.controller.ts"
      via: "GET /reader/authors/:id"
      pattern: "apiRequest.*reader/authors"
    - from: "apps/web/src/features/author-profile/author-profile.tsx"
      to: "apps/web/src/features/discovery/discovery.tsx"
      via: "reused discovery-card class pattern per D-05"
      pattern: "discovery-card"
    - from: "apps/web/app/author/[id]/page.tsx"
      to: "apps/web/src/features/author-profile/author-profile.tsx"
      via: "route composition"
      pattern: "AuthorProfileView"
---

<objective>
Implement a fully public `/author/[id]` profile page that reuses discovery catalog patterns and displays live author stats with robust empty/error handling.

Purpose: Deliver AUTHOR-01..03 in the web app with consistency to existing discovery UX and strict adherence to D-01..D-12.
Output: New author profile frontend module + route + styling integrated with backend plan 28-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-public-author-profiles/28-CONTEXT.md
@.planning/phases/21-novel-discovery-ui/21-01-SUMMARY.md
@.planning/phases/28-public-author-profiles/28-01-SUMMARY.md

<interfaces>
From apps/web/src/features/discovery/types.ts:
```typescript
export interface DiscoveryNovel {
  id: number;
  title: string;
  viewCount: number;
  createdAt: string;
  updatedAt: string;
  terms: DiscoveryTerm[];
}
```

From apps/web/src/features/discovery/api.ts:
```typescript
export async function fetchDiscoveryNovels(init: DiscoveryQueryInit, signal?: AbortSignal)
```

From apps/web/src/features/discovery/discovery.tsx:
```tsx
<article className="discovery-card">...</article>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define author profile frontend contracts and API wrapper</name>
  <files>
    apps/web/src/features/author-profile/types.ts
    apps/web/src/features/author-profile/api.ts
  </files>
  <behavior>
    - Test 1: API wrapper resolves typed author payload and propagates 404 errors cleanly.
    - Test 2: Query/href helpers preserve pagination and default sort as updatedAt desc per D-06 and D-07.
  </behavior>
  <action>
Create a feature-local module mirroring discovery patterns (`types` + `api`). Define contracts matching backend output from plan 28-01. Implement `fetchAuthorProfile` as a public GET wrapper (no Bearer token) per D-10, and include href/query helpers for page changes. Keep defaults aligned with D-06 and D-07.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Author profile types/api compile, align with backend contract, and expose reusable query helpers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build author profile route and discovery-consistent catalog UI</name>
  <files>
    apps/web/src/features/author-profile/author-profile.tsx
    apps/web/app/author/[id]/page.tsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: `/author/[id]` renders author header with displayName fallback and avatar/bio section per D-02 and D-03.
    - Test 2: Catalog uses discovery-card pattern and paginates with latest updated order per D-05..D-07.
    - Test 3: Zero novels shows friendly empty state and unknown id shows not-found state per D-11 and D-12.
  </behavior>
  <action>
Create `AuthorProfileView` and route entry at `/author/[id]` (per D-01). Render author metadata with clean fallback layout when fields are incomplete (per D-04). Reuse existing discovery card classes/structure for catalog items exactly (per D-05), with paginated controls matching current discovery behavior (per D-07). Show stats cards for total novels, total views, and latest update date from API (per D-08). Keep page public and avoid any auth/session gating (per D-10). Render friendly empty-state copy when author exists with zero novels (per D-12). Handle backend 404 by presenting not-found UI and/or Next notFound flow (per D-11).
  </action>
  <verify>
    <automated>cd apps/web && npm run lint -- --max-warnings 0 src/features/author-profile/types.ts src/features/author-profile/api.ts src/features/author-profile/author-profile.tsx app/author/[id]/page.tsx && npm run check-types</automated>
  </verify>
  <done>
    Public author page is reachable, contract-driven, discovery-consistent, and satisfies AUTHOR-01..03 states (normal, empty, not-found).
  </done>
</task>

</tasks>

<verification>
- Web lint and typecheck pass for new author profile module and route files.
- Manual smoke check confirms `/author/[id]` works without sign-in and handles empty/not-found safely.
</verification>

<success_criteria>
- AUTHOR-01: public author profile page renders without auth barriers.
- AUTHOR-02: authored novel catalog is visible using discovery card visuals.
- AUTHOR-03: aggregate stats are displayed from live backend payload.
- Decision traceability preserved for D-01..D-12 where applicable.
</success_criteria>

<output>
After completion, create `.planning/phases/28-public-author-profiles/28-02-SUMMARY.md`.
</output>
