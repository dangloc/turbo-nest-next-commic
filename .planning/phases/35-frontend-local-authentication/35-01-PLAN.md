---
phase: 35-frontend-local-authentication
plan: 01
type: execute
wave: 1
depends_on: [20-02, 24-01]
files_modified:
  - apps/web/src/features/auth/types.ts
  - apps/web/src/features/auth/api.ts
  - apps/web/src/features/auth/validation.ts
  - apps/web/src/features/auth/__tests__/auth-local-api.test.ts
  - apps/web/src/features/auth/__tests__/auth-local-forms.dom.test.jsx
  - apps/web/app/auth/login/page.tsx
  - apps/web/app/auth/register/page.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "User can sign in with username or email plus password from the web UI"
    - "User can register with username, email, and password, then be signed in immediately"
    - "Validation and backend conflicts (duplicate username/email, invalid credentials, network failures) surface as clear inline feedback"
    - "Successful local auth updates app session state so protected dashboard flows work without manual refresh"
  artifacts:
    - path: "apps/web/src/features/auth/api.ts"
      provides: "typed local auth API wrappers for /auth/login and /auth/register"
      exports: ["loginLocal", "registerLocal", "extractAuthErrorMessage"]
    - path: "apps/web/app/auth/login/page.tsx"
      provides: "local login UX with remember-me support plus Google fallback"
      min_lines: 120
    - path: "apps/web/app/auth/register/page.tsx"
      provides: "local registration UX with password rules and duplicate handling"
      min_lines: 140
    - path: "apps/web/src/features/auth/__tests__/auth-local-forms.dom.test.jsx"
      provides: "DOM behavior coverage for login/register validation and submit flows"
      min_lines: 90
  key_links:
    - from: "apps/web/src/features/auth/api.ts"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "POST /auth/login and POST /auth/register"
      pattern: "/auth/login|/auth/register"
    - from: "apps/web/app/auth/login/page.tsx"
      to: "apps/web/src/lib/auth/session-store.ts"
      via: "persistSessionToken and persistSessionToStorage after successful auth"
      pattern: "persistSessionToken|persistSessionToStorage"
    - from: "apps/web/app/auth/register/page.tsx"
      to: "apps/web/src/providers/app-provider.tsx"
      via: "setUser after successful auth"
      pattern: "useContext\(AppContext\)|setUser"
---

<objective>
Deliver phase-35 local authentication UX in the Next.js frontend: login and registration screens that consume the new backend local auth endpoints and integrate with current session bootstrap behavior.

Purpose: satisfy AUTH-01 and AUTH-02 for v1.14 so users can authenticate without Google-only dependency and immediately access dashboard/account flows.
Output: typed auth feature module, local login/register pages, validation/error UX, and automated frontend tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-frontend-foundation-auth-integration/20-02-SUMMARY.md
@.planning/phases/24-dashboard-foundation/24-01-SUMMARY.md
@apps/api/src/auth/auth.controller.ts
@apps/api/src/auth/dto/login.dto.ts
@apps/api/src/auth/dto/register.dto.ts
@apps/web/src/lib/api/http.ts
@apps/web/src/lib/api/types.ts
@apps/web/src/lib/auth/session-store.ts
@apps/web/src/lib/auth/api.ts
@apps/web/src/providers/app-provider.tsx
@apps/web/app/auth/login/page.tsx
@apps/web/app/page.tsx

<interfaces>
From apps/api/src/auth/auth.controller.ts:
```typescript
@Post('register')
@HttpCode(201)
async register(@Body() body: RegisterDto, @Res({ passthrough: true }) res: Response)

@Post('login')
async login(@Body() body: LoginDto, @Res({ passthrough: true }) res: Response)
```

From apps/api/src/auth/dto/login.dto.ts:
```typescript
class LoginDto {
  username: string; // accepts username or email identifier
  password: string;
  rememberMe?: boolean;
}
```

From apps/api/src/auth/dto/register.dto.ts:
```typescript
class RegisterDto {
  username: string; // 3-32, [a-z0-9._-]
  email: string;    // valid email
  password: string; // 8-128, must include letter + number
}
```

From apps/web/src/lib/api/http.ts:
```typescript
export async function apiRequest<T>(path: string, options?: RequestOptions): Promise<ApiResult<T>>;
```

From apps/web/src/lib/auth/session-store.ts:
```typescript
export function persistSessionToken(token: string): void;
export function persistSessionToStorage(user: SessionPayload["user"]): void;
```

From apps/web/src/providers/app-provider.tsx:
```typescript
interface AppContextValue {
  user: SessionUser | null;
  loaded: boolean;
  setUser: (user: SessionUser | null) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed local-auth frontend contracts, validators, and API wrappers</name>
  <files>
    apps/web/src/features/auth/types.ts
    apps/web/src/features/auth/validation.ts
    apps/web/src/features/auth/api.ts
    apps/web/src/features/auth/__tests__/auth-local-api.test.ts
  </files>
  <behavior>
    - Test 1: login payload builder accepts username OR email identifier and optional rememberMe flag, returning backend-compatible field names.
    - Test 2: register validator enforces D-API constraints from backend DTO (username pattern/length, password letter+number, email format).
    - Test 3: API wrapper converts common backend error payloads (duplicate username/email, invalid credentials, network) into stable user-facing messages.
  </behavior>
  <action>
Implement interface-first local auth module for AUTH-01 and AUTH-02.

Details:
- Create `features/auth/types.ts` request/response contracts matching backend payloads.
- Create `features/auth/validation.ts` pure validation helpers mirroring backend rules per DTO constraints.
- Create `features/auth/api.ts` wrappers using `apiRequest` for:
  - `POST /auth/login` with `{ username, password, rememberMe? }`
  - `POST /auth/register` with `{ username, email, password }`
  - shared `extractAuthErrorMessage` mapper for inline form errors.
- Add focused tests covering payload mapping, validator behavior, and error normalization.
- Do not introduce new dependencies; use existing fetch/http + vitest setup.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/features/auth/__tests__/auth-local-api.test.ts && npm run check-types</automated>
  </verify>
  <done>
Local auth feature contracts and API wrappers exist, match backend endpoints, and have passing tests proving validation/error mapping behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build login and registration pages with session-store/AppContext integration</name>
  <files>
    apps/web/app/auth/login/page.tsx
    apps/web/app/auth/register/page.tsx
    apps/web/src/features/auth/__tests__/auth-local-forms.dom.test.jsx
    apps/web/app/page.tsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: submitting invalid login/register forms blocks network call and shows inline validation feedback.
    - Test 2: successful local login persists token/user, calls `setUser`, and routes user toward dashboard.
    - Test 3: successful registration auto-signs in user, while duplicate username/email displays actionable error message.
    - Test 4: login page still exposes Google sign-in fallback alongside local auth flow.
  </behavior>
  <action>
Deliver full AUTH-01/AUTH-02 UI behavior per roadmap requirements.

Details:
- Upgrade `app/auth/login/page.tsx` from Google-only flow to dual-option auth screen:
  - local login form (`username/email`, `password`, `rememberMe`)
  - existing Google CTA preserved as fallback.
- Create `app/auth/register/page.tsx` with username/email/password fields and client-side validation using Task 1 helpers.
- On successful local auth:
  - persist token via `persistSessionToken`
  - persist user via `persistSessionToStorage`
  - update app state via `setUser`
  - route users to `/dashboard`.
- Add clear inline and submit-level error states for invalid credentials, duplicate identity, and network failures.
- Update home CTA copy/linking to support local auth entry (keep existing behavior for signed-in users).
- Extend `globals.css` auth styles for responsive login/register layouts consistent with current storefront visual language.
- Add DOM tests for validation + success/error flows (`auth-local-forms.dom.test.jsx`) using existing testing-library/vitest tooling.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/features/auth/__tests__/auth-local-forms.dom.test.jsx && npm run lint && npm run check-types</automated>
  </verify>
  <done>
Users can register and login locally from responsive UI forms with robust validation/error handling, and successful auth state is immediately reflected across the app session context.
  </done>
</task>

</tasks>

<verification>
- Local login with username/email and password succeeds and redirects to dashboard with active session.
- Local registration enforces client-side rules, surfaces duplicate identity errors clearly, and signs user in on success.
- Existing Google sign-in path remains available on login page as fallback.
</verification>

<success_criteria>
AUTH-01 and AUTH-02 are complete with production-ready local auth frontend flows, automated tests passing, and no regressions to existing session bootstrap behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/35-frontend-local-authentication/35-01-SUMMARY.md`
</output>
