---
phase: 33-cross-device-progression-continuity-and-conflict-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/reader/types.ts
  - apps/api/src/reader/reader-personal.controller.ts
  - apps/api/src/reader/reader.service.ts
  - apps/api/src/reader/__tests__/reader-progression-sync.spec.ts
autonomous: true
requirements: [SYNC-03, SYNC-04]
must_haves:
  truths:
    - "Authenticated reader receives deterministic progression outcome when two sessions update nearby progress values"
    - "Sync response includes enough metadata for clients to explain whether server accepted or overrode incoming progress"
    - "Latest server progression remains queryable for cross-device resume"
  artifacts:
    - path: "apps/api/src/reader/types.ts"
      provides: "Conflict-safe progression sync input/output contracts"
      exports: ["ReaderChapterOpenInput", "ReaderChapterOpenResult"]
    - path: "apps/api/src/reader/reader.service.ts"
      provides: "Deterministic merge policy implementation for chapter-open sync"
      exports: ["syncChapterOpen"]
    - path: "apps/api/src/reader/__tests__/reader-progression-sync.spec.ts"
      provides: "Merge/conflict policy verification coverage"
  key_links:
    - from: "apps/api/src/reader/reader-personal.controller.ts"
      to: "apps/api/src/reader/reader.service.ts"
      via: "POST /reader/me/chapter-opens request/response contract"
      pattern: "syncChapterOpen"
    - from: "apps/api/src/reader/reader.service.ts"
      to: "readingHistory"
      via: "deterministic merge write-path"
      pattern: "readingHistory\.(findFirst|create|update)"
---

<objective>
Implement backend deterministic progression merge policy and metadata-rich sync contract for multi-session chapter progress updates.

Purpose: Close SYNC-04 and backend half of SYNC-03 by making conflict handling explicit, deterministic, and client-consumable.
Output: Extended sync contracts, service merge logic, and tests proving policy behavior.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-01-SUMMARY.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-02-SUMMARY.md
@apps/api/src/reader/types.ts
@apps/api/src/reader/reader-personal.controller.ts
@apps/api/src/reader/reader.service.ts
@apps/api/src/reader/__tests__/reader-progression-sync.spec.ts

<interfaces>
From apps/api/src/reader/types.ts:
```typescript
export interface ReaderChapterOpenInput {
  chapterId: number;
  novelId?: number;
  progressPercent?: number;
}

export interface ReaderChapterOpenResult {
  chapterId: number;
  novelId: number;
  firstOpen: boolean;
  progressPercent: number;
  lastReadAt: Date;
}
```

From apps/api/src/reader/reader.service.ts:
```typescript
async syncChapterOpen(userId: number, input: ReaderChapterOpenInput): Promise<ReaderChapterOpenResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend sync contracts and RED tests for deterministic merge metadata</name>
  <files>
    apps/api/src/reader/types.ts
    apps/api/src/reader/__tests__/reader-progression-sync.spec.ts
  </files>
  <behavior>
    - Test 1: Sync response exposes merge metadata (`appliedPolicy`, `conflictDetected`, `serverAcceptedProgress`, `effectiveProgressPercent`, timestamps).
    - Test 2: When incoming update is stale relative to stored sync timestamp, service returns deterministic server-wins result without regressing progress.
    - Test 3: Fresh incoming update from another session applies and returns updated authoritative progression metadata.
  </behavior>
  <action>
Expand chapter-open sync DTOs with client-provided sync timestamp and response metadata describing merge decisions. Add RED tests that encode deterministic last-write policy using a stable comparison key (incoming `clientUpdatedAt` vs stored `lastReadAt`) and verify no non-deterministic outcomes.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-progression-sync.spec.ts</automated>
  </verify>
  <done>
    Contracts and failing tests precisely define deterministic merge behavior and required response metadata.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic merge policy in sync endpoint/service</name>
  <files>
    apps/api/src/reader/reader-personal.controller.ts
    apps/api/src/reader/reader.service.ts
  </files>
  <behavior>
    - Test 1: Stale update does not overwrite newer server progression and returns server-authoritative metadata.
    - Test 2: Newer update from second session advances persisted progression deterministically.
    - Test 3: Existing idempotent chapter-open analytics behavior remains intact for first-open view increments.
  </behavior>
  <action>
Implement deterministic merge in `syncChapterOpen` using timestamp-based last-write policy: apply update only when incoming sync timestamp is newer than persisted `lastReadAt`, otherwise keep server state and mark conflict metadata. Preserve phase-30 idempotent first-open counter behavior and expose policy metadata through controller response.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-progression-sync.spec.ts src/reader/__tests__/reader-personal.spec.ts && npm run check-types</automated>
  </verify>
  <done>
    Backend sync endpoint enforces deterministic merge policy and emits metadata required for cross-device reconciliation.
  </done>
</task>

</tasks>

<verification>
- Deterministic merge policy prevents stale-session overwrite regressions.
- Sync responses include conflict/decision metadata for client reconciliation.
- API tests and typecheck pass.
</verification>

<success_criteria>
- SYNC-04 is satisfied via deterministic conflict-safe merge behavior.
- Backend contract needed for SYNC-03 resume reconciliation is available.
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-device-progression-continuity-and-conflict-safety/33-01-SUMMARY.md`
</output>
