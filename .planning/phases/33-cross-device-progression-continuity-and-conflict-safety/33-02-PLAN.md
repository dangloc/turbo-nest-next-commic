---
phase: 33-cross-device-progression-continuity-and-conflict-safety
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/api.ts
  - apps/web/src/features/reader/reader.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [SYNC-03, SYNC-04]
must_haves:
  truths:
    - "Authenticated reader opening the same novel on another session resumes from latest server-authoritative progression"
    - "Reader UI communicates when server kept existing progression versus accepted incoming update"
    - "Rapid multi-session updates do not cause local resume jumps to stale values"
  artifacts:
    - path: "apps/web/src/features/reader/types.ts"
      provides: "Progression sync metadata contracts for frontend"
      exports: ["ReaderChapterOpenInput", "ReaderChapterOpenResult"]
    - path: "apps/web/src/features/reader/api.ts"
      provides: "Sync API wrapper carrying conflict-resolution timestamp fields"
      exports: ["syncReaderChapterOpen"]
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Cross-device resume reconciliation logic and UX messaging"
    - path: "apps/web/app/globals.css"
      provides: "Resume sync status styles"
  key_links:
    - from: "apps/web/src/features/reader/api.ts"
      to: "/reader/me/chapter-opens"
      via: "metadata-aware sync payloads"
      pattern: "syncReaderChapterOpen"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/reader/api.ts"
      via: "chapter init sync + resume reconciliation"
      pattern: "fetchReadingHistory|syncReaderChapterOpen"
---

<objective>
Integrate backend deterministic progression metadata into chapter-reader reconciliation so cross-device resume stays authoritative and understandable.

Purpose: Close SYNC-03 and complete UI-facing SYNC-04 by reconciling local/session progression against server outcomes.
Output: Frontend sync contracts, metadata-aware API calls, resume reconciliation logic, and user-facing sync status messaging.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-02-SUMMARY.md
@.planning/phases/33-cross-device-progression-continuity-and-conflict-safety/33-01-PLAN.md
@apps/web/src/features/reader/types.ts
@apps/web/src/features/reader/api.ts
@apps/web/src/features/reader/reader.tsx
@apps/web/app/globals.css

<interfaces>
From apps/web/src/features/reader/api.ts:
```typescript
export async function fetchReadingHistory(token?: string, novelId?: number)
export async function syncReaderChapterOpen(input: ReaderChapterOpenInput, token?: string)
```

From apps/web/src/features/reader/reader.tsx:
```typescript
const historyResult = await fetchReadingHistory(token, chapterResult.data.novelId);
const syncResult = await syncReaderChapterOpen({ ... }, token);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend frontend sync contracts and API bridge for merge metadata</name>
  <files>
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/api.ts
  </files>
  <behavior>
    - Test 1: Sync request sends client timestamp metadata required by backend deterministic policy.
    - Test 2: Sync response type exposes merge decision metadata used by chapter reader reconciliation.
    - Test 3: Existing reader API calls remain backward compatible with previous phase flows.
  </behavior>
  <action>
Update frontend reader sync types and API wrapper to carry client update timestamp and parse backend merge metadata fields without breaking existing chapter-open call sites.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Frontend contracts and API wrapper can fully consume deterministic sync metadata from plan 33-01 backend.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cross-device resume reconciliation and sync-status UX</name>
  <files>
    apps/web/src/features/reader/reader.tsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: Opening same chapter in second session applies server-authoritative latest progression.
    - Test 2: When backend reports stale incoming update, UI preserves server progression and displays a deterministic sync status message.
    - Test 3: Rapid chapter switches do not surface stale local progression after metadata reconciliation.
  </behavior>
  <action>
Refactor chapter initialization flow to reconcile reading history and sync response metadata in deterministic order (history baseline -> sync call -> effective progression update). Add lightweight status messaging in the reader card to indicate when server accepted versus rejected stale local progression updates. Keep purchase and typography features unchanged.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types && npm run lint -- --no-warn-ignored src/features/reader/types.ts src/features/reader/api.ts src/features/reader/reader.tsx app/globals.css</automated>
  </verify>
  <done>
    Cross-device resume is server-authoritative, conflict-safe, and clearly communicated in reader UX.
  </done>
</task>

</tasks>

<verification>
- Chapter resume behavior is deterministic across multi-session updates.
- Sync status messaging explains authoritative progression outcome.
- Web typecheck and targeted lint pass.
</verification>

<success_criteria>
- SYNC-03 is met via reliable cross-device resume continuity.
- SYNC-04 is met end-to-end with deterministic conflict handling and user-visible reconciliation feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-device-progression-continuity-and-conflict-safety/33-02-SUMMARY.md`
</output>
