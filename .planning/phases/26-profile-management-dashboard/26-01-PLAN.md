---
phase: 26-profile-management-dashboard
plan: 01
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/__tests__/auth.controller.spec.ts
  - apps/web/src/features/profile/types.ts
  - apps/web/src/features/profile/api.ts
  - apps/web/src/features/dashboard/api.ts
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [PROF-01, PROF-02]
must_haves:
  truths:
    - "Signed-in users can open the dashboard profile section and see persisted nickname/avatar profile fields"
    - "Users can submit profile updates and immediately see saved values reflected in dashboard session UI"
    - "Users can review identity and session details (id, email, role, session context) relevant to account management"
  artifacts:
    - path: "apps/api/src/auth/auth.controller.ts"
      provides: "authenticated profile read/update endpoints with session-token parsing"
      exports: ["me", "getProfile", "updateProfile"]
    - path: "apps/web/src/features/profile/api.ts"
      provides: "profile fetch/update API wrappers using existing Bearer token conventions"
      exports: ["fetchProfile", "updateProfile"]
    - path: "apps/web/src/features/dashboard/dashboard.tsx"
      provides: "profile section UI with edit form, validation feedback, and session detail panel"
      min_lines: 280
  key_links:
    - from: "apps/web/src/features/profile/api.ts"
      to: "/auth/profile"
      via: "apiRequest GET/PATCH"
      pattern: "auth/profile"
    - from: "apps/web/src/features/dashboard/dashboard.tsx"
      to: "apps/web/src/features/profile/api.ts"
      via: "profile section load/save handlers"
      pattern: "fetchProfile|updateProfile"
    - from: "apps/web/src/features/dashboard/dashboard.tsx"
      to: "apps/web/src/lib/auth/session-store.ts"
      via: "post-save session user sync"
      pattern: "persistSessionToStorage|setUser"
---

<objective>
Deliver profile management inside dashboard with backend-backed read/update contracts and clear identity/session visibility.

Purpose: satisfy PROF-01 and PROF-02 while preserving the phase-24 dashboard session guard architecture.
Output: authenticated profile API endpoints, typed web profile module, and dashboard profile edit/identity panel UX.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-frontend-foundation-auth-integration/20-01-SUMMARY.md
@.planning/phases/24-dashboard-foundation/24-01-SUMMARY.md
@apps/api/src/auth/auth.controller.ts
@apps/api/src/auth/__tests__/auth.controller.spec.ts
@apps/web/src/lib/auth/api.ts
@apps/web/src/lib/auth/session-store.ts
@apps/web/src/features/dashboard/types.ts
@apps/web/src/features/dashboard/api.ts
@apps/web/src/features/dashboard/dashboard.tsx

<interfaces>
From apps/api/src/auth/auth.controller.ts:
```typescript
@Get("me")
async me(@Req() req: Request)

@Post("logout")
logout(@Res({ passthrough: true }) res: Response)
```

From apps/web/src/lib/api/types.ts:
```typescript
export interface SessionUser {
  id: number;
  email: string;
  nickname?: string | null;
  role: "USER" | "AUTHOR" | "ADMIN";
}
```

From apps/web/src/features/dashboard/api.ts:
```typescript
export async function bootstrapDashboardSession(
  currentUser: SessionUser | null,
): Promise<DashboardBootstrapResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add authenticated profile read/update contracts with validation</name>
  <files>
    apps/api/src/auth/auth.controller.ts
    apps/api/src/auth/__tests__/auth.controller.spec.ts
  </files>
  <behavior>
    - Test 1: `GET /auth/profile` returns profile payload for valid session and rejects missing/invalid session.
    - Test 2: `PATCH /auth/profile` updates nickname/avatar metadata with validation (trim, bounds, nullable avatar) and returns persisted values.
    - Test 3: Existing `GET /auth/me` and `POST /auth/logout` behavior remains stable.
  </behavior>
  <action>
Implement PROF-01 and PROF-02 backend profile contracts by extending existing auth controller session-token flow (no new auth framework).

Details:
- Add authenticated profile read endpoint (`GET /auth/profile`) returning editable fields plus identity/session metadata needed by dashboard panel.
- Add authenticated profile update endpoint (`PATCH /auth/profile`) for nickname/avatar metadata with explicit validation and clear error messages.
- Keep `GET /auth/me` contract backward-compatible for existing dashboard bootstrap logic.
- Extend auth controller tests to cover new profile read/update paths and guard against regressions in current auth endpoints.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/auth/__tests__/auth.controller.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Profile API contracts are stable, validated, and usable by dashboard profile UI without breaking existing auth session bootstrap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard profile section with edit form and identity/session panel</name>
  <files>
    apps/web/src/features/profile/types.ts
    apps/web/src/features/profile/api.ts
    apps/web/src/features/dashboard/api.ts
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement PROF-01 and PROF-02 in dashboard profile section using existing dashboard query navigation pattern.

Details:
- Create a feature-local profile module (`types.ts`, `api.ts`) with typed fetch/update wrappers for `/auth/profile` using session token conventions from phase 20.
- Render `section=profile` UI in dashboard with editable nickname/avatar fields, inline validation feedback, submit/loading/success/error states, and retry flow.
- Add account identity/session details panel sourced from backend profile payload (id, email, role, session context timestamps/flags where available).
- After successful profile update, synchronize AppContext/session-store user snapshot so dashboard greeting reflects new nickname immediately.
- Keep wallet and purchases sections behavior unchanged (no regressions to phase 25 work).
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Dashboard profile section supports persisted profile editing and displays accurate identity/session details with clear validation and update feedback.
  </done>
</task>

</tasks>

<verification>
- Signed-in user can open `/dashboard?section=profile` and view current nickname/avatar and identity/session details.
- Profile save updates persisted backend values and reflects in dashboard greeting/session state without full-page reload.
- Validation errors are explicit and recoverable (no silent failures).
</verification>

<success_criteria>
PROF-01 and PROF-02 are complete with backend-backed profile management and dashboard-native identity/session visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/26-profile-management-dashboard/26-01-SUMMARY.md`
</output>
