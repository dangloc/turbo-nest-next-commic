---
phase: 16-reader-content-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/reader/reader.module.ts
  - apps/api/src/reader/reader-discovery.controller.ts
  - apps/api/src/reader/reader-chapter.controller.ts
  - apps/api/src/reader/reader-personal.controller.ts
  - apps/api/src/reader/reader.service.ts
  - apps/api/src/reader/types.ts
  - apps/api/src/reader/__tests__/reader-discovery.spec.ts
  - apps/api/src/reader/__tests__/reader-chapter.spec.ts
  - apps/api/src/reader/__tests__/reader-personal.spec.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05, READ-06]
must_haves:
  truths:
    - "Public clients can browse novels with pagination/sorting/filtering"
    - "Reading a chapter increments chapter and parent novel view counters safely"
    - "Authenticated users can create/list/remove bookmarks"
    - "Authenticated users can upsert and retrieve reading progress"
  artifacts:
    - path: "apps/api/src/reader/reader-discovery.controller.ts"
      provides: "Public novel discovery API surface"
      min_lines: 60
    - path: "apps/api/src/reader/reader-chapter.controller.ts"
      provides: "Chapter read endpoint and view analytics trigger"
      min_lines: 50
    - path: "apps/api/src/reader/reader-personal.controller.ts"
      provides: "Bookmark and reading history authenticated APIs"
      min_lines: 90
    - path: "apps/api/src/reader/reader.service.ts"
      provides: "Prisma-backed business logic and transaction-safe updates"
      min_lines: 180
  key_links:
    - from: "apps/api/src/reader/reader-chapter.controller.ts"
      to: "apps/api/src/reader/reader.service.ts"
      via: "readChapter() triggers transactional counter increment"
      pattern: "readChapter"
    - from: "apps/api/src/reader/reader-personal.controller.ts"
      to: "apps/api/src/auth/guards/roles.guard.ts"
      via: "authenticated endpoints protected by roles guard"
      pattern: "@UseGuards"
    - from: "apps/api/src/reader/reader.service.ts"
      to: "prisma.chapter/prisma.novel/prisma.bookmark/prisma.readingHistory"
      via: "single service orchestration"
      pattern: "prisma\\.(chapter|novel|bookmark|readingHistory)"
---

<objective>
Implement Phase 16 reader delivery APIs so frontend clients can discover novels, read chapters with analytics updates, and manage bookmark/history UX.

Purpose: provide stable public and authenticated reader endpoints before social interaction rollout.
Output: Reader module with tested discovery, chapter analytics, and personal UX APIs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.5-ROADMAP.md

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Novel {
  id          Int
  title       String
  viewCount   BigInt
  updatedAt   DateTime
  terms       Term[]
}

model Chapter {
  id          Int
  novelId     Int
  title       String
  postContent String
  viewCount   BigInt
}

model Bookmark {
  userId    Int
  novelId   Int
  chapterId Int?
}

model ReadingHistory {
  userId          Int
  novelId         Int
  chapterId       Int?
  progressPercent Int
  lastReadAt      DateTime
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build public novel discovery API with pagination/sort/filter</name>
  <files>
    apps/api/src/reader/types.ts
    apps/api/src/reader/reader.service.ts
    apps/api/src/reader/reader-discovery.controller.ts
    apps/api/src/reader/__tests__/reader-discovery.spec.ts
  </files>
  <behavior>
    - Test 1: GET /reader/novels returns paginated payload with page/limit/total/items.
    - Test 2: sortBy=viewCount|updatedAt|createdAt and sortDir=asc|desc map to deterministic Prisma orderBy.
    - Test 3: category/tag/status filters constrain result set without breaking pagination metadata.
  </behavior>
  <action>
Create reader discovery contracts and implement public GET endpoint for novel listing.

Implementation details:
- Query params: `page`, `limit`, `sortBy`, `sortDir`, `category`, `tag`, `status`.
- Validate defaults and bounds (`limit` max 50).
- Build Prisma where/orderBy from query safely.
- Return response shape:
  `{ page, limit, total, totalPages, items: [...] }`.
- Keep endpoint public (no auth guard).
- Include `viewCount`, `updatedAt`, taxonomy fields needed by web/mobile listing cards.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/reader/__tests__/reader-discovery.spec.ts</automated>
  </verify>
  <done>
Novel discovery endpoint supports required pagination, sorting, and filtering behavior with passing tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chapter read endpoint with transactional analytics increment</name>
  <files>
    apps/api/src/reader/reader.service.ts
    apps/api/src/reader/reader-chapter.controller.ts
    apps/api/src/reader/__tests__/reader-chapter.spec.ts
  </files>
  <behavior>
    - Test 1: GET /reader/chapters/:id returns chapter payload by id.
    - Test 2: successful read atomically increments chapter.viewCount and parent novel.viewCount once per request.
    - Test 3: invalid chapter id returns not-found response without counter writes.
  </behavior>
  <action>
Add chapter read endpoint and service logic.

Implementation details:
- Endpoint: `GET /reader/chapters/:id`.
- Use Prisma transaction to:
  1. fetch chapter + parent novel id,
  2. increment chapter `viewCount`,
  3. increment novel `viewCount`,
  4. return chapter content payload.
- Preserve read consistency under concurrent requests (single transaction path).
- Return minimal payload fields for reader screen: chapter id/title/postContent/novelId.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/reader/__tests__/reader-chapter.spec.ts</automated>
  </verify>
  <done>
Chapter read API returns content and safely updates analytics counters for chapter and novel.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add authenticated bookmark and reading-history APIs</name>
  <files>
    apps/api/src/reader/reader.module.ts
    apps/api/src/reader/reader.service.ts
    apps/api/src/reader/reader-personal.controller.ts
    apps/api/src/reader/__tests__/reader-personal.spec.ts
    apps/api/src/app.module.ts
  </files>
  <behavior>
    - Test 1: authenticated user can list/add/remove bookmarks only for own userId.
    - Test 2: reading history upsert stores progressPercent and lastReadAt for (user, novel, chapter scope).
    - Test 3: reading history retrieval returns latest persisted resume position for authenticated user.
    - Test 4: unauthenticated access to personal endpoints is rejected.
  </behavior>
  <action>
Implement personal UX endpoints under reader module and wire into AppModule.

Implementation details:
- Routes:
  - `GET /reader/me/bookmarks`
  - `POST /reader/me/bookmarks`
  - `DELETE /reader/me/bookmarks/:bookmarkId`
  - `PUT /reader/me/reading-history`
  - `GET /reader/me/reading-history`
- Protect with existing auth guard/role guard pattern for signed-in users.
- Enforce ownership in service layer (queries scoped by request user id).
- Use Prisma upsert for reading history writes.
- Return frontend-friendly structures for resume cards/bookmark lists.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/reader/__tests__/reader-personal.spec.ts && npm test --workspace=api -- --runInBand && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Reader personal APIs are authenticated, ownership-safe, and regression-tested with full suite/typecheck passing.
  </done>
</task>

</tasks>

<verification>
- Discovery endpoint exposes stable paginated and filterable novel listing.
- Chapter read path updates both chapter and novel analytics counters safely.
- Bookmark/history endpoints support real resume/bookmark UX and block unauthenticated access.
- Full API regression and typecheck stay green.
</verification>

<success_criteria>
- READ-01..READ-06 are implemented and covered by automated tests.
- APIs are frontend-consumable with deterministic query/response contracts.
- No regressions introduced to auth/cms/etl flows.
</success_criteria>

<output>
After completion, create .planning/phases/16-reader-content-apis/16-01-SUMMARY.md.
</output>
