---
phase: 06-content-schema-id-preservation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/api/src/etl/source-mysql-loaders.ts
  - apps/api/src/etl/prisma-repositories.ts
  - apps/api/src/etl/etl-runner.ts
  - apps/api/src/etl/index.ts
  - apps/api/src/etl/__tests__/content-source-loaders.spec.ts
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
must_haves:
  truths:
    - "The MySQL source loader can extract novels and chapters directly from wp_posts without parsing Word/Text files."
    - "The loader can preserve the wp_postmeta chapter-to-novel relationship exactly by carrying the chapter ID and parent novel ID through the pipeline."
    - "The ETL wiring exposes content migration contracts that later phases can use to upsert exact source IDs into PostgreSQL."
  artifacts:
    - path: "apps/api/src/etl/source-mysql-loaders.ts"
      provides: "Novel/chapter/wp_postmeta extraction helpers for strict DB-to-DB content migration"
      min_lines: 260
    - path: "apps/api/src/etl/prisma-repositories.ts"
      provides: "Content repository adapters for exact-ID upserts"
      min_lines: 150
    - path: "apps/api/src/etl/etl-runner.ts"
      provides: "ETL dependency surface extended for content loaders and writers"
      min_lines: 140
    - path: "apps/api/src/etl/__tests__/content-source-loaders.spec.ts"
      provides: "Loader and contract tests that prove exact-ID extraction and relationship mapping"
      min_lines: 60
  key_links:
    - from: "apps/api/src/etl/source-mysql-loaders.ts"
      to: "wp_posts / wp_postmeta"
      via: "strict content extraction queries"
      pattern: "post_type = 'truyen_chu'|post_type = 'chuong_truyen'|meta_key = 'chuong_with_truyen'"
    - from: "apps/api/src/etl/prisma-repositories.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "exact-ID content upserts"
      pattern: "Novel|Chapter|upsert|createMany"
    - from: "apps/api/src/etl/etl-runner.ts"
      to: "apps/api/src/etl/source-mysql-loaders.ts"
      via: "content loader dependency surface"
      pattern: "loadNovels|loadChapters|loadChapterRelations"
---

<objective>
Wire the content source loaders and repository adapters needed for strict DB-to-DB migration of novels and chapters.

Purpose: prepare the import pipeline to move exact MySQL IDs and chapter-to-novel relationships through typed loader/repository contracts without introducing rich-content parsing risk.
Output: source loader helpers, exact-ID write adapters, and ETL wiring ready for the actual content import phase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-schema-id-preservation/06-CONTEXT.md
@.planning/phases/06-content-schema-id-preservation/06-01-PLAN.md
@apps/api/prisma/schema.prisma
@apps/api/src/etl/types.ts
@apps/api/src/etl/source-mysql-loaders.ts
@apps/api/src/etl/prisma-repositories.ts
@apps/api/src/etl/etl-runner.ts

<interfaces>
From apps/api/src/etl/source-mysql-loaders.ts:
```typescript
export function createSourceLoaders(client: MySqlClient) {
  return {
    async loadUsers(): Promise<SourceUserRow[]> { ... },
    async loadPurchasedChapters(): Promise<SourceChapterRow[]> { ... },
  };
}
```

From apps/api/src/etl/prisma-repositories.ts:
```typescript
export function createPrismaRepositories(prisma: PrismaClient) {
  return {
    userRepo: { upsert(...) { ... } },
    purchasedChapterRepo: { createMany(...) { ... } },
  };
}
```

From apps/api/src/etl/etl-runner.ts:
```typescript
export type EtlRunnerDeps = {
  loadUsers: () => Promise<SourceUserRow[]>;
  loadPurchasedChapters: () => Promise<SourceChapterRow[]>;
  purchasedChapterRepo: { createMany: (...) => Promise<number> };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict wp_posts and wp_postmeta source loaders for content</name>
  <files>apps/api/src/etl/source-mysql-loaders.ts, apps/api/src/etl/__tests__/content-source-loaders.spec.ts</files>
  <behavior>
    - Novel extraction filters wp_posts to post_type = 'truyen_chu' and returns the original post ID and title.
    - Chapter extraction filters wp_posts to post_type = 'chuong_truyen' and returns the original post ID, title, and raw post_content.
    - Relationship extraction reads wp_postmeta meta_key = 'chuong_with_truyen' and carries chapter ID plus parent novel ID through the source layer unchanged.
  </behavior>
  <action>
Extend the MySQL loader layer with strict content-specific queries for novels, chapters, and chapter-to-novel relations. Do not parse or transform chapter content beyond copying the raw post_content field. Keep the extraction logic deterministic and explicit so later import work can trust the source rows as a direct representation of the WordPress database state. Add focused tests that prove the loader filters and field mappings exactly match the DB-only contract.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand content-source-loaders.spec.ts</automated>
  </verify>
  <done>The source loader layer can read the content migration domain directly from WordPress tables with exact IDs and raw content intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add content repository and ETL wiring surfaces for exact-ID upserts</name>
  <files>apps/api/src/etl/prisma-repositories.ts, apps/api/src/etl/etl-runner.ts, apps/api/src/etl/index.ts</files>
  <behavior>
    - Content repository adapters accept exact source IDs and preserve them on create or upsert.
    - ETL dependency surfaces expose the content loaders and writers needed for phase 7 without reinterpreting source data.
    - The wiring remains backward-compatible with the existing migration pipeline and does not alter v1.0 behavior.
  </behavior>
  <action>
Add repository methods and ETL dependency interfaces for content migration so later phases can perform exact-ID upserts into the target content tables. Keep the wiring narrow: define the content loader and repository entry points now, but avoid full migration orchestration beyond what is needed to lock the interface contract. Preserve the existing ETL behavior for users, wallets, VIP, transactions, and purchased chapters unchanged.
  </action>
  <verify>
    <automated>cd apps/api && npm run check-types:api && npm test -- --runInBand content-source-loaders.spec.ts</automated>
  </verify>
  <done>The ETL layer exposes content import entry points and exact-ID repository contracts without disturbing the shipped migration paths.</done>
</task>

</tasks>

<verification>
The phase is complete when the strict content loaders, repository surfaces, and ETL wiring all compile, and the loader tests prove that exact IDs and raw content are preserved from WordPress source rows.
</verification>

<success_criteria>
- `apps/api/src/etl/source-mysql-loaders.ts` can read novels, chapters, and chapter relationships from the correct WordPress tables.
- `apps/api/src/etl/prisma-repositories.ts` and `apps/api/src/etl/etl-runner.ts` expose content import contracts without changing shipped migration behavior.
- `apps/api/src/etl/__tests__/content-source-loaders.spec.ts` proves the DB-only extraction contract.
- The content migration pipeline is scaffolded for the actual import phase with no heavy Word/Text parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-schema-id-preservation/06-02-SUMMARY.md`.
</output>
