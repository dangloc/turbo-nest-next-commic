---
phase: 06-content-schema-id-preservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/etl/types.ts
  - apps/api/src/etl/__tests__/content-schema.spec.ts
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
must_haves:
  truths:
    - "The target schema exposes first-class Novel and Chapter tables that preserve original MySQL IDs without remapping."
    - "A chapter row carries its parent novel ID as a real relationship field, so source relationships stay exact inside PostgreSQL."
    - "ETL contracts expose typed source and write shapes for novels and chapters, so later phases can wire loaders without guessing field meaning."
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Novel and Chapter Prisma models with exact source-ID preservation strategy"
      min_lines: 120
    - path: "apps/api/src/etl/types.ts"
      provides: "Content migration source/write contracts and relation types"
      min_lines: 120
    - path: "apps/api/src/etl/__tests__/content-schema.spec.ts"
      provides: "Focused contract assertions for exact-ID and relation semantics"
      min_lines: 40
  key_links:
    - from: "apps/api/prisma/schema.prisma"
      to: "apps/api/src/etl/types.ts"
      via: "shared content row contracts and exact ID fields"
      pattern: "model Novel|model Chapter"
    - from: "apps/api/src/etl/types.ts"
      to: "apps/api/src/etl/__tests__/content-schema.spec.ts"
      via: "source/write contract assertions"
      pattern: "SourceNovelRow|SourceChapterRow|ChapterRelation"
    - from: "apps/api/prisma/schema.prisma"
      to: "original MySQL identifiers"
      via: "ID-preserving PK/unique strategy"
      pattern: "id Int @id|novelId Int|chapterId Int"
---

<objective>
Establish the strict content domain schema and contracts for novels and chapters.

Purpose: lock the exact-ID model before any import wiring so later phases can migrate content without remapping identities or reinterpreting source relationships.
Output: Prisma content models plus TypeScript contracts that make the novel/chapter boundary explicit and testable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-content-schema-id-preservation/06-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/etl/types.ts
@apps/api/src/etl/source-mysql-loaders.ts
@apps/api/src/etl/prisma-repositories.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model User {
  id Int @id
  email String @unique
  ...
}

model PurchasedChapter {
  id Int @id @default(autoincrement())
  userId Int
  novelId Int
  chapterId Int
  @@unique([userId, chapterId])
}
```

From apps/api/src/etl/types.ts:
```typescript
export type SourceUserRow = {
  id: number;
  email: string;
  password: string;
};

export type SourceChapterRow = {
  userId: number;
  purchasesRaw: string;
};

export type MigrationStats = {
  chaptersInserted: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact-ID Novel and Chapter Prisma models</name>
  <files>apps/api/prisma/schema.prisma</files>
  <behavior>
    - Novel rows preserve the original MySQL post ID rather than generating a new surrogate ID.
    - Chapter rows preserve the original MySQL post ID and keep a direct relation back to the parent novel ID.
    - The schema exposes a stable location for raw chapter content so later phases can import the text without changing identity semantics.
  </behavior>
  <action>
Add the content migration tables to Prisma with explicit ID preservation. Model novels and chapters so the source MySQL IDs can be inserted directly into PostgreSQL and so chapter-to-novel linkage remains a native relation rather than an inferred join. Keep the schema aligned with the repository's existing Prisma conventions and avoid introducing extra parsing or transformation fields that belong to later phases.
  </action>
  <verify>
    <automated>cd apps/api && npm run prisma:validate && npm run check-types</automated>
  </verify>
  <done>The Prisma schema has content tables that preserve source IDs and support chapter-to-novel relationships without remapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ETL contracts for novel and chapter content rows</name>
  <files>apps/api/src/etl/types.ts, apps/api/src/etl/__tests__/content-schema.spec.ts</files>
  <behavior>
    - Content source rows distinguish novels from chapters and expose source IDs, titles, raw content, and parent IDs explicitly.
    - Write contracts make it impossible for later phases to lose the original MySQL ID when constructing destination rows.
    - Tests lock the shape of the new contracts so phase 7 can depend on them without rediscovering source semantics.
  </behavior>
  <action>
Add the novel/chapter source and write types needed for DB-to-DB content migration. Keep the contracts narrow and explicit: include only the source IDs, titles, raw chapter content, and parent-relationship fields required by the phase boundary. Add focused tests that assert the content contracts are present and that their field names clearly express the exact-ID preservation strategy.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand content-schema.spec.ts</automated>
  </verify>
  <done>The ETL contract layer exposes exact-ID content row shapes and is covered by focused tests.</done>
</task>

</tasks>

<verification>
The phase is complete when the content schema exists, the ETL contract layer exposes novel/chapter row shapes with exact source IDs, and tests prove the schema/contracts are explicit enough for the loader phase.
</verification>

<success_criteria>
- `apps/api/prisma/schema.prisma` defines content tables that preserve original MySQL IDs.
- `apps/api/src/etl/types.ts` includes explicit content row contracts for novels and chapters.
- `apps/api/src/etl/__tests__/content-schema.spec.ts` exists and verifies the exact-ID contract shape.
- The phase leaves no ambiguity about how chapters map to novels in later import work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-schema-id-preservation/06-01-SUMMARY.md`.
</output>
