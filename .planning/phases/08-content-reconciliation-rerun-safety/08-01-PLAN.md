---
phase: 08-content-reconciliation-rerun-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/etl/content-reconciliation-report.ts
  - apps/api/src/etl/verify-content-reconciliation.ts
  - apps/api/src/etl/__tests__/content-reconciliation-report.spec.ts
  - apps/api/src/etl/__tests__/verify-content-reconciliation.spec.ts
  - apps/api/package.json
autonomous: true
requirements: [CONTENT-05, CONTENT-06]

must_haves:
  truths:
    - "Reconciliation output shows source vs target novel and chapter totals with explicit deltas"
    - "Reconciliation output detects chapter-to-novel integrity problems (missing parent novel, relation mismatch)"
    - "Reconciliation output includes raw content fidelity checks (hash or exact string mismatches)"
    - "Rerun verification shows no duplicate growth in novels/chapters between run snapshots"
    - "A single CLI command can produce and persist the reconciliation report"
  artifacts:
    - path: "apps/api/src/etl/content-reconciliation-report.ts"
      provides: "Typed content reconciliation report builders and summary formatting"
      exports:
        - "buildContentReconciliationReport"
        - "buildRerunSafetyReport"
        - "persistContentReconciliationReport"
        - "formatContentReconciliationSummary"
    - path: "apps/api/src/etl/verify-content-reconciliation.ts"
      provides: "CLI verification workflow querying MySQL source and Prisma target"
      exports:
        - "verifyContentReconciliation"
    - path: "apps/api/src/etl/__tests__/content-reconciliation-report.spec.ts"
      provides: "Deterministic report math + mismatch detection tests"
    - path: "apps/api/src/etl/__tests__/verify-content-reconciliation.spec.ts"
      provides: "Verification orchestration tests with mocked source/target datasets"
  key_links:
    - from: "verify-content-reconciliation.ts"
      to: "source-mysql-loaders.ts"
      via: "loadNovels/loadChapters or equivalent SQL queries"
      pattern: "loadNovels|loadChapters|wp_posts"
    - from: "verify-content-reconciliation.ts"
      to: "prisma-client.ts"
      via: "target reads for novel/chapter datasets"
      pattern: "prisma\.novel|prisma\.chapter"
    - from: "content-reconciliation-report.ts"
      to: "apps/api/tmp/content-reconciliation.json"
      via: "persistContentReconciliationReport"
      pattern: "writeFile"
    - from: "rerun safety snapshot"
      to: "CONTENT-05"
      via: "count deltas across two ETL runs"
      pattern: "before.*after|delta"
---

<objective>
Implement content reconciliation and rerun-safety verification for Phase 8 so we can prove novels and chapters migrated correctly, preserve relationships/content fidelity, and remain safe to rerun without duplication.

Purpose: Close the v1.1 milestone by producing machine-verifiable evidence for CONTENT-05 and CONTENT-06.
Output: Content reconciliation report module, verification CLI, and tests proving deterministic behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-content-db-to-db-import/07-02-SUMMARY.md
@apps/api/src/etl/verify-wallet-reconciliation.ts
@apps/api/src/etl/reconciliation-report.ts
@apps/api/src/etl/verify-purchased-chapters.ts
@apps/api/src/etl/purchased-chapter-verification-report.ts

<interfaces>
From apps/api/src/etl/types.ts:
```typescript
export type SourceNovelRow = {
  id: number;
  title: string;
  postContent: string;
  createdAt: Date;
};

export type SourceChapterContentRow = {
  id: number;
  novelId: number;
  title: string;
  postContent: string;
  createdAt: Date;
};

export type ChapterRelation = {
  chapterId: number;
  novelId: number;
};
```

From apps/api/src/etl/config.ts runtime paths:
```typescript
runtime: {
  walletReconciliationPath: string;
  purchasedChapterReconciliationPath: string;
  socialMappingVerificationPath: string;
}
```

From apps/api/src/etl/reconciliation-report.ts pattern:
```typescript
buildXReport(input): Report
persistXReport(outputPath, report): Promise<void>
formatXSummary(report): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Content Reconciliation Report Module</name>
  <files>
    apps/api/src/etl/content-reconciliation-report.ts,
    apps/api/src/etl/__tests__/content-reconciliation-report.spec.ts
  </files>
  <behavior>
    - Test 1: Given source/target counts with exact match, report deltas are zero and mismatches are empty.
    - Test 2: Given missing target chapters or novels, report includes deterministic mismatch rows sorted by ID.
    - Test 3: Given chapter->novel relation mismatches, report flags orphan chapters and relationMismatchCount.
    - Test 4: Given raw content differences, report increments contentMismatchCount and includes sample IDs.
    - Test 5: Rerun-safety helper computes duplicateGrowth=false when afterRun counts equal beforeRun counts.
  </behavior>
  <action>
    Create `content-reconciliation-report.ts` with the same shape/pattern used by wallet and purchased-chapter reports:

    1. Define report types:
       - `ContentReconciliationMismatch` (entityType, id, reason, sourceValue, targetValue)
       - `ContentReconciliationReport` with:
         - `totals`: sourceNovels, targetNovels, sourceChapters, targetChapters
         - `deltas`: novelDelta, chapterDelta
         - `integrity`: orphanChapters, relationMismatches, contentMismatches
         - `mismatches`: detailed rows
       - `RerunSafetyReport` with before/after counts and duplicateGrowth flags

    2. Implement pure functions:
       - `buildContentReconciliationReport(input)`
       - `buildRerunSafetyReport(input)`
       - `formatContentReconciliationSummary(report)`
       - `persistContentReconciliationReport(outputPath, payload)`

    3. Reuse robust path normalization logic from `reconciliation-report.ts` so commands work from root or `apps/api` cwd.

    4. Keep report deterministic:
       - sort mismatch rows by entityType then ID
       - deterministic summary order

    5. Write tests first in `content-reconciliation-report.spec.ts` (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npm test -- --runInBand src/etl/__tests__/content-reconciliation-report.spec.ts</automated>
  </verify>
  <done>
    - Report module exports all four required functions.
    - Tests cover count parity, relationship integrity, content fidelity, and rerun-safety calculations.
    - Report output is deterministic and JSON-serializable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Content Reconciliation CLI Verification</name>
  <files>
    apps/api/src/etl/verify-content-reconciliation.ts,
    apps/api/src/etl/__tests__/verify-content-reconciliation.spec.ts,
    apps/api/package.json
  </files>
  <behavior>
    - Test 1: CLI composes source and target datasets and persists a report file path from config.
    - Test 2: CLI summary output includes novel/chapter deltas and integrity counters.
    - Test 3: CLI computes rerun safety using before/after snapshots and reports duplicateGrowth=false for stable rerun.
  </behavior>
  <action>
    Create `verify-content-reconciliation.ts` mirroring existing verify-* scripts:

    1. Load config via `loadEtlConfig`, connect mysql/prisma, ensure disconnect in finally.

    2. Build source snapshots:
       - novels from WordPress source (`truyen_chu`)
       - chapters from WordPress source (`chuong_truyen` + parent mapping)
       - include `postContent` and parent novelId for fidelity/integrity checks

    3. Build target snapshots:
       - Prisma `novel.findMany` and `chapter.findMany` selecting id, novelId, postContent

    4. Compute:
       - total deltas (source vs target)
       - relation integrity (chapter parent exists, parent ID match)
       - content fidelity (exact string compare by id for overlapping rows)

    5. Add rerun safety check:
       - capture counts before invoking ETL (`runEtlCli` import)
       - invoke ETL once
       - capture counts after ETL
       - build rerun report where duplicate growth must be false when rerun is idempotent
       - note: if ETL invocation is too expensive for default path, gate with env flag `ETL_VERIFY_RERUN=true`; when false, still emit `rerun: skipped` field.

    6. Persist combined payload to new config path (add runtime key if missing):
       - default `apps/api/tmp/content-reconciliation.json`

    7. Add npm script to `apps/api/package.json`:
       - `"etl:verify:content": "ts-node src/etl/verify-content-reconciliation.ts"`

    8. Add unit-style tests for verification orchestration using mocked datasets (no live DB).
  </action>
  <verify>
    <automated>npm test -- --runInBand src/etl/__tests__/verify-content-reconciliation.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
    - `etl:verify:content` script exists and runs.
    - CLI writes a content reconciliation JSON report.
    - Summary logs include deltas + integrity counters.
    - Rerun-safety section exists (executed or explicitly skipped with reason).
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-End Verification for Phase Exit</name>
  <files>
    apps/api/src/etl/verify-content-reconciliation.ts,
    apps/api/src/etl/content-reconciliation-report.ts,
    apps/api/src/etl/__tests__/content-reconciliation-report.spec.ts,
    apps/api/src/etl/__tests__/verify-content-reconciliation.spec.ts
  </files>
  <action>
    Run final verification sequence and resolve any defects:

    1. `npm run check-types --workspace=api`
    2. `npm test --workspace=api -- --runInBand src/etl/__tests__/content-reconciliation-report.spec.ts src/etl/__tests__/verify-content-reconciliation.spec.ts`
    3. `npm run etl:verify:content --workspace=api` (with available env)

    If command 3 cannot run in CI/local due missing env, verify by:
    - running command with mocked env in test
    - asserting generated report file path and payload shape

    Ensure completion criteria map directly to requirements:
    - CONTENT-05: rerun safety section indicates no duplicate growth when rerun executed.
    - CONTENT-06: reconciliation totals and integrity checks are present in output.
  </action>
  <verify>
    <automated>npm run check-types --workspace=api && npm test --workspace=api -- --runInBand src/etl/__tests__/content-reconciliation-report.spec.ts src/etl/__tests__/verify-content-reconciliation.spec.ts</automated>
  </verify>
  <done>
    - Type-check and targeted tests pass.
    - Content reconciliation output format is stable and usable for milestone audit.
    - Requirement coverage is explicit in code and summary.
  </done>
</task>

</tasks>

<verification>
1. Automated tests pass for new report and verification modules.
2. Running `etl:verify:content` emits summary plus JSON artifact path.
3. Report includes both reconciliation (counts/integrity/fidelity) and rerun-safety evidence.
4. No changes to migration behavior itself, only verification/reporting surfaces.
</verification>

<success_criteria>
- [ ] CONTENT-05 covered by rerun-safety evidence in reconciliation output.
- [ ] CONTENT-06 covered by source/target count and integrity/fidelity checks.
- [ ] `etl:verify:content` command available and documented via package script.
- [ ] Deterministic report builder tested and passing.
- [ ] Phase ready for `/gsd:verify-work 8` and milestone closure checks.
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-reconciliation-rerun-safety/08-01-SUMMARY.md`
</output>
