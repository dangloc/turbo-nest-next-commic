---
phase: 10-taxonomy-tags-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/*/migration.sql
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06]

must_haves:
  truths:
    - "WordPress terms are stored with original term IDs, name, slug, and taxonomy type"
    - "Novel-to-term many-to-many links exist and can represent categories, tags, and custom taxonomies"
    - "Existing 176 novels are linked to their taxonomy terms from WordPress source data"
    - "Taxonomy migration does not alter existing novel IDs, uploader ownership, or chapter relationships"
    - "Migration remains rerunnable without duplicating terms or novel-term links"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Taxonomy term model and Novel-Term many-to-many relation"
      contains: "model Term"
    - path: "apps/api/prisma/migrations/*/migration.sql"
      provides: "DDL for taxonomy tables and join table"
  key_links:
    - from: "wp_terms"
      to: "Term"
      via: "preserved term_id/name/slug/taxonomy mapping"
      pattern: "term_id|slug|taxonomy"
    - from: "wp_term_taxonomy"
      to: "Term"
      via: "term taxonomy type preservation"
      pattern: "taxonomy"
    - from: "wp_term_relationships"
      to: "Novel <-> Term"
      via: "object_id mapped to Novel.id"
      pattern: "object_id|term_taxonomy_id"
---

<objective>
Implement taxonomy and tag migration support by adding a preserved WordPress term model and mapping novels to terms through a many-to-many relationship.

Purpose: Preserve WordPress categories, tags, and custom taxonomies so novels can be classified without losing source term identity.
Output: Updated Prisma schema plus migration path for taxonomy storage and novel-term mapping.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/api/prisma/schema.prisma

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Novel {
  id            Int                @id
  uploaderId    Int                @default(1)
  uploader      User               @relation(fields: [uploaderId], references: [id], onDelete: Restrict)
  title         String
  postContent   String
  chapters      Chapter[]
  purchasedChps PurchasedChapter[]
}
```

Target contract for this phase:
```prisma
model Term {
  id        Int     @id
  name      String
  slug      String  @unique
  taxonomy  String
  novels    Novel[]
}

model Novel {
  ...
  terms Term[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Taxonomy Term Model and Novel Relation</name>
  <files>apps/api/prisma/schema.prisma</files>
  <behavior>
    - Test 1: Prisma schema validates with a preserved term model storing id, name, slug, and taxonomy.
    - Test 2: Novel model exposes a many-to-many relation to terms.
    - Test 3: Existing uploader/chapter relations remain unchanged.
  </behavior>
  <action>
    Update `schema.prisma` to add taxonomy support per TAX-01, TAX-02, and TAX-03:

    1. Create a `Term` model that stores:
       - `id Int @id`
       - `name String`
       - `slug String @unique`
       - `taxonomy String`

    2. Add a many-to-many relation between `Novel` and `Term`:
       - `Novel.terms Term[]`
       - `Term.novels Novel[]`

    3. Preserve existing `User`, `Novel`, `Chapter`, and `PurchasedChapter` relations.

    4. Run Prisma formatter/validator to ensure schema is valid before migration work.
  </action>
  <verify>
    <automated>npm run prisma:format --workspace=api && npm run prisma:validate --workspace=api</automated>
  </verify>
  <done>
    - Prisma schema compiles and validates.
    - Term model and Novel-Term many-to-many relation exist.
    - Existing content and uploader relations remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and Apply Taxonomy Migration</name>
  <files>apps/api/prisma/migrations/*/migration.sql</files>
  <action>
    Generate and apply migration for TAX-04 through TAX-06 using the updated schema and existing Postgres database:

    1. Create migration in `apps/api` for taxonomy storage.
    2. Ensure generated SQL includes:
       - a preserved taxonomy term table keyed by original term ID
       - the taxonomy type/slug/name fields
       - a join table that links novels and terms
       - indexes/constraints that support rerunnable imports and lookup by slug/taxonomy
    3. Apply the migration to local PostgreSQL and regenerate Prisma client.
    4. Prepare for ETL reconstruction of wp_term_relationships by preserving original IDs and join keys.
  </action>
  <verify>
    <automated>cd apps/api && npx prisma migrate dev --name add_taxonomy_terms && npm run prisma:generate</automated>
  </verify>
  <done>
    - Migration folder exists with taxonomy DDL.
    - Migration applies successfully.
    - Prisma client regenerates successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Compatibility Verification Against Existing Flow</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/*/migration.sql</files>
  <action>
    Run lightweight compatibility checks to ensure taxonomy changes do not break the current API/ETL baseline:

    1. Type check API package.
    2. Run the current UAT-backed uploader ownership/reconciliation tests.
    3. Confirm the novel/content model still compiles with the new Term relation.

    This phase should only add taxonomy structure and preserve the current UGC/content behavior.
  </action>
  <verify>
    <automated>npm run check-types --workspace=api && npm test --workspace=api -- --runInBand src/etl/__tests__/verify-content-reconciliation.spec.ts</automated>
  </verify>
  <done>
    - API type-check remains green.
    - Existing reconciliation tests still pass.
    - Taxonomy schema update is ready for ETL implementation in future steps.
  </done>
</task>

</tasks>

<verification>
1. Prisma schema validates and formats successfully.
2. Migration SQL includes taxonomy term storage plus Novel-Term join support.
3. Migration applies without breaking the existing migrated novel dataset.
4. API type-check/tests pass after taxonomy schema update.
</verification>

<success_criteria>
- [ ] TAX-01 complete: Term model stores original WordPress term identity.
- [ ] TAX-02 complete: Term model supports categories, tags, and custom taxonomies.
- [ ] TAX-03 complete: Novel-Term many-to-many relation exists.
- [ ] TAX-04 complete: existing novels can be linked to preserved taxonomy terms.
- [ ] TAX-05 complete: migration preserves current novel/uploader/chapter data.
- [ ] TAX-06 complete: migration can be rerun without duplicating term links.
</success_criteria>

<output>
After completion, create `.planning/phases/10-taxonomy-tags-migration/10-01-SUMMARY.md`
</output>
