---
phase: 25-wallet-top-up-and-chapter-purchases
plan: 01
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - apps/api/src/finance/types.ts
  - apps/api/src/finance/payment.controller.ts
  - apps/api/src/finance/finance.service.ts
  - apps/api/src/finance/__tests__/payment.spec.ts
  - apps/web/src/features/finance/types.ts
  - apps/web/src/features/finance/api.ts
  - apps/web/src/features/dashboard/api.ts
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [WAL-01, WAL-02]
must_haves:
  truths:
    - "Signed-in users can initiate and verify wallet top-ups from the dashboard wallet section"
    - "Users can see current wallet balances and recent transaction history with clear status labels"
    - "Wallet dashboard state persists correctly after refresh via backend-backed data"
  artifacts:
    - path: "apps/api/src/finance/payment.controller.ts"
      provides: "wallet summary and top-up verification endpoints"
      exports: ["initiateTopUp", "verifyTopUp", "getWalletSummary"]
    - path: "apps/api/src/finance/finance.service.ts"
      provides: "wallet summary query + top-up settlement orchestration"
      min_lines: 200
    - path: "apps/web/src/features/finance/api.ts"
      provides: "frontend finance API wrappers for top-up + wallet summary"
      exports: ["initiateTopUp", "verifyTopUp", "fetchWalletSummary"]
    - path: "apps/web/src/features/dashboard/dashboard.tsx"
      provides: "wallet section UI with top-up form and transaction list"
      min_lines: 180
  key_links:
    - from: "apps/web/src/features/finance/api.ts"
      to: "/finance/payments/initiate"
      via: "apiRequest POST"
      pattern: "finance/payments/initiate"
    - from: "apps/web/src/features/finance/api.ts"
      to: "/finance/wallet/summary"
      via: "apiRequest GET"
      pattern: "finance/wallet/summary"
    - from: "apps/web/src/features/dashboard/dashboard.tsx"
      to: "apps/web/src/features/finance/api.ts"
      via: "wallet section data/actions"
      pattern: "fetchWalletSummary|initiateTopUp|verifyTopUp"
---

<objective>
Deliver wallet top-up + wallet status dashboard experience: backend summary APIs, finance frontend contracts, and wallet section UI inside dashboard.

Purpose: satisfy WAL-01/WAL-02 and establish the finance contract layer that purchase UX builds on.
Output: end-to-end wallet flow with top-up initiation/verification and labeled transaction history.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-deposit-purchase-apis/18-01-SUMMARY.md
@.planning/phases/24-dashboard-foundation/24-01-SUMMARY.md
@apps/api/src/finance/payment.controller.ts
@apps/api/src/finance/finance.service.ts
@apps/api/src/finance/types.ts
@apps/api/src/finance/__tests__/payment.spec.ts
@apps/web/src/features/dashboard/api.ts
@apps/web/src/features/dashboard/dashboard.tsx

<interfaces>
From apps/api/src/finance/payment.controller.ts:
```typescript
@Post("initiate")
initiateTopUp(req, body: InitiateTopUpInput)

@Post("verify")
verifyTopUp(req, body: VerifyTopUpInput)
```

From apps/api/src/finance/types.ts:
```typescript
export interface InitiateTopUpInput {
  amount: number;
  provider: "VNPAY" | "MOMO";
  reference: string;
  returnUrl?: string;
  callbackUrl?: string;
}

export interface VerifyTopUpInput {
  provider: "VNPAY" | "MOMO";
  reference: string;
  providerTransactionId: string;
  amount: number;
  success: boolean;
}
```

From apps/web/src/features/dashboard/api.ts:
```typescript
export async function bootstrapDashboardSession(
  currentUser: SessionUser | null,
): Promise<DashboardBootstrapResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend finance backend for wallet summary + labeled transaction history</name>
  <files>
    apps/api/src/finance/types.ts
    apps/api/src/finance/payment.controller.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/__tests__/payment.spec.ts
  </files>
  <behavior>
    - Test 1: wallet summary endpoint returns deposited/earned/total balances with deterministic defaults for users without wallet rows.
    - Test 2: summary includes recent transactions with normalized status labels and descending timestamps.
    - Test 3: existing initiate/verify top-up tests still pass with no regression.
  </behavior>
  <action>
Implement WAL-02 backend support by adding authenticated wallet summary read API (balance + recent transactions).

Details:
- Keep existing `POST /finance/payments/initiate` and `POST /finance/payments/verify` contracts stable.
- Add `GET /finance/wallet/summary` (or equivalent finance wallet route) returning balances and recent transaction entries with explicit labels used by UI.
- Extend payment tests to cover the new read endpoint shape and preserve existing top-up idempotency behavior.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/payment.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Backend exposes authenticated wallet summary payload required by dashboard wallet module and all payment tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard wallet module with top-up + wallet history UX</name>
  <files>
    apps/web/src/features/finance/types.ts
    apps/web/src/features/finance/api.ts
    apps/web/src/features/dashboard/api.ts
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement WAL-01/WAL-02 dashboard wallet UX using the new backend summary endpoint.

Details:
- Create a dedicated frontend finance module (`types.ts` + `api.ts`) wrapping initiate/verify/summary calls with existing auth token conventions.
- Wire wallet section in dashboard to show current balance cards, recent transaction list with status labels, and top-up initiate/verify interaction states.
- Keep provider flow deterministic for this phase (mockable provider reference + verification step from backend contract), without introducing external payment SDK UI.
- Preserve dashboard query-section navigation pattern from phase 24.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Wallet dashboard section supports top-up initiation + verification and renders backend-backed wallet balances/history labels.
  </done>
</task>

</tasks>

<verification>
- Wallet section in dashboard loads balances and transaction statuses for signed-in users.
- Top-up initiate + verify flow updates wallet view without requiring full page reload.
- Signed-out users remain blocked by existing dashboard auth guard.
</verification>

<success_criteria>
WAL-01 and WAL-02 are complete with integrated backend/frontend wallet flows and reusable finance API contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/25-wallet-top-up-and-chapter-purchases/25-01-SUMMARY.md`
</output>
