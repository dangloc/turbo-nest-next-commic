---
phase: 25-wallet-top-up-and-chapter-purchases
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - apps/web/src/features/finance/types.ts
  - apps/web/src/features/finance/api.ts
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/api.ts
  - apps/web/src/features/reader/reader.tsx
  - apps/web/src/features/dashboard/api.ts
  - apps/web/src/features/dashboard/dashboard.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [BUY-01, BUY-02]
must_haves:
  truths:
    - "Users can purchase locked chapters with explicit confirmation and insufficient-balance messaging"
    - "Successful chapter purchase unlocks chapter access state immediately in reader navigation"
    - "Dashboard purchases section reflects recent purchase activity for signed-in users"
  artifacts:
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "chapter purchase CTA/confirm flow and unlocked-state rendering"
      min_lines: 260
    - path: "apps/web/src/features/reader/api.ts"
      provides: "purchase chapter client calls and unlock status helpers"
      exports: ["purchaseChapter", "fetchReadingHistory", "fetchChapterById"]
    - path: "apps/web/src/features/finance/api.ts"
      provides: "purchase history/summary client methods used by dashboard and reader"
      exports: ["purchaseChapter", "fetchWalletSummary"]
    - path: "apps/web/src/features/dashboard/dashboard.tsx"
      provides: "purchases section summary/list rendering"
      min_lines: 220
  key_links:
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/finance/api.ts"
      via: "purchase action handler"
      pattern: "purchaseChapter"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/reader/api.ts"
      via: "chapter data refresh + navigation"
      pattern: "fetchChapterById|buildChapterHref"
    - from: "apps/web/src/features/dashboard/dashboard.tsx"
      to: "apps/web/src/features/finance/api.ts"
      via: "purchases section summary"
      pattern: "purchase|transaction"
---

<objective>
Ship chapter purchase UX integrated into reader flow and dashboard purchase visibility, leveraging phase-25 wallet contracts.

Purpose: satisfy BUY-01/BUY-02 with immediate unlock behavior and consistent monetization feedback.
Output: chapter purchase actions, unlock state refresh, and purchases dashboard summary.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-reader-experience-ui/22-01-SUMMARY.md
@.planning/phases/24-dashboard-foundation/24-01-SUMMARY.md
@.planning/phases/25-wallet-top-up-and-chapter-purchases/25-01-PLAN.md
@apps/web/src/features/reader/types.ts
@apps/web/src/features/reader/api.ts
@apps/web/src/features/reader/reader.tsx
@apps/web/src/features/dashboard/api.ts
@apps/web/src/features/dashboard/dashboard.tsx

<interfaces>
From apps/api/src/finance/purchase.controller.ts:
```typescript
@Post("chapters/:chapterId")
purchaseChapter(req, chapterId, body: { novelId: number; price: number })
```

From apps/api/src/finance/finance.service.ts response contract:
```typescript
{
  status: "purchased" | "already_owned";
  chapterId: number;
  novelId: number;
  purchasedChapterId: number | null;
  transactionId?: number;
  depositedBalance?: number;
}
```

From apps/web/src/features/reader/types.ts:
```typescript
export interface ReaderChapter {
  id: number;
  novelId: number;
  title: string;
  postContent: string;
  viewCount: number | string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add purchase contracts and reader-facing purchase client actions</name>
  <files>
    apps/web/src/features/finance/types.ts
    apps/web/src/features/finance/api.ts
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/api.ts
  </files>
  <behavior>
    - Test 1: purchase client returns deterministic status union (`purchased`, `already_owned`, `insufficient_balance`).
    - Test 2: purchase payload enforces chapter/novel/price requirements before request dispatch.
    - Test 3: reader API exposes helpers required to refresh unlocked-state after a successful purchase.
  </behavior>
  <action>
Define and implement BUY-01 data contracts on the frontend.

Details:
- Extend finance module with purchase request/response typing and purchase API wrapper.
- Map backend insufficient-balance error response into explicit UI-friendly purchase status.
- Keep reader API focused on orchestrating chapter refresh and unlock checks after purchase completion.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Purchase client contract is typed and ready for reader UI integration with explicit status handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate chapter purchase flow and immediate unlock state in reader + dashboard</name>
  <files>
    apps/web/src/features/reader/reader.tsx
    apps/web/src/features/dashboard/api.ts
    apps/web/src/features/dashboard/dashboard.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement BUY-01/BUY-02 UI behavior.

Details:
- Add purchase CTA/confirmation flow in chapter reader when content is locked, including explicit insufficient-balance path and link back to wallet section.
- On successful purchase, refresh chapter/readability state immediately and enable navigation without reload.
- Surface recent purchase information in dashboard purchases section so users can confirm unlock actions from account hub.
- Keep visual language consistent with existing reader/dashboard style system.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Reader purchase UX handles confirmation/success/failure paths, and purchased access state updates immediately in navigation and dashboard summary.
  </done>
</task>

</tasks>

<verification>
- Locked chapter flow shows purchase CTA and clear insufficient-balance handling.
- Successful purchase updates unlocked chapter access state in same reader session.
- Purchases section in dashboard reflects recent purchase outcomes.
</verification>

<success_criteria>
BUY-01 and BUY-02 are complete with robust reader purchase UX and immediate unlock propagation.
</success_criteria>

<output>
After completion, create `.planning/phases/25-wallet-top-up-and-chapter-purchases/25-02-SUMMARY.md`
</output>
