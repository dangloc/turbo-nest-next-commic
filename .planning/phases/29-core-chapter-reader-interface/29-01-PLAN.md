---
phase: 29-core-chapter-reader-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/reader/types.ts
  - apps/api/src/reader/reader-chapter.controller.ts
  - apps/api/src/reader/reader.service.ts
  - apps/api/src/reader/__tests__/reader-chapter-navigation.spec.ts
autonomous: true
requirements: [READER-02]

must_haves:
  truths:
    - "Reader chapter payload includes navigation boundaries and table-of-contents entries"
    - "First and last chapter boundaries are explicit so UI can disable invalid navigation"
    - "Chapter navigation metadata remains available without authentication"
  artifacts:
    - path: "apps/api/src/reader/types.ts"
      provides: "Chapter navigation response contracts"
      exports: ["ReaderChapterNavigation", "ReaderChapterContext"]
    - path: "apps/api/src/reader/reader.service.ts"
      provides: "Chapter context query with prev/next and TOC"
      exports: ["getChapterContext()"]
    - path: "apps/api/src/reader/reader-chapter.controller.ts"
      provides: "Public chapter context endpoint"
      exports: ["GET /reader/chapters/:id/context"]
    - path: "apps/api/src/reader/__tests__/reader-chapter-navigation.spec.ts"
      provides: "Boundary and TOC behavior coverage"
  key_links:
    - from: "apps/api/src/reader/reader-chapter.controller.ts"
      to: "apps/api/src/reader/reader.service.ts"
      via: "controller delegation"
      pattern: "this\\.readerService\\.getChapterContext"
    - from: "apps/api/src/reader/reader.service.ts"
      to: "prisma.chapter|prisma.novel"
      via: "chapter sequence + novel ownership queries"
      pattern: "prisma\\.(chapter|novel)"
---

<objective>
Add reader navigation backend contracts so chapter pages can render deterministic previous/next controls and table-of-contents jumps.

Purpose: Provide the minimum public data interface required for READER-02 without adding new dependencies.
Output: Public chapter context endpoint + typed contracts + automated tests for boundary and TOC behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reader-content-apis/16-01-SUMMARY.md
@apps/api/src/reader/types.ts
@apps/api/src/reader/reader-chapter.controller.ts
@apps/api/src/reader/reader.service.ts

<interfaces>
From apps/api/src/reader/reader-chapter.controller.ts:
```typescript
@Get('chapters/:id')
async readChapter(@Param('id', ParseIntPipe) id: number, @Query('novelId') novelId?: string)
```

From apps/api/src/reader/reader.service.ts:
```typescript
async readChapter(chapterId: number, novelId?: number)
```

From apps/api/src/reader/types.ts:
```typescript
export interface ReaderChapter {
  id: number;
  novelId: number;
  title: string;
  postContent: string;
  viewCount: number | string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define chapter navigation contracts and RED tests</name>
  <files>
    apps/api/src/reader/types.ts
    apps/api/src/reader/__tests__/reader-chapter-navigation.spec.ts
  </files>
  <behavior>
    - Test 1: Context response includes current chapter, previousChapterId, nextChapterId, and TOC list.
    - Test 2: First chapter returns previousChapterId as null.
    - Test 3: Last chapter returns nextChapterId as null.
  </behavior>
  <action>
Add explicit reader types for chapter context and TOC item contracts, then create RED tests for boundary behavior and deterministic chapter ordering. Keep contract public and auth-free to preserve reader browsing parity.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-chapter-navigation.spec.ts</automated>
  </verify>
  <done>
    New contracts compile and RED tests capture boundary plus TOC expectations for chapter navigation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement public chapter context endpoint for prev/next and TOC</name>
  <files>
    apps/api/src/reader/reader-chapter.controller.ts
    apps/api/src/reader/reader.service.ts
  </files>
  <behavior>
    - Test 1: GET /reader/chapters/:id/context returns context for a valid chapter.
    - Test 2: Unknown chapter returns not-found behavior.
    - Test 3: Prev/next ids and TOC ordering are deterministic by chapter id ascending.
  </behavior>
  <action>
Add `GET /reader/chapters/:id/context` and implement service query logic that resolves chapter novel ownership, computes previous and next chapter ids, and returns a lightweight TOC list for jumps. Preserve existing `readChapter` behavior and avoid introducing auth guards for this public reader data.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand src/reader/__tests__/reader-chapter-navigation.spec.ts src/reader/__tests__/reader-chapter.spec.ts && npm run check-types</automated>
  </verify>
  <done>
    Chapter context endpoint is public, returns deterministic navigation metadata, and tests/typecheck pass.
  </done>
</task>

</tasks>

<verification>
- Chapter context endpoint returns required fields for prev/next and TOC.
- Boundary behavior is test-covered for first and last chapters.
- Existing chapter read endpoint behavior remains green.
</verification>

<success_criteria>
- READER-02 backend contract is available for frontend chapter navigation and table-of-contents rendering.
- No new dependencies or auth requirements were introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/29-core-chapter-reader-interface/29-01-SUMMARY.md`
</output>
