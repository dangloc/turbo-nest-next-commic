---
phase: 29-core-chapter-reader-interface
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/api.ts
  - apps/web/src/features/reader/reader.tsx
  - apps/web/app/reader/chapters/[chapterId]/page.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [READER-01, READER-02]

must_haves:
  truths:
    - "Reader sees immersive chapter surface optimized for long-form text"
    - "Reader can move to previous or next chapter with boundary-safe controls"
    - "Reader can jump to any chapter through a visible table-of-contents section"
  artifacts:
    - path: "apps/web/src/features/reader/types.ts"
      provides: "Frontend chapter-context contracts"
      exports: ["ReaderChapterContext", "ReaderChapterTocItem"]
    - path: "apps/web/src/features/reader/api.ts"
      provides: "Chapter context API wrapper"
      exports: ["fetchChapterContextById()"]
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Immersive chapter layout with TOC and boundary-safe navigation"
    - path: "apps/web/app/globals.css"
      provides: "Reader layout styling for long-form immersion"
  key_links:
    - from: "apps/web/src/features/reader/api.ts"
      to: "/reader/chapters/:id/context"
      via: "apiRequest wrapper"
      pattern: "reader/chapters/.*/context"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/reader/api.ts"
      via: "chapter context fetch in effect"
      pattern: "fetchChapterContextById"
---

<objective>
Deliver the Core Chapter Reader Interface with immersive reading layout, chapter boundary navigation, and table-of-contents jumps.

Purpose: Complete READER-01 and READER-02 with production-ready UI behavior tied to backend navigation contracts.
Output: Updated chapter reader view and route integration with deterministic previous/next/TOC behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-reader-experience-ui/22-01-SUMMARY.md
@.planning/phases/29-core-chapter-reader-interface/29-01-PLAN.md
@apps/web/src/features/reader/types.ts
@apps/web/src/features/reader/api.ts
@apps/web/src/features/reader/reader.tsx
@apps/web/app/reader/chapters/[chapterId]/page.tsx
@apps/web/app/globals.css

<interfaces>
From apps/web/src/features/reader/types.ts:
```typescript
export interface ReaderChapter {
  id: number;
  novelId: number;
  title: string;
  postContent: string;
  viewCount: number | string;
}

export function buildChapterHref(chapterId: number, novelId?: number): string;
```

From apps/web/src/features/reader/api.ts:
```typescript
export async function fetchChapterById(chapterId: number): Promise<ApiResult<ReaderChapter>>;
```

From apps/web/src/features/reader/reader.tsx:
```typescript
export function ChapterReaderView({ chapterId }: { chapterId: number })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend chapter-context contracts and API bridge</name>
  <files>
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/api.ts
  </files>
  <behavior>
    - Test 1: API wrapper resolves chapter context payload with prev/next and TOC fields.
    - Test 2: Invalid response path surfaces API error message consistently.
  </behavior>
  <action>
Extend reader frontend contracts with chapter context + TOC types and add a typed `fetchChapterContextById` API wrapper calling backend context endpoint from plan 29-01. Keep existing chapter fetch and purchase APIs unchanged.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Reader frontend can request and type-check chapter context data from backend without regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immersive chapter reader layout with TOC and boundary-safe nav</name>
  <files>
    apps/web/src/features/reader/reader.tsx
    apps/web/app/reader/chapters/[chapterId]/page.tsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: Previous button is disabled/hidden on first chapter boundary.
    - Test 2: Next button is disabled/hidden on last chapter boundary.
    - Test 3: TOC renders chapter links and supports chapter jump.
    - Test 4: Loading and missing context states render stable fallback messages.
  </behavior>
  <action>
Refactor `ChapterReaderView` to consume chapter context metadata and render an immersive long-form reading layout: readable content width, persistent chapter metadata header, boundary-safe previous/next controls, and a table-of-contents section for chapter jumps. Ensure invalid `chapterId` route fallback remains safe and user-friendly. Preserve existing purchase lock messaging and history save behavior.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types && npm run lint -- src/features/reader/reader.tsx src/features/reader/api.ts src/features/reader/types.ts app/reader/chapters/[chapterId]/page.tsx</automated>
  </verify>
  <done>
    Reader chapter UI supports immersive reading plus deterministic navigation/TOC behaviors with no type/lint regressions.
  </done>
</task>

</tasks>

<verification>
- Chapter reader route renders stable states: loading, missing chapter, first/last boundaries.
- TOC links and previous/next navigation share one source of truth from backend chapter context.
- Existing reader purchase and progress-save interactions still compile and render.
</verification>

<success_criteria>
- READER-01 is satisfied with an immersive, long-form chapter reading experience.
- READER-02 is satisfied with previous/next navigation and table-of-contents jumps including boundary safety.
</success_criteria>

<output>
After completion, create `.planning/phases/29-core-chapter-reader-interface/29-02-SUMMARY.md`
</output>
