---
phase: 22-reader-experience-ui
plan: 01
type: execute
wave: 1
depends_on: [20-01, 20-02, 21-01]
files_modified:
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/api.ts
  - apps/web/src/features/reader/reader.tsx
  - apps/web/src/features/discovery/discovery.tsx
  - apps/web/app/novels/[id]/page.tsx
  - apps/web/app/reader/chapters/[chapterId]/page.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [READ-01, READ-02, READ-03]
must_haves:
  truths:
    - "Readers can open a novel from the storefront, browse its chapter list, and enter a chapter reading view with visible navigation context"
    - "Opening a chapter loads the live chapter content from the backend and triggers the backend view-count increment path"
    - "Authenticated readers can resume reading progress and their history updates persist across revisits"
  artifacts:
    - path: "apps/web/src/features/reader/types.ts"
      provides: "Typed reader/novel/chapter/history contracts for chapter UX"
      min_lines: 60
    - path: "apps/web/src/features/reader/api.ts"
      provides: "Shared reader fetch helpers for novel detail, chapter content, and reading history"
      min_lines: 80
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Reusable novel detail and chapter reading UI shell"
      min_lines: 120
    - path: "apps/web/app/novels/[id]/page.tsx"
      provides: "Novel detail route with chapter navigation context"
      min_lines: 60
    - path: "apps/web/app/reader/chapters/[chapterId]/page.tsx"
      provides: "Chapter reading route that loads content and persists progress"
      min_lines: 80
  key_links:
    - from: "apps/web/src/features/reader/api.ts"
      to: "apps/api/src/reader/reader-chapter.controller.ts"
      via: "chapter content fetch"
      pattern: "reader/chapters/"
    - from: "apps/web/src/features/reader/api.ts"
      to: "apps/api/src/reader/reader-personal.controller.ts"
      via: "reading-history bootstrap and save"
      pattern: "reader/me/reading-history"
    - from: "apps/web/app/novels/[id]/page.tsx"
      to: "apps/api/src/novels/novels.controller.ts"
      via: "novel detail fetch for chapter navigation context"
      pattern: "novels/\:id|/novels/"
    - from: "apps/web/src/features/discovery/discovery.tsx"
      to: "apps/web/app/novels/[id]/page.tsx"
      via: "storefront novel card navigation"
      pattern: "/novels/"
---

<objective>
Deliver the core chapter reading experience for the storefront: a novel detail entry point, chapter navigation, live chapter rendering, and authenticated reading-history resume behavior.

Purpose: provide the primary reader experience needed before social interactions can attach to the chapter view.
Output: reader contracts, reader API helpers, reusable reader UI shell, novel detail route, chapter reading route, and supporting styles/navigation links.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-frontend-foundation-auth-integration/20-01-SUMMARY.md
@.planning/phases/20-frontend-foundation-auth-integration/20-02-SUMMARY.md
@.planning/phases/21-novel-discovery-ui/21-01-SUMMARY.md
@apps/api/src/novels/novels.controller.ts
@apps/api/src/reader/reader-chapter.controller.ts
@apps/api/src/reader/reader-personal.controller.ts
@apps/api/src/reader/reader.service.ts
@apps/api/prisma/schema.prisma
@apps/web/src/lib/auth/api.ts
@apps/web/src/lib/auth/session-store.ts
@apps/web/src/features/discovery/discovery.tsx

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Novel {
  id        Int      @id
  title     String
  postContent String
  viewCount BigInt   @default(0)
  chapters  Chapter[]
  terms     Term[]
}

model Chapter {
  id          Int      @id
  novelId     Int
  title       String
  postContent String
  viewCount   BigInt   @default(0)
}
```

From apps/api/src/novels/novels.controller.ts:
```typescript
@Get(':id')
async findOne(@Param('id', ParseIntPipe) id: number) {
  return this.prisma.novel.findUnique({ where: { id } });
}
```

From apps/api/src/reader/reader-chapter.controller.ts:
```typescript
@Get('chapters/:id')
async readChapter(@Param('id', ParseIntPipe) id: number) {
  return this.readerService.readChapter(id);
}
```

From apps/api/src/reader/reader-personal.controller.ts:
```typescript
@Get('reading-history')
async listReadingHistory(@Req() req: { user?: { id: number } }, @Query('novelId') novelId?: string)
```

From apps/web/src/lib/auth/session-store.ts:
```typescript
export function getSessionToken() {
  return loadSessionFromStorage().token;
}
```

From apps/web/src/lib/auth/api.ts:
```typescript
export async function fetchSession(token?: string): Promise<ApiResult<SessionPayload | { user: null }>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define reader contracts and shared data helpers for novels, chapters, and progress</name>
  <files>
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/api.ts
  </files>
  <behavior>
    - Test 1: novel-detail and chapter payload types preserve the backend field names used by the novel and reader controllers.
    - Test 2: history bootstrap/save helpers include the authenticated session token when present and degrade safely when it is absent.
    - Test 3: route helper functions produce stable novel/chapter URLs and normalizing a missing chapter id does not break the reading flow.
  </behavior>
  <action>
Create the reader data layer before building the UI shell.

Implementation details:
- Define frontend DTOs for novel detail, chapter detail, and reading-history payloads that match the existing API shapes.
- Add shared fetch helpers for `GET /novels/:id`, `GET /reader/chapters/:id`, `GET /reader/me/reading-history`, and `PUT /reader/me/reading-history` using the phase 20 HTTP client.
- Add URL helper functions for novel and chapter routes so discovery cards and reader pages use the same canonical paths.
- Keep the helpers pure and auth-aware; use the persisted session token from phase 20 rather than inventing a second auth store.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Reader contracts and API helpers exist and can be consumed by novel and chapter routes without ad hoc fetch logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build novel detail and chapter reading pages with history resume and discovery navigation</name>
  <files>
    apps/web/src/features/reader/reader.tsx
    apps/web/src/features/discovery/discovery.tsx
    apps/web/app/novels/[id]/page.tsx
    apps/web/app/reader/chapters/[chapterId]/page.tsx
    apps/web/app/globals.css
  </files>
  <action>
Implement the user-facing reader experience and connect it back to storefront discovery.

Implementation details:
- Add a novel detail page that fetches the novel record, renders chapter navigation, and provides a clear entry into the chapter reader.
- Add the chapter reading page that loads the live chapter payload, displays the chapter content, and saves reading progress/history for authenticated readers.
- Reuse the shared reader UI shell for chapter navigation, progress/resume affordances, loading states, and not-found/error states.
- Update storefront discovery cards so novel navigation lands on the new detail page instead of leaving the user at a dead end.
  </action>
  <verify>
    <automated>npm run lint --workspace=web && npm run check-types --workspace=web</automated>
  </verify>
  <done>
Readers can move from discovery to novel detail to chapter reading, and authenticated progress persists through the shared reader history API.
  </done>
</task>

</tasks>

<verification>
- Novel detail and chapter routes are reachable from the storefront discovery flow.
- Chapter opens trigger the backend chapter analytics path and save reading-history state for logged-in readers.
- Loading, empty, and error states are visible without breaking navigation or resume behavior.
</verification>

<success_criteria>
READ-01, READ-02, and READ-03 are complete with a working chapter reading experience that integrates discovery navigation, analytics, and persisted reader history.
</success_criteria>

<output>
After completion, create `.planning/phases/22-reader-experience-ui/22-01-SUMMARY.md`
</output>
