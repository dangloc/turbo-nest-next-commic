---
phase: 11-taxonomy-etl-backfill-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/etl/types.ts
  - apps/api/src/etl/source-mysql-loaders.ts
  - apps/api/src/etl/prisma-repositories.ts
  - apps/api/src/etl/etl-runner.ts
  - apps/api/src/etl/index.ts
  - apps/api/src/etl/config.ts
  - apps/api/src/etl/verify-taxonomy-reconciliation.ts
  - apps/api/src/etl/taxonomy-reconciliation-report.ts
  - apps/api/src/etl/__tests__/source-mysql-loaders.spec.ts
  - apps/api/src/etl/__tests__/etl-runner.spec.ts
  - apps/api/src/etl/__tests__/taxonomy-reconciliation-report.spec.ts
  - apps/api/src/etl/__tests__/verify-taxonomy-reconciliation.spec.ts
  - apps/api/package.json
autonomous: true
requirements: [TAX-04, TAX-05, TAX-06]

must_haves:
  truths:
    - "WordPress taxonomy source rows from wp_terms, wp_term_taxonomy, and wp_term_relationships are extracted into typed ETL inputs"
    - "Target terms table stores source-preserved term identity and taxonomy values without duplicate growth on rerun"
    - "Novel-term relationships are reconstructed in PostgreSQL using the implicit _NovelToTerm join table"
    - "Running taxonomy ETL twice does not create additional terms or duplicate novel-term links"
    - "A reconciliation command reports source/target totals and link deltas for auditability"
  artifacts:
    - path: "apps/api/src/etl/source-mysql-loaders.ts"
      provides: "Taxonomy source loaders for terms, term_taxonomy, and term_relationships"
      contains: "loadTerms|loadTermTaxonomyRows|loadTermRelationships"
    - path: "apps/api/src/etl/prisma-repositories.ts"
      provides: "Idempotent term upsert and novel-term link write operations"
      contains: "termRepo|novelTermRepo"
    - path: "apps/api/src/etl/etl-runner.ts"
      provides: "Taxonomy backfill orchestration in executeEtl"
      contains: "taxonomy migration step"
    - path: "apps/api/src/etl/verify-taxonomy-reconciliation.ts"
      provides: "CLI reconciliation check for taxonomy counts and link parity"
      exports: ["verifyTaxonomyReconciliation"]
    - path: "apps/api/src/etl/taxonomy-reconciliation-report.ts"
      provides: "Deterministic taxonomy reconciliation and rerun-safety report builders"
  key_links:
    - from: "wp_terms/wp_term_taxonomy"
      to: "Term"
      via: "typed source loaders + term upsert"
      pattern: "term_id|taxonomy|slug"
    - from: "wp_term_relationships.object_id"
      to: "Novel.id"
      via: "relationship mapper before join-table writes"
      pattern: "object_id"
    - from: "wp_term_relationships.term_taxonomy_id"
      to: "_NovelToTerm.B"
      via: "term_taxonomy join to preserved term ids"
      pattern: "term_taxonomy_id"
    - from: "verify-taxonomy-reconciliation.ts"
      to: "apps/api/tmp/taxonomy-reconciliation.json"
      via: "persistTaxonomyReconciliationReport"
      pattern: "writeFile"
---

<objective>
Implement taxonomy ETL backfill and verification so WordPress taxonomy source data is loaded into PostgreSQL terms and reconstructed novel-term links with rerun-safe semantics.

Purpose: Complete the migration behavior required for TAX-04 through TAX-06 after Phase 10 schema setup.
Output: Taxonomy ETL loaders + repository writes + runner wiring + reconciliation CLI/tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-taxonomy-tags-migration/10-01-SUMMARY.md
@apps/api/prisma/schema.prisma
@apps/api/src/etl/source-mysql-loaders.ts
@apps/api/src/etl/prisma-repositories.ts
@apps/api/src/etl/etl-runner.ts
@apps/api/src/etl/verify-content-reconciliation.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Term {
  id       Int     @id
  name     String
  slug     String
  taxonomy String
  novels   Novel[]

  @@unique([slug, taxonomy])
  @@map("terms")
}

model Novel {
  id    Int    @id
  terms Term[]
  @@map("novels")
}
```

From apps/api/src/etl/source-mysql-loaders.ts pattern:
```typescript
export function createSourceLoaders(client: MySqlClient) {
  return {
    async loadNovels(): Promise<SourceNovelRow[]> { ... },
    async loadChapters(): Promise<SourceChapterContentRow[]> { ... },
  };
}
```

From apps/api/src/etl/etl-runner.ts dependency contract:
```typescript
export type EtlRunnerDeps = {
  loadUsers: () => Promise<SourceUserRow[]>;
  loadNovels: () => Promise<SourceNovelRow[]>;
  novelRepo: { upsert: (...) => Promise<void> };
};
```

From apps/api/src/etl/prisma-repositories.ts idempotence pattern:
```typescript
await prisma.model.upsert({ where, update, create });
await prisma.model.createMany({ data, skipDuplicates: true });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Taxonomy Source Contracts and Extraction Loaders</name>
  <files>
    apps/api/src/etl/types.ts,
    apps/api/src/etl/source-mysql-loaders.ts,
    apps/api/src/etl/__tests__/source-mysql-loaders.spec.ts
  </files>
  <behavior>
    - Test 1: loadTerms extracts term_id/name/slug and taxonomy from wp_terms joined to wp_term_taxonomy.
    - Test 2: loadTermRelationships extracts object_id and term_taxonomy_id from wp_term_relationships.
    - Test 3: loaders normalize missing/null values deterministically and filter invalid IDs.
  </behavior>
  <action>
    Implement taxonomy extraction from the exact WordPress sources requested:

    1. Extend `types.ts` with taxonomy ETL row types, for example:
       - `SourceTermRow` (termId, name, slug, taxonomy)
       - `SourceTermRelationshipRow` (novelId/objectId, termTaxonomyId)
       - optional `SourceTermTaxonomyRow` if intermediate mapping is needed.

    2. Add source loader functions in `source-mysql-loaders.ts`:
       - `loadTerms()` using `wp_terms` + `wp_term_taxonomy` join.
       - `loadTermTaxonomyRows()` if needed for explicit mapping preservation.
       - `loadTermRelationships()` reading `wp_term_relationships`.

    3. Preserve deterministic normalization patterns already used by other loaders (`asNumber`, `asString`, fallback defaults) and filter non-positive IDs.

    4. Add or update loader tests to assert SQL mapping and row normalization behavior.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/source-mysql-loaders.spec.ts</automated>
  </verify>
  <done>
    - Source taxonomy loaders exist and return typed rows from wp_terms/wp_term_taxonomy/wp_term_relationships.
    - Invalid relationship rows (missing IDs) are excluded.
    - Tests validate extraction shape and normalization behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill Terms and Reconstruct Novel-Term Links in ETL Runner</name>
  <files>
    apps/api/src/etl/prisma-repositories.ts,
    apps/api/src/etl/etl-runner.ts,
    apps/api/src/etl/index.ts,
    apps/api/src/etl/__tests__/etl-runner.spec.ts
  </files>
  <behavior>
    - Test 1: taxonomy rows are upserted into Term preserving source term IDs and taxonomy values.
    - Test 2: relationships are inserted as Novel-Term links and skipped safely when duplicates already exist.
    - Test 3: rerunning executeEtl does not increase term count or join-table link count unexpectedly.
  </behavior>
  <action>
    Wire taxonomy migration into the existing ETL flow with explicit idempotence:

    1. Add taxonomy repos in `prisma-repositories.ts`:
       - `termRepo.upsert` keyed by `id` (source term_id preserved).
       - `novelTermRepo.createMany` (or equivalent) using `skipDuplicates: true` and mapping to Prisma many-to-many relation writes.
       - If Prisma many-to-many APIs are insufficient for bulk implicit join writes, use `prisma.$executeRaw`/`$executeRawUnsafe` with guarded INSERT ... ON CONFLICT DO NOTHING for `_NovelToTerm`.

    2. Extend `EtlRunnerDeps` and load phase in `etl-runner.ts`:
       - load taxonomy terms and relationships in parallel with other source loads.
       - execute taxonomy write stage after novels are available.
       - map relationship rows so `object_id -> Novel.id` and `term_taxonomy_id -> preserved Term.id`.

    3. Update `index.ts` wiring so source loaders and repos pass taxonomy dependencies into `executeEtl`.

    4. Update runner tests to assert taxonomy writes are called and duplicate-safe semantics are preserved on rerun.

    5. Keep existing user/content migration behavior unchanged (per TAX-05), adding only taxonomy stages.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/etl-runner.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
    - ETL runner writes terms and novel-term links.
    - Relationship mapping from wp_term_relationships is reconstructed into PostgreSQL join links.
    - Rerun semantics prevent duplicate term/link growth.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Taxonomy Reconciliation and Rerun-Safety Verification CLI</name>
  <files>
    apps/api/src/etl/config.ts,
    apps/api/src/etl/taxonomy-reconciliation-report.ts,
    apps/api/src/etl/verify-taxonomy-reconciliation.ts,
    apps/api/src/etl/__tests__/taxonomy-reconciliation-report.spec.ts,
    apps/api/src/etl/__tests__/verify-taxonomy-reconciliation.spec.ts,
    apps/api/package.json
  </files>
  <behavior>
    - Test 1: report builder computes source/target term totals, link totals, and mismatch deltas deterministically.
    - Test 2: verification CLI persists a taxonomy reconciliation payload to tmp path and prints summary counters.
    - Test 3: rerun-safety snapshot reports duplicate growth false/false when a second ETL run is stable.
  </behavior>
  <action>
    Implement explicit auditability for taxonomy backfill completion:

    1. Add runtime config path for taxonomy reconciliation output in `config.ts` and `types.ts`:
       - default `apps/api/tmp/taxonomy-reconciliation.json`.

    2. Create `taxonomy-reconciliation-report.ts` mirroring existing reconciliation modules with:
       - typed payload (source totals, target totals, deltas, missing links, orphan relationships)
       - deterministic sort for mismatches
       - rerun-safety helper using before/after counts.

    3. Create `verify-taxonomy-reconciliation.ts` that:
       - loads source taxonomy counts/rows via loaders,
       - reads target counts via Prisma (`term` and relation view),
       - computes parity/mismatch report,
       - optionally runs one ETL rerun when `ETL_VERIFY_RERUN=true`,
       - persists JSON report and prints summary.

    4. Add npm script in `apps/api/package.json`:
       - `etl:verify:taxonomy`.

    5. Add tests for report math and CLI orchestration (mocked datasets; no live DB required).
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/taxonomy-reconciliation-report.spec.ts src/etl/__tests__/verify-taxonomy-reconciliation.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
    - Reconciliation command exists and produces taxonomy-specific report output.
    - Report includes expected link counts and rerun-safety fields.
    - Phase has objective evidence for TAX-04 and TAX-06 completion checks.
  </done>
</task>

</tasks>

<verification>
1. ETL taxonomy loaders extract from wp_terms, wp_term_taxonomy, and wp_term_relationships.
2. ETL migration writes Term records and _NovelToTerm links with rerun-safe semantics.
3. Type-check and targeted tests pass for loader, runner, and taxonomy verification modules.
4. Taxonomy reconciliation output includes source/target totals, link deltas, and rerun-safety evidence.
</verification>

<success_criteria>
- [ ] TAX-04 covered: existing novels can be linked to migrated terms via reconstructed WordPress relationships.
- [ ] TAX-05 covered: taxonomy ETL integration does not regress existing novel/uploader/chapter behavior.
- [ ] TAX-06 covered: ETL and link writes are rerunnable without duplicate growth.
- [ ] `etl:verify:taxonomy` generates deterministic reconciliation evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-taxonomy-etl-backfill-verification/11-01-SUMMARY.md`
</output>
