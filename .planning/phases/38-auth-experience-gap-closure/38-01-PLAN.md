---
phase: 38-auth-experience-gap-closure
plan: 01
type: execute
wave: 1
depends_on: [36-01, 37-01]
files_modified:
  - apps/web/src/features/auth/types.ts
  - apps/web/src/features/auth/api.ts
  - apps/web/src/features/auth/validation.ts
  - apps/web/src/features/auth/__tests__/auth-local-api.test.ts
  - apps/web/src/features/auth/__tests__/auth-local-forms.dom.test.jsx
  - apps/web/app/auth/login/page.tsx
  - apps/web/app/auth/register/page.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - .planning/phases/35-frontend-local-authentication/35-01-SUMMARY.md
  - .planning/phases/35-frontend-local-authentication/35-VERIFICATION.md
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Users can register with username/email/password and are signed in immediately after success"
    - "Users can login with username or email plus password and land in authenticated dashboard flows"
    - "Validation, duplicate-account, invalid-credential, and network failures show clear inline feedback"
    - "Auth session handoff works for downstream account flows in phases 36 and 37"
  artifacts:
    - path: "apps/web/app/auth/login/page.tsx"
      provides: "Local login form integrated with session persistence and AppContext"
      min_lines: 120
    - path: "apps/web/app/auth/register/page.tsx"
      provides: "Local registration form with password strength and duplicate handling"
      min_lines: 140
    - path: "apps/web/src/features/auth/api.ts"
      provides: "Typed wrappers for /auth/login and /auth/register plus error normalization"
      exports: ["loginLocal", "registerLocal", "extractAuthErrorMessage"]
    - path: ".planning/phases/35-frontend-local-authentication/35-VERIFICATION.md"
      provides: "Requirement-level verification evidence for AUTH-01 and AUTH-02"
      contains: "AUTH-01"
  key_links:
    - from: "apps/web/src/features/auth/api.ts"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "POST /auth/login and POST /auth/register"
      pattern: "/auth/login|/auth/register"
    - from: "apps/web/app/auth/login/page.tsx"
      to: "apps/web/src/lib/auth/session-store.ts"
      via: "persistSessionToken and persistSessionToStorage"
      pattern: "persistSessionToken|persistSessionToStorage"
    - from: "apps/web/app/auth/register/page.tsx"
      to: "apps/web/src/providers/app-provider.tsx"
      via: "setUser after successful register/login"
      pattern: "setUser"
---

<objective>
Close v1.14 auth requirement gaps by fully executing the previously planned local-auth frontend scope and producing missing verification artifacts.

Purpose: resolve orphaned AUTH-01/AUTH-02 and unblock milestone re-audit by adding implementation + proof artifacts required by the milestone audit.
Output: completed local-auth UI/API/test work and phase-35 summary/verification artifacts with requirement evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.14-MILESTONE-AUDIT.md
@.planning/phases/35-frontend-local-authentication/35-01-PLAN.md
@.planning/phases/36-user-profile-security-management/36-01-SUMMARY.md
@.planning/phases/37-wallet-and-purchase-history-dashboard/37-01-SUMMARY.md
@apps/api/src/auth/auth.controller.ts
@apps/api/src/auth/dto/login.dto.ts
@apps/api/src/auth/dto/register.dto.ts
@apps/web/src/lib/api/http.ts
@apps/web/src/lib/auth/session-store.ts
@apps/web/src/providers/app-provider.tsx
@apps/web/src/features/dashboard/dashboard.tsx
@apps/web/app/auth/login/page.tsx
@apps/web/app/page.tsx

<interfaces>
From apps/api/src/auth/dto/login.dto.ts:
```typescript
class LoginDto {
  username: string; // username or email identifier
  password: string;
  rememberMe?: boolean;
}
```

From apps/api/src/auth/dto/register.dto.ts:
```typescript
class RegisterDto {
  username: string;
  email: string;
  password: string; // 8-128, must include letter and number
}
```

From apps/web/src/lib/api/http.ts:
```typescript
export async function apiRequest<T>(path: string, options?: RequestOptions): Promise<ApiResult<T>>;
```

From apps/web/src/lib/auth/session-store.ts:
```typescript
export function persistSessionToken(token: string): void;
export function persistSessionToStorage(user: SessionPayload["user"]): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement local auth feature module and login/register UI flows (AUTH-01, AUTH-02)</name>
  <files>
    apps/web/src/features/auth/types.ts
    apps/web/src/features/auth/validation.ts
    apps/web/src/features/auth/api.ts
    apps/web/app/auth/login/page.tsx
    apps/web/app/auth/register/page.tsx
    apps/web/app/page.tsx
    apps/web/app/globals.css
  </files>
  <behavior>
    - Test 1: login accepts username or email identifier plus password and sends backend-compatible payload.
    - Test 2: register enforces client-side rules (email format, username pattern, password 8+ chars with letter+number).
    - Test 3: successful login/register persists token and session user, then transitions into authenticated dashboard path.
    - Test 4: duplicate email/username, invalid credentials, and network failures display actionable inline error messages.
  </behavior>
  <action>
Implement the phase-35 auth scope per roadmap and audit gap closure requirements.

Details:
- Build typed feature contracts and validators in `features/auth/*` aligned to backend DTO constraints.
- Implement `loginLocal` and `registerLocal` wrappers against `/auth/login` and `/auth/register` using existing `apiRequest`.
- Upgrade `app/auth/login/page.tsx` and `app/auth/register/page.tsx` with responsive forms, validation, submit states, error mapping, and session persistence.
- Preserve Google sign-in option on login while making local auth primary for this phase.
- Ensure successful auth updates both `session-store` and `AppContext` so downstream dashboard areas used by phases 36/37 are immediately accessible.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
Local auth UX and API integration are complete and AUTH-01/AUTH-02 behaviors are implemented in production code.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add automated auth regression coverage and prove cross-flow compatibility</name>
  <files>
    apps/web/src/features/auth/__tests__/auth-local-api.test.ts
    apps/web/src/features/auth/__tests__/auth-local-forms.dom.test.jsx
  </files>
  <behavior>
    - Test 1: API wrapper tests verify payload mapping and stable user-facing error normalization.
    - Test 2: DOM tests cover invalid form blocking, success paths, and duplicate/credential/network error rendering.
    - Test 3: auth success path confirms behavior compatible with dashboard access assumptions introduced in phases 36 and 37.
  </behavior>
  <action>
Create and stabilize auth tests so gap closure is evidence-driven.

Details:
- Add focused API-contract tests for login/register wrappers and error extraction.
- Add DOM tests for login/register forms and state transitions after successful auth.
- Avoid brittle call-count assertions; validate user-visible outcomes and auth handoff semantics.
- Keep test scope local to auth feature while referencing dashboard handoff expectations from audit gaps.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/features/auth/__tests__/auth-local-api.test.ts src/features/auth/__tests__/auth-local-forms.dom.test.jsx && npm run check-types</automated>
  </verify>
  <done>
Automated tests pass and guard against regressions in AUTH-01/AUTH-02 plus auth-to-dashboard handoff behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Produce missing phase-35 completion evidence for milestone audit closure</name>
  <files>
    .planning/phases/35-frontend-local-authentication/35-01-SUMMARY.md
    .planning/phases/35-frontend-local-authentication/35-VERIFICATION.md
  </files>
  <action>
Create the missing artifacts called out in the v1.14 milestone audit.

Details:
- Write `35-01-SUMMARY.md` describing shipped auth implementation, files touched, and verification commands that passed.
- Write `35-VERIFICATION.md` with explicit requirement table entries for AUTH-01 and AUTH-02, status/evidence links, and any residual non-blocking debt.
- Ensure evidence language can be consumed by `/gsd:audit-milestone` without ambiguity.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-frontend-local-authentication/35-01-SUMMARY.md && test -f .planning/phases/35-frontend-local-authentication/35-VERIFICATION.md && grep -q "AUTH-01" .planning/phases/35-frontend-local-authentication/35-VERIFICATION.md && grep -q "AUTH-02" .planning/phases/35-frontend-local-authentication/35-VERIFICATION.md</automated>
  </verify>
  <done>
Phase 35 is no longer orphaned in milestone audit inputs because both summary and verification artifacts exist with requirement-level proof.
  </done>
</task>

</tasks>

<verification>
- Local login/register UX is validated with automated tests and type checks.
- Auth success path transitions into authenticated app usage without manual refresh.
- Missing phase-35 SUMMARY and VERIFICATION artifacts are present and reference AUTH-01/AUTH-02 evidence.
</verification>

<success_criteria>
AUTH-01 and AUTH-02 move from orphaned to satisfied, and milestone re-audit no longer reports missing phase-35 execution artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/38-auth-experience-gap-closure/38-01-SUMMARY.md`
</output>
