---
phase: 03-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/etl/verify-wallet-reconciliation.ts
  - apps/api/src/etl/reconciliation-report.ts
  - apps/api/src/etl/__tests__/verify-wallet-reconciliation.spec.ts
  - apps/api/package.json
  - apps/api/tmp/wallet-reconciliation.json
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "Source and target wallet totals can be computed in one command and compared deterministically."
    - "Verification output includes both a global total diff and user-level mismatch details."
    - "Running reconciliation repeatedly against unchanged data yields the same totals and mismatch set."
  artifacts:
    - path: "apps/api/src/etl/verify-wallet-reconciliation.ts"
      provides: "CLI reconciliation runner comparing WordPress and PostgreSQL wallet sums"
      min_lines: 120
    - path: "apps/api/src/etl/reconciliation-report.ts"
      provides: "Structured result formatter and discrepancy report builder"
      min_lines: 60
    - path: "apps/api/tmp/wallet-reconciliation.json"
      provides: "Auditable reconciliation evidence with totals and per-user diffs"
  key_links:
    - from: "apps/api/src/etl/verify-wallet-reconciliation.ts"
      to: "wp_users.price"
      via: "source aggregate + per-user extraction"
      pattern: "SUM\\(price\\)|SELECT ID, price"
    - from: "apps/api/src/etl/verify-wallet-reconciliation.ts"
      to: "wallets.balance"
      via: "target aggregate + per-user extraction"
      pattern: "SUM\\(balance\\)|SELECT userId, balance"
    - from: "apps/api/src/etl/reconciliation-report.ts"
      to: "apps/api/tmp/wallet-reconciliation.json"
      via: "JSON artifact persistence"
      pattern: "totalSource|totalTarget|delta|mismatches"
---

<objective>
Implement the first Phase 3 verification workflow for wallet balance reconciliation.

Purpose: Prove financial migration integrity by matching legacy wallet totals against normalized target totals.
Output: Reconciliation CLI + report artifact that captures aggregate totals and per-user discrepancies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification/03-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/etl/source-mysql-loaders.ts
@apps/api/src/etl/config.ts
@apps/api/src/etl/mysql-client.ts
@apps/api/src/etl/prisma-client.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Wallet {
  userId  Int     @unique
  balance Decimal @db.Decimal(20, 2)
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

From apps/api/src/etl/source-mysql-loaders.ts (wallet source):
```typescript
SELECT ID, price FROM wp_users
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build wallet reconciliation query layer and deterministic diff model</name>
  <files>apps/api/src/etl/verify-wallet-reconciliation.ts, apps/api/src/etl/reconciliation-report.ts, apps/api/src/etl/__tests__/verify-wallet-reconciliation.spec.ts</files>
  <behavior>
    - Reconciliation computes source total, target total, and delta using fixed 2-decimal normalization.
    - Reconciliation identifies per-user mismatch rows where source and target balances differ.
    - Output model is deterministic and sorted by user ID for stable rerun diffs.
  </behavior>
  <action>
Implement a dedicated verification module that connects to both databases, fetches source wallet values from `wp_users.price`, fetches target wallet values from `wallets.balance`, and computes both aggregate and per-user diffs. Preserve D-10 by prioritizing wallet checks first. Use strict decimal normalization (2 places) to avoid floating-point noise. Write tests first for aggregation math, mismatch detection, and deterministic sort order.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand verify-wallet-reconciliation.spec.ts</automated>
  </verify>
  <done>Wallet reconciliation logic produces deterministic totals and mismatch lists with test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CLI command and persist audit artifact for reconciliation evidence</name>
  <files>apps/api/src/etl/verify-wallet-reconciliation.ts, apps/api/package.json, apps/api/tmp/wallet-reconciliation.json</files>
  <behavior>
    - One CLI command runs reconciliation and prints summary totals to stdout.
    - Reconciliation writes `apps/api/tmp/wallet-reconciliation.json` with totals, delta, and mismatch rows.
    - Command is rerunnable and updates the same artifact path without creating ambiguous duplicates.
  </behavior>
  <action>
Expose a script entry in `apps/api/package.json` (for example `etl:verify:wallet`) that runs the wallet reconciliation module and persists the report JSON artifact. Ensure output uses the established ETL path conventions and remains focused on verification scope per D-11 (no scaffold cleanup). Include explicit non-zero exit behavior only for unrecoverable DB/config errors; mismatches should be reported, not treated as runtime crashes.
  </action>
  <verify>
    <automated>cd apps/api && npm run check-types && npm run etl:verify:wallet</automated>
  </verify>
  <done>Wallet verification command generates a stable report artifact and provides actionable total/diff output.</done>
</task>

</tasks>

<verification>
Phase 3 kickoff is complete when the wallet reconciliation command can be run repeatedly to produce consistent totals and a machine-readable discrepancy report.
</verification>

<success_criteria>
- VER-01 has an executable reconciliation command and report artifact.
- Aggregate source vs target wallet totals are visible in command output and report JSON.
- User-level mismatches are listed for manual investigation and remediation.
- Report generation is deterministic and safe to rerun.
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification/03-01-SUMMARY.md`.
</output>
