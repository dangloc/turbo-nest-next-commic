---
phase: 03-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/api/src/etl/verify-social-mappings.ts
  - apps/api/src/etl/social-mapping-verification-report.ts
  - apps/api/src/etl/__tests__/verify-social-mappings.spec.ts
  - apps/api/package.json
  - apps/api/tmp/social-mapping-verification.json
autonomous: true
requirements:
  - VER-03
must_haves:
  truths:
    - "Google social mappings are compared between source and target with normalized keys."
    - "Report distinguishes missing-in-target and extra-in-target mappings."
    - "Verification can be rerun deterministically against unchanged data."
  artifacts:
    - path: "apps/api/src/etl/verify-social-mappings.ts"
      provides: "CLI verification command for social mapping parity"
      min_lines: 120
    - path: "apps/api/src/etl/social-mapping-verification-report.ts"
      provides: "Deterministic diff report for social mapping sets"
      min_lines: 70
    - path: "apps/api/tmp/social-mapping-verification.json"
      provides: "Auditable social mapping verification artifact"
  key_links:
    - from: "apps/api/src/etl/verify-social-mappings.ts"
      to: "wp_social_users"
      via: "source google mapping extraction"
      pattern: "provider|social_type|provider_id"
    - from: "apps/api/src/etl/verify-social-mappings.ts"
      to: "user_providers"
      via: "target google mapping extraction"
      pattern: "provider='google'"
---

<objective>
Implement Google social mapping verification to satisfy VER-03.

Purpose: prove identity provider linkage parity for migrated Google users.
Output: CLI verification command and machine-readable diff report.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build social mapping comparison and deterministic diff report</name>
  <files>apps/api/src/etl/verify-social-mappings.ts, apps/api/src/etl/social-mapping-verification-report.ts, apps/api/src/etl/__tests__/verify-social-mappings.spec.ts</files>
  <behavior>
    - Source and target Google mapping sets are normalized to stable keys.
    - Report outputs missing and extra mappings deterministically.
    - Aggregate counts are included for source, target, and matched keys.
  </behavior>
  <action>
Implement set-based comparison for Google provider mappings using normalized key format (`userId|provider|providerId`). Include explicit arrays for `missingInTarget` and `extraInTarget`. Add tests for deterministic sorting and count math.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand verify-social-mappings.spec.ts</automated>
  </verify>
  <done>Social mapping diff logic is tested and produces stable outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose social verification CLI and write artifact</name>
  <files>apps/api/src/etl/verify-social-mappings.ts, apps/api/package.json, apps/api/tmp/social-mapping-verification.json</files>
  <behavior>
    - One command runs social mapping verification.
    - Artifact path is canonical and rerun-safe.
    - Command prints matched/missing/extra counts.
  </behavior>
  <action>
Add npm script `etl:verify:social` and persist report JSON to `apps/api/tmp/social-mapping-verification.json` using canonical path resolution. Ensure command does not crash on non-zero mismatches; mismatches are reported as data findings.
  </action>
  <verify>
    <automated>cd apps/api && npm run check-types && npm run etl:verify:social</automated>
  </verify>
  <done>Social mapping verification command generates report and summary counts suitable for signoff.</done>
</task>

</tasks>
