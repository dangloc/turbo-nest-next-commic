---
phase: 03-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/api/src/etl/verify-purchased-chapters.ts
  - apps/api/src/etl/purchased-chapter-verification-report.ts
  - apps/api/src/etl/__tests__/verify-purchased-chapters.spec.ts
  - apps/api/package.json
  - apps/api/tmp/purchased-chapter-reconciliation.json
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - "Successfully decoded purchased chapter totals from source are cross-checked against target rows."
    - "Verification output contains aggregate totals and per-user mismatch rows."
    - "Decode failures are visible in report metadata and excluded from source-success totals."
  artifacts:
    - path: "apps/api/src/etl/verify-purchased-chapters.ts"
      provides: "CLI verification command for purchased chapter reconciliation"
      min_lines: 120
    - path: "apps/api/src/etl/purchased-chapter-verification-report.ts"
      provides: "Deterministic report model for totals and mismatches"
      min_lines: 70
    - path: "apps/api/tmp/purchased-chapter-reconciliation.json"
      provides: "Auditable purchased chapter reconciliation evidence"
  key_links:
    - from: "apps/api/src/etl/verify-purchased-chapters.ts"
      to: "parsePurchasedChapters"
      via: "source successful decode counting"
      pattern: "parsePurchasedChapters"
    - from: "apps/api/src/etl/verify-purchased-chapters.ts"
      to: "purchased_chapters"
      via: "target per-user and total counts"
      pattern: "groupBy|COUNT"
---

<objective>
Implement purchased chapter reconciliation to satisfy VER-02.

Purpose: prove target purchased chapter history matches successfully decoded source records.
Output: runnable CLI verification and machine-readable report with totals + mismatches.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build purchased chapter verification and report model</name>
  <files>apps/api/src/etl/verify-purchased-chapters.ts, apps/api/src/etl/purchased-chapter-verification-report.ts, apps/api/src/etl/__tests__/verify-purchased-chapters.spec.ts</files>
  <behavior>
    - Source successful decode total and target total are computed deterministically.
    - Per-user mismatches are sorted by userId.
    - Decode failure counts are surfaced explicitly.
  </behavior>
  <action>
Implement source extraction from `_purchased_chapters`, decode with existing parser, and compare against target `purchased_chapters` counts by user. Include decode-failure metadata so only successfully decoded source rows are used for equality checks. Add unit tests for totals and mismatch ordering.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- --runInBand verify-purchased-chapters.spec.ts</automated>
  </verify>
  <done>Purchased chapter verification logic and tests pass with deterministic reconciliation output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose CLI command and persist reconciliation artifact</name>
  <files>apps/api/src/etl/verify-purchased-chapters.ts, apps/api/package.json, apps/api/tmp/purchased-chapter-reconciliation.json</files>
  <behavior>
    - One command runs purchased chapter reconciliation.
    - JSON artifact is written to canonical tmp path.
    - Command reports total delta and mismatch count.
  </behavior>
  <action>
Add npm script `etl:verify:purchases` and ensure the command persists report JSON in `apps/api/tmp/purchased-chapter-reconciliation.json` with path normalization behavior matching existing ETL outputs.
  </action>
  <verify>
    <automated>cd apps/api && npm run check-types && npm run etl:verify:purchases</automated>
  </verify>
  <done>Purchased chapter reconciliation command produces auditable artifact and usable summary output.</done>
</task>

</tasks>
