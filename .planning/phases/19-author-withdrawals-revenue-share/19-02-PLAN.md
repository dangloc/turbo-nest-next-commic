---
phase: 19-author-withdrawals-revenue-share
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - apps/api/src/finance/withdrawal-admin.controller.ts
  - apps/api/src/finance/finance.service.ts
  - apps/api/src/finance/finance.module.ts
  - apps/api/src/finance/types.ts
  - apps/api/src/finance/__tests__/withdrawal-admin.spec.ts
autonomous: true
requirements: [WDR-02, WDR-03]
must_haves:
  truths:
    - "Admins can list withdrawal requests with status and author filters to manage payout queue"
    - "Admins can approve or reject pending requests with deterministic state transitions"
    - "Rejecting a request restores frozen earnings, while approving finalizes payout state with audit records"
  artifacts:
    - path: "apps/api/src/finance/withdrawal-admin.controller.ts"
      provides: "Admin withdrawal list and decision endpoints"
      min_lines: 90
    - path: "apps/api/src/finance/finance.service.ts"
      provides: "Admin list/approve/reject service operations with transactional safety"
      min_lines: 420
    - path: "apps/api/src/finance/__tests__/withdrawal-admin.spec.ts"
      provides: "Admin workflow tests for listing and status transitions"
      min_lines: 150
  key_links:
    - from: "apps/api/src/finance/withdrawal-admin.controller.ts"
      to: "apps/api/src/finance/finance.service.ts"
      via: "listWithdrawalRequests() and resolveWithdrawalRequest()"
      pattern: "listWithdrawalRequests|resolveWithdrawalRequest"
    - from: "apps/api/src/finance/finance.service.ts"
      to: "prisma.withdrawalRequest/prisma.wallet/prisma.transaction"
      via: "approval and rejection state transitions"
      pattern: "prisma\\.(withdrawalRequest|wallet|transaction)"
    - from: "apps/api/src/finance/finance.module.ts"
      to: "withdrawal-admin.controller.ts"
      via: "controller registration"
      pattern: "WithdrawalAdminController"
---

<objective>
Implement admin withdrawal management APIs so payout operations are filterable, auditable, and balance-safe across approve/reject decisions.

Purpose: complete withdrawal operations and governance controls required for financial operations.
Output: admin withdrawal list + decision endpoints, transactional service logic, and WDR-02/WDR-03 tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-author-withdrawals-revenue-share/19-01-PLAN.md
@apps/api/prisma/schema.prisma
@apps/api/src/finance/finance.service.ts
@apps/api/src/auth/guards/roles.guard.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
enum WithdrawalStatus {
  PENDING
  APPROVED
  REJECTED
  PAID
  CANCELED
}

model WithdrawalRequest {
  id              Int
  authorProfileId Int
  amount          Decimal
  status          WithdrawalStatus
  note            String?
  requestedAt     DateTime
  processedAt     DateTime?
}
```

From apps/api/src/auth/guards/roles.guard.ts:
```typescript
@UseGuards(RolesGuard)
@Roles('ADMIN')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin withdrawal listing API with status/filter pagination</name>
  <files>
    apps/api/src/finance/types.ts
    apps/api/src/finance/withdrawal-admin.controller.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/finance.module.ts
    apps/api/src/finance/__tests__/withdrawal-admin.spec.ts
  </files>
  <behavior>
    - Test 1: admin can list withdrawal requests filtered by status and author profile id.
    - Test 2: list response includes payout context fields (amount, status, requestedAt, processedAt, author identity).
    - Test 3: non-admin role is denied by route metadata and guard behavior.
  </behavior>
  <action>
Build admin queue visibility endpoint.

Implementation details:
- Add guarded admin endpoint `GET /finance/admin/withdrawals` with query filters (`status`, `authorProfileId`, `page`, `pageSize`).
- Implement service query with deterministic ordering (`requestedAt desc, id desc`) and total count for pagination metadata.
- Return normalized response object `{items, total, page, pageSize}` for dashboard consumption.
- Keep DTO/query parsing minimal and aligned with existing controller style in the project.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/withdrawal-admin.spec.ts</automated>
  </verify>
  <done>
Admins can retrieve a reliable withdrawal queue with operational filters and pagination context.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin approve/reject workflows with deterministic balance transitions</name>
  <files>
    apps/api/src/finance/withdrawal-admin.controller.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/__tests__/withdrawal-admin.spec.ts
  </files>
  <behavior>
    - Test 1: approving a pending request updates status and processed timestamp once, with no earned-balance refund.
    - Test 2: rejecting a pending request updates status and processed timestamp, and refunds frozen amount back to author earned balance.
    - Test 3: repeated decisions on non-pending requests are rejected and do not mutate balances.
  </behavior>
  <action>
Add admin decision APIs and transactional state machine behavior.

Implementation details:
- Add endpoints:
  - `POST /finance/admin/withdrawals/:id/approve`
  - `POST /finance/admin/withdrawals/:id/reject`
- Implement `resolveWithdrawalRequest(id, decision, note)` in `finance.service.ts` with strict `PENDING -> APPROVED|REJECTED` transitions.
- On reject, refund request amount back to author `wallet.earnedBalance` atomically.
- Write immutable transaction audit rows for approval/rejection/refund events using explicit `content` markers.
- Keep transition logic idempotent by rejecting second resolution attempts with clear errors.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/withdrawal-admin.spec.ts && npm test --workspace=api -- --runInBand && npm run check-types --workspace=api</automated>
  </verify>
  <done>
Admin payout decisions are deterministic, auditable, and balance-safe for both approval and rejection paths.
  </done>
</task>

</tasks>

<verification>
- Admin queue endpoints support real operations filtering and pagination.
- Approve/reject workflows enforce one-way state transitions and correct balance outcomes.
</verification>

<success_criteria>
WDR-02 and WDR-03 are complete with tested admin endpoints and transaction-safe payout state transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/19-author-withdrawals-revenue-share/19-02-SUMMARY.md`
</output>
