---
phase: 19-author-withdrawals-revenue-share
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/finance/types.ts
  - apps/api/src/finance/finance.service.ts
  - apps/api/src/finance/withdrawal-author.controller.ts
  - apps/api/src/finance/finance.module.ts
  - apps/api/src/finance/__tests__/purchase-revenue.spec.ts
  - apps/api/src/finance/__tests__/withdrawal-author.spec.ts
autonomous: true
requirements: [REV-01, WDR-01]
must_haves:
  truths:
    - "Each successful chapter purchase distributes revenue as 95% author earnings and 5% platform fee with auditable records"
    - "Authors can submit withdrawal requests only when they have enough earned balance"
    - "Creating a withdrawal request freezes funds immediately so they cannot be spent twice"
  artifacts:
    - path: "apps/api/src/finance/finance.service.ts"
      provides: "Transactional purchase split and author withdrawal request logic"
      min_lines: 320
    - path: "apps/api/src/finance/withdrawal-author.controller.ts"
      provides: "Authenticated author withdrawal request endpoint"
      min_lines: 60
    - path: "apps/api/src/finance/__tests__/purchase-revenue.spec.ts"
      provides: "Revenue split verification tests"
      min_lines: 120
    - path: "apps/api/src/finance/__tests__/withdrawal-author.spec.ts"
      provides: "Author withdrawal request behavior tests"
      min_lines: 120
  key_links:
    - from: "apps/api/src/finance/finance.service.ts"
      to: "prisma.wallet/prisma.purchasedChapter/prisma.transaction"
      via: "single purchase transaction with buyer debit + author/platform credit"
      pattern: "prisma\\.(wallet|purchasedChapter|transaction)"
    - from: "apps/api/src/finance/withdrawal-author.controller.ts"
      to: "apps/api/src/finance/finance.service.ts"
      via: "createWithdrawalRequest()"
      pattern: "createWithdrawalRequest"
    - from: "apps/api/src/finance/finance.module.ts"
      to: "withdrawal-author.controller.ts"
      via: "controller registration"
      pattern: "WithdrawalAuthorController"
---

<objective>
Implement revenue-sharing purchase settlement and author withdrawal request APIs so earnings are distributed transparently and authors can request payouts safely.

Purpose: complete the financial pipeline from buyer payment to author payout request readiness.
Output: updated finance service logic, author withdrawal endpoint, and targeted tests for REV-01 and WDR-01.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-deposit-purchase-apis/18-01-SUMMARY.md
@apps/api/prisma/schema.prisma
@apps/api/src/finance/finance.service.ts
@apps/api/src/finance/purchase.controller.ts
@apps/api/src/auth/guards/roles.guard.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model Wallet {
  userId           Int     @unique
  depositedBalance Decimal @default(0)
  earnedBalance    Decimal @default(0)
  totalDeposited   Decimal @default(0)
}

model Transaction {
  userId      Int
  amountIn    Decimal
  amountOut   Decimal
  accumulated Decimal
  type        TransactionType
  content     String?
}

model WithdrawalRequest {
  authorProfileId Int
  amount          Decimal
  status          WithdrawalStatus
  requestedAt     DateTime
  processedAt     DateTime?
}
```

From apps/api/src/finance/finance.service.ts:
```typescript
async purchaseChapter(userId: number, input: ChapterPurchaseInput)
```

From apps/api/src/auth/guards/roles.guard.ts:
```typescript
@UseGuards(RolesGuard)
@Roles('AUTHOR')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply 95/5 revenue split in purchase settlement with audit-safe ledger writes</name>
  <files>
    apps/api/src/finance/types.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/__tests__/purchase-revenue.spec.ts
  </files>
  <behavior>
    - Test 1: successful purchase deducts buyer deposited balance once and credits 95% to the chapter author's earned balance.
    - Test 2: successful purchase credits 5% platform fee to a deterministic platform wallet sink and writes auditable transaction rows.
    - Test 3: duplicate purchase and insufficient-funds paths do not apply any author/platform balance updates.
  </behavior>
  <action>
Refactor purchase settlement to include revenue distribution while preserving existing duplicate/funds protections.

Implementation details:
- Extend purchase workflow in `finance.service.ts` transaction to:
  1. debit buyer deposited balance,
  2. resolve chapter novel uploader as author beneficiary,
  3. credit author `wallet.earnedBalance` by 95% of price,
  4. credit platform `wallet.earnedBalance` by 5% of price,
  5. append transaction rows for buyer debit, author earning credit, and platform fee credit.
- Keep decimal-safe arithmetic and deterministic rounding strategy for 2-decimal currency.
- Preserve current purchase API response shape and duplicate-race safety behavior.
- Use structured `content` markers in `Transaction` rows to distinguish `PURCHASE_BUYER`, `PURCHASE_AUTHOR_REVENUE`, `PURCHASE_PLATFORM_FEE` until dedicated transaction types are introduced.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/purchase-revenue.spec.ts</automated>
  </verify>
  <done>
Revenue split is applied exactly once per successful purchase and fully represented in wallet + transaction state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add author withdrawal request endpoint with immediate earned-balance freeze</name>
  <files>
    apps/api/src/finance/withdrawal-author.controller.ts
    apps/api/src/finance/finance.service.ts
    apps/api/src/finance/finance.module.ts
    apps/api/src/finance/__tests__/withdrawal-author.spec.ts
  </files>
  <behavior>
    - Test 1: authenticated author with approved profile and sufficient earned balance can create a pending withdrawal request.
    - Test 2: requesting more than available earned balance is rejected and leaves balances unchanged.
    - Test 3: successful request reduces `wallet.earnedBalance` immediately and creates a `WithdrawalRequest` row atomically.
  </behavior>
  <action>
Create author-facing withdrawal submission API and service path.

Implementation details:
- Add guarded endpoint `POST /finance/withdrawals/requests` with roles restricted to `AUTHOR` and `ADMIN`.
- Implement `createWithdrawalRequest(userId, amount, note)` in `finance.service.ts`:
  - resolve approved `AuthorProfile` for caller,
  - validate positive amount,
  - ensure sufficient `wallet.earnedBalance`,
  - atomically decrement earned balance and create `WithdrawalRequest` with `PENDING` status.
- Add immutable transaction audit row for withdrawal-freeze event using current enum type plus explicit `content` marker.
- Register new author withdrawal controller in `finance.module.ts`.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/finance/__tests__/withdrawal-author.spec.ts</automated>
  </verify>
  <done>
Authors can submit valid withdrawal requests and funds are frozen transactionally at request time.
  </done>
</task>

</tasks>

<verification>
- Purchase revenue split updates buyer/author/platform wallets exactly once per successful purchase.
- Author withdrawal request flow prevents overspending and records pending requests with frozen funds.
</verification>

<success_criteria>
REV-01 and WDR-01 are complete with deterministic wallet changes, transaction audit records, and passing targeted tests.
</success_criteria>

<output>
After completion, create `.planning/phases/19-author-withdrawals-revenue-share/19-01-SUMMARY.md`
</output>
