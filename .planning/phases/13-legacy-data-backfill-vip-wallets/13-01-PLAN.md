---
phase: 13-legacy-data-backfill-vip-wallets
plan: 01
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - apps/api/src/etl/types.ts
  - apps/api/src/etl/source-mysql-loaders.ts
  - apps/api/src/etl/prisma-repositories.ts
  - apps/api/src/etl/etl-runner.ts
  - apps/api/src/etl/index.ts
  - apps/api/src/etl/__tests__/content-source-loaders.spec.ts
  - apps/api/src/etl/__tests__/etl-runner.spec.ts
  - apps/api/src/etl/__tests__/etl-integration.spec.ts
  - apps/api/src/etl/verify-wallet-reconciliation.ts
  - apps/api/src/etl/__tests__/verify-wallet-reconciliation.spec.ts
autonomous: true
requirements: [ECO-16, ECO-17]

must_haves:
  truths:
    - "Legacy wp_vip_levels rows are migrated into VipLevel with preserved integer IDs and stable rerun behavior"
    - "Existing wallets are backfilled so depositedBalance and totalDeposited accurately reflect legacy balance baseline"
    - "Backfill can be rerun without duplicate VIP levels or repeated wallet inflation"
    - "Verification output proves source/target VIP parity and wallet aggregate consistency"
  artifacts:
    - path: "apps/api/src/etl/source-mysql-loaders.ts"
      provides: "WordPress VIP level source extraction"
      contains: "loadVipLevels"
    - path: "apps/api/src/etl/prisma-repositories.ts"
      provides: "Idempotent VipLevel and wallet backfill writes"
      contains: "vipLevelRepo|walletBackfillRepo"
    - path: "apps/api/src/etl/etl-runner.ts"
      provides: "Backfill stage orchestration"
      contains: "vip level backfill step|wallet split backfill step"
    - path: "apps/api/src/etl/verify-wallet-reconciliation.ts"
      provides: "Wallet and VIP parity verification output"
      contains: "legacy balance decomposition checks"
  key_links:
    - from: "wp_vip_levels.id"
      to: "VipLevel.id"
      via: "upsert by preserved source ID"
      pattern: "id"
    - from: "Wallet.balance"
      to: "Wallet.depositedBalance + Wallet.totalDeposited"
      via: "one-time baseline copy guarded for reruns"
      pattern: "depositedBalance|totalDeposited"
---

<objective>
Backfill legacy VIP and wallet financial data into the new v1.4 schema while preserving source identity, preventing duplicate growth, and producing deterministic verification evidence.

Purpose: Complete the migration bridge between schema expansion and production-safe data usage for VIP progression and wallet accounting.
Output: VIP source loader + idempotent backfill orchestration + verification evidence.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-platform-ecosystem-foundation-schema-expansion/12-01-SUMMARY.md
@apps/api/prisma/schema.prisma
@apps/api/src/etl/source-mysql-loaders.ts
@apps/api/src/etl/prisma-repositories.ts
@apps/api/src/etl/etl-runner.ts
@apps/api/src/etl/verify-wallet-reconciliation.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model VipLevel {
  id       Int @id
  name     String
  vndValue Int
  kimTe    Int
}

model Wallet {
  balance          Decimal
  depositedBalance Decimal
  earnedBalance    Decimal
  totalDeposited   Decimal
}
```

From ETL contracts:
```typescript
export function createSourceLoaders(client: MySqlClient) {
  return {
    async loadVipRows(): Promise<SourceVipRow[]> { ... }
  };
}

export function createPrismaRepositories(prisma: PrismaClient) {
  return {
    walletRepo: { upsert(...) }
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Legacy VIP Level Source Loader and Contracts</name>
  <files>
    apps/api/src/etl/types.ts,
    apps/api/src/etl/source-mysql-loaders.ts,
    apps/api/src/etl/__tests__/content-source-loaders.spec.ts
  </files>
  <behavior>
    - Test 1: wp_vip_levels rows are loaded into typed SourceVipLevelRow with preserved source id.
    - Test 2: missing optional visual fields map to null/empty defaults deterministically.
    - Test 3: invalid numeric rows are filtered out safely.
  </behavior>
  <action>
    Add `SourceVipLevelRow` ETL contract and implement `loadVipLevels()` in source loaders:

    1. Query `wp_vip_levels` with fields needed by `VipLevel` (`id`, `name`, threshold money value, kim_te, optional color/icon columns if present).
    2. Normalize with existing helper conventions (`asNumber`, `asString`) and guard invalid IDs.
    3. Extend loader tests to assert loader presence and mapping contracts.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/content-source-loaders.spec.ts</automated>
  </verify>
  <done>
    - Legacy VIP levels are extractable in typed form with preserved IDs.
    - Loader behavior is deterministic for missing/invalid source values.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Idempotent VIP + Wallet Backfill Stage in ETL Flow</name>
  <files>
    apps/api/src/etl/prisma-repositories.ts,
    apps/api/src/etl/etl-runner.ts,
    apps/api/src/etl/index.ts,
    apps/api/src/etl/__tests__/etl-runner.spec.ts,
    apps/api/src/etl/__tests__/etl-integration.spec.ts
  </files>
  <behavior>
    - Test 1: VipLevel rows upsert by id and remain stable on rerun.
    - Test 2: wallet backfill populates depositedBalance and totalDeposited from legacy balance baseline.
    - Test 3: rerun does not re-add baseline values when split fields are already populated.
  </behavior>
  <action>
    Wire backfill into ETL orchestration using additive, rerun-safe writes:

    1. Add `vipLevelRepo.upsert` keyed by `id`.
    2. Add `walletBackfillRepo.backfillFromLegacyBalance` that updates only records requiring baseline decomposition (guard condition on split fields).
    3. Extend `EtlRunnerDeps` and `index.ts` wiring with `loadVipLevels` and backfill repo methods.
    4. Execute VIP upsert and wallet backfill stages in a deterministic order and preserve existing flow behavior.
    5. Add test coverage proving rerun safety.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/etl-runner.spec.ts src/etl/__tests__/etl-integration.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
    - VipLevel target data is migrated from source with preserved ids.
    - Wallet split baseline is correctly populated without duplicate growth on rerun.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add VIP/Wallet Backfill Verification Report and CLI Checks</name>
  <files>
    apps/api/src/etl/verify-wallet-reconciliation.ts,
    apps/api/src/etl/__tests__/verify-wallet-reconciliation.spec.ts,
    apps/api/package.json
  </files>
  <behavior>
    - Test 1: verification reports source/target VIP level count parity.
    - Test 2: verification reports wallet aggregate parity for legacy balance vs deposited/total backfill.
    - Test 3: rerun safety branch marks no additional wallet inflation after repeat backfill.
  </behavior>
  <action>
    Extend wallet verification tooling to include VIP and legacy-balance decomposition checks:

    1. Add source-vs-target VIP count and ID mismatch checks.
    2. Add wallet integrity checks comparing legacy `balance` baseline against `depositedBalance` and `totalDeposited` aggregates.
    3. Expose final report output in existing verification command path.
    4. Update script aliases only if needed to preserve one-command verification.
  </action>
  <verify>
    <automated>npm test --workspace=api -- --runInBand src/etl/__tests__/verify-wallet-reconciliation.spec.ts && npm run check-types --workspace=api</automated>
  </verify>
  <done>
    - Verification output includes VIP and wallet backfill integrity evidence.
    - Backfill correctness and rerun safety are auditable by command output.
  </done>
</task>

</tasks>

<verification>
- VIP loader and ETL integration tests pass.
- Wallet backfill rerun-safety tests pass.
- Verification report proves parity and non-inflation behavior.
</verification>

<success_criteria>
- ECO-16 and ECO-17 are verifiably complete with rerun-safe evidence.
- Legacy VIP and wallet data are migrated without identity or value drift.
</success_criteria>

<output>
After completion, create `.planning/phases/13-legacy-data-backfill-vip-wallets/13-01-SUMMARY.md`
</output>
