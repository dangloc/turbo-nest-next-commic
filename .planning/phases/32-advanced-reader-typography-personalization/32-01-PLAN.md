---
phase: 32-advanced-reader-typography-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/features/reader/types.ts
  - apps/web/src/features/reader/preferences.ts
  - apps/web/src/features/reader/reader.tsx
autonomous: true
requirements: [READER-06]
must_haves:
  truths:
    - "Reader typography preferences are rehydrated when opening a chapter"
    - "Typography preferences persist across chapter navigation and browser reload"
    - "Reader falls back to safe defaults when no preference exists"
  artifacts:
    - path: "apps/web/src/features/reader/preferences.ts"
      provides: "Typed typography preference persistence helpers"
      exports: ["READER_TYPOGRAPHY_DEFAULTS", "loadReaderTypographyPreferences", "saveReaderTypographyPreferences"]
    - path: "apps/web/src/features/reader/types.ts"
      provides: "Typography preference contracts"
      exports: ["ReaderFontFamilyOption", "ReaderLineHeightOption", "ReaderContentWidthOption", "ReaderTypographyPreferences"]
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Preference rehydration and persistence wiring"
  key_links:
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/reader/preferences.ts"
      via: "load/save preference helpers"
      pattern: "loadReaderTypographyPreferences|saveReaderTypographyPreferences"
    - from: "apps/web/src/features/reader/preferences.ts"
      to: "window.localStorage"
      via: "stable reader preference keys"
      pattern: "reader:font|reader:line-height|reader:content-width"
---

<objective>
Define and wire a typed typography preference persistence model for chapter reader sessions.

Purpose: Close READER-06 by making typography settings durable and safely rehydrated before control-surface expansion.
Output: Reader typography contracts, storage helpers, and chapter-view rehydration integration.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-03-SUMMARY.md
@apps/web/src/features/reader/types.ts
@apps/web/src/features/reader/reader.tsx
@apps/web/app/globals.css

<interfaces>
From apps/web/src/features/reader/types.ts:
```typescript
export type ReaderFontSizeOption = "sm" | "md" | "lg";
export type ReaderThemeMode = "light" | "dark";
```

From apps/web/src/features/reader/reader.tsx:
```typescript
const [fontSize, setFontSize] = useState<ReaderFontSizeOption>("md");
const [themeMode, setThemeMode] = useState<ReaderThemeMode>("light");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define advanced typography contracts and persistence helpers</name>
  <files>
    apps/web/src/features/reader/types.ts
    apps/web/src/features/reader/preferences.ts
  </files>
  <behavior>
    - Test 1: Preference contract supports font family, line height, and content width with explicit option unions.
    - Test 2: Load helper returns defaults when local storage is unavailable or malformed.
    - Test 3: Save helper writes deterministic keys and serializes typed values only.
  </behavior>
  <action>
Create advanced typography option types and a dedicated reader preference helper module to centralize storage keys, default values, and serialization/rehydration logic. Keep this module backward-compatible with existing font-size/theme keys from phase 30.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Reader feature has reusable, typed preference contracts and persistence helpers for advanced typography settings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire chapter reader rehydration and persistence to helper module</name>
  <files>
    apps/web/src/features/reader/reader.tsx
  </files>
  <behavior>
    - Test 1: Chapter view rehydrates typography settings on mount and applies defaults if missing.
    - Test 2: Updating typography state persists through shared helper API.
    - Test 3: Navigating to another chapter keeps selected typography values without manual reset.
  </behavior>
  <action>
Refactor existing in-component localStorage access in chapter reader to use the new helper functions, and ensure typography preference state initializes once per mount before chapter content render. Preserve existing progression sync and purchase flows.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types && npm run lint -- --no-warn-ignored src/features/reader/types.ts src/features/reader/preferences.ts src/features/reader/reader.tsx</automated>
  </verify>
  <done>
    Reader typography persistence pipeline is centralized and stable across reload/navigation.
  </done>
</task>

</tasks>

<verification>
- Typography preferences rehydrate consistently on chapter open.
- Preference persistence remains stable across route changes and reloads.
- Reader typecheck and targeted lint pass.
</verification>

<success_criteria>
- READER-06 is met with deterministic persistence and rehydration for advanced typography preferences.
</success_criteria>

<output>
After completion, create `.planning/phases/32-advanced-reader-typography-personalization/32-01-SUMMARY.md`
</output>
