---
phase: 32-advanced-reader-typography-personalization
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - apps/web/src/features/reader/reader.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [READER-05, READER-06]
must_haves:
  truths:
    - "Reader can change font family, line height, and content width while reading"
    - "Typography changes apply immediately to chapter content without reload"
    - "Rehydrated typography values render correctly after page refresh"
  artifacts:
    - path: "apps/web/src/features/reader/reader.tsx"
      provides: "Typography control panel and class application wiring"
    - path: "apps/web/app/globals.css"
      provides: "Font family, line-height, and content-width style variants"
      contains: "reader-content--font-family-* / --line-height-* / --width-*"
  key_links:
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/app/globals.css"
      via: "reader-content class composition"
      pattern: "reader-content--font-family|reader-content--line-height|reader-content--width"
    - from: "apps/web/src/features/reader/reader.tsx"
      to: "apps/web/src/features/reader/preferences.ts"
      via: "control handlers update persisted values"
      pattern: "saveReaderTypographyPreferences"
---

<objective>
Deliver advanced typography controls and visual variants in chapter reader while preserving existing reader ergonomics.

Purpose: Close READER-05 and complete UI-facing READER-06 behavior for long-form reading comfort.
Output: Chapter control UI for font family/line height/content width plus CSS variant system.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reader-preferences-and-progression-sync/30-03-SUMMARY.md
@.planning/phases/32-advanced-reader-typography-personalization/32-01-PLAN.md
@apps/web/src/features/reader/reader.tsx
@apps/web/app/globals.css

<interfaces>
From apps/web/src/features/reader/reader.tsx:
```typescript
<article className={`reader-content reader-content--font-${fontSize} reader-content--theme-${themeMode}`}>...</article>
```

From apps/web/app/globals.css:
```css
.reader-content--font-sm { ... }
.reader-content--font-md { ... }
.reader-content--font-lg { ... }
.reader-content--theme-light { ... }
.reader-content--theme-dark { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add advanced typography controls in chapter reader UI</name>
  <files>
    apps/web/src/features/reader/reader.tsx
  </files>
  <behavior>
    - Test 1: User can select font family (serif/sans/mono) and content updates instantly.
    - Test 2: User can select line-height preset (compact/comfortable/airy) and paragraph readability changes instantly.
    - Test 3: User can select content width preset (narrow/standard/wide) and reader column width updates without layout breakage.
  </behavior>
  <action>
Extend the existing reader preference bar with three new controls (font family, line height, content width). Reuse preference helper APIs from 32-01 so control changes update persisted state and class composition in one flow. Keep existing font-size/theme controls and purchase/progression interactions intact.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types</automated>
  </verify>
  <done>
    Chapter reader exposes all advanced typography controls with immediate visual response.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typography style variants and responsive layout safety</name>
  <files>
    apps/web/app/globals.css
    apps/web/src/features/reader/reader.tsx
  </files>
  <behavior>
    - Test 1: Each typography option maps to a concrete CSS variant class.
    - Test 2: Combined typography classes render correctly on desktop and mobile without overflow.
    - Test 3: Reloaded preferences reuse the same classes and visual output.
  </behavior>
  <action>
Add CSS tokens/selectors for font-family, line-height, and content-width variants, and update reader content container class assembly to include new modifiers. Ensure responsive breakpoints preserve readability and avoid clipping in TOC + content split layout.
  </action>
  <verify>
    <automated>cd apps/web && npm run check-types && npm run lint -- --no-warn-ignored src/features/reader/types.ts src/features/reader/preferences.ts src/features/reader/reader.tsx app/globals.css</automated>
  </verify>
  <done>
    Advanced typography variants are fully wired and stable across viewport sizes and reload scenarios.
  </done>
</task>

</tasks>

<verification>
- All typography controls apply instantly in chapter reader.
- Visual variants remain legible and responsive across mobile/desktop widths.
- Typecheck and targeted lint pass.
</verification>

<success_criteria>
- READER-05 is met with immediate typography control behavior.
- READER-06 is fully represented in UI by persisted and rehydrated advanced typography rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/32-advanced-reader-typography-personalization/32-02-SUMMARY.md`
</output>
