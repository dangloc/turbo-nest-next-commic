---
phase: 09-ugc-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/*/migration.sql
autonomous: true
requirements: [UGC-01, UGC-02, UGC-03]

must_haves:
  truths:
    - "Each novel has an uploader linked to an existing user"
    - "Existing migrated novels receive uploaderId=1 without manual data scripts"
    - "Prisma migration applies successfully on current PostgreSQL state"
    - "Existing content ETL/reporting code remains compatible after schema update"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "User->Novel one-to-many relation and uploaderId default"
      contains: "uploaderId Int @default(1)"
    - path: "apps/api/prisma/migrations/*/migration.sql"
      provides: "DDL adding uploader_id with default and FK"
  key_links:
    - from: "User"
      to: "Novel"
      via: "uploaderId foreign key"
      pattern: "@relation\(fields: \[uploaderId\], references: \[id\]"
    - from: "Novel"
      to: "existing rows"
      via: "database default 1"
      pattern: "DEFAULT 1"
---

<objective>
Implement the UGC ownership foundation by linking novels to uploader users in Prisma and database schema, with safe default assignment for existing migrated novels.

Purpose: Establish ownership semantics required for future user-submitted novels without risking existing content data.
Output: Updated Prisma schema plus applied migration that backfills uploader ownership via default Admin ID.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/api/prisma/schema.prisma

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
model User {
  id                Int                @id
  ...
}

model Novel {
  id            Int                @id
  title         String
  postContent   String
  ...
}
```

Target contract for this phase:
```prisma
model User {
  ...
  uploadedNovels Novel[]
}

model Novel {
  ...
  uploaderId Int @default(1)
  uploader   User @relation(fields: [uploaderId], references: [id], onDelete: Restrict)
  @@index([uploaderId])
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Prisma Ownership Contract</name>
  <files>apps/api/prisma/schema.prisma</files>
  <behavior>
    - Test 1: Prisma schema validates with User->Novel one-to-many ownership relation.
    - Test 2: Novel requires uploaderId with default 1 and relation to User.id.
    - Test 3: Novel model exposes uploaderId index for ownership queries.
  </behavior>
  <action>
    Update `schema.prisma` to introduce UGC ownership per UGC-01 and UGC-02:

    1. In `User`, add collection relation for uploaded novels:
       - `uploadedNovels Novel[]`

    2. In `Novel`, add:
       - `uploaderId Int @default(1)`
       - `uploader User @relation(fields: [uploaderId], references: [id], onDelete: Restrict)`
       - `@@index([uploaderId])`

    3. Keep existing `purchasedChps` and `chapters` relations untouched.

    4. Run Prisma formatter/validator to ensure schema is syntactically and semantically valid.
  </action>
  <verify>
    <automated>npm run prisma:format --workspace=api && npm run prisma:validate --workspace=api</automated>
  </verify>
  <done>
    - Prisma schema compiles and validates.
    - Novel->User ownership relation exists with default uploaderId=1.
    - Existing relation graph remains intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and Apply PostgreSQL Migration</name>
  <files>apps/api/prisma/migrations/*/migration.sql</files>
  <action>
    Generate and apply migration for UGC-03 using the updated schema:

    1. Create migration in `apps/api`:
       - `npx prisma migrate dev --name add_novel_uploader_relation`

    2. Ensure generated SQL includes:
       - add `uploader_id` column on `novels`
       - default `1` on `uploader_id`
       - foreign key to `users(id)`
       - index on `uploader_id`

    3. Apply migration to local PostgreSQL and regenerate Prisma client.

    4. Verify existing rows are safe by checking novels with null uploader count is zero.
  </action>
  <verify>
    <automated>cd apps/api && npx prisma migrate dev --name add_novel_uploader_relation && npm run prisma:generate && npx prisma db execute --stdin --schema prisma/schema.prisma <<< "SELECT COUNT(*) FROM novels WHERE uploader_id IS NULL;"</automated>
  </verify>
  <done>
    - Migration folder exists with DDL for uploader ownership.
    - Migration applied successfully.
    - Existing novels are non-null for uploader ownership.
  </done>
</task>

<task type="auto">
  <name>Task 3: Compatibility Verification Against Existing Flow</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/*/migration.sql</files>
  <action>
    Run lightweight compatibility checks to ensure this schema change does not break current API/ETL baseline:

    1. Type check API package.
    2. Run existing content reconciliation verifier test suite.
    3. Run one ETL dry command path if env is present (`etl:migrate`), otherwise keep to compile+tests.

    Confirm no compile-time regressions from introducing `Novel.uploaderId` relation.
  </action>
  <verify>
    <automated>npm run check-types --workspace=api && npm test --workspace=api -- --runInBand src/etl/__tests__/verify-content-reconciliation.spec.ts</automated>
  </verify>
  <done>
    - API type-check remains green.
    - Existing reconciliation tests still pass.
    - Ownership schema update is ready for future UGC feature phases.
  </done>
</task>

</tasks>

<verification>
1. Prisma schema validates and formats successfully.
2. Migration SQL includes uploader_id default 1 plus FK/index.
3. Migration applies without failing on existing 176 novels.
4. API type-check/tests pass after schema update.
</verification>

<success_criteria>
- [ ] UGC-01 complete: User->Novel ownership relation exists.
- [ ] UGC-02 complete: Novel.uploaderId is required with default 1.
- [ ] UGC-03 complete: migration generated/applied and compatibility checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ugc-foundation/09-01-SUMMARY.md`
</output>
